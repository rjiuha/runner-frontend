# CLAUDE.md

Заметки по фронтенду Runner для себя (Claude) — чтобы не перечитывать весь проект на каждый запрос.
Правь этот файл при значимых изменениях архитектуры и веди раздел «Заметки» внизу.

## Проект

React Native (Expo 56, RN 0.85, React 19) клиент игры Runner — мультиплеер, пошаговая, 2-4 игрока.
Бэкенд — отдельный репозиторий `D:\Programming\runner-game-backend` (Symfony), доступ у меня **только на
чтение**. Контракты — `D:\Programming\runner-game-backend\README.md`, правила игры — загруженный
пользователем «Правила игры.txt» (мультиплеерная настолка про гонку отрядов по трассе). Полезно свериться
с бэковым `CLAUDE.md` в его репозитории — там расписана архитектура Orchestrator/Step-сервисов и Mercure.

Запуск: `npm run start` (Expo), `npm run web`, `npm run android`. Тестов нет.

## Структура

```
src/
  api/         клиент + эндпоинт-модули (client.js — токены/refresh/retry, ApiError, normalize.js,
               lobby.js, runnerGame.js)
  components/
    ui/        общие виджеты (Screen, Button, Input, ParallaxBackground)
    menu/      главное меню
    game/      всё про игровую доску (см. ниже), включая GameWaitingRoom (пред-игровой ready-up)
  config/      env.js — API_URL/MERCURE_URL, зависят от Platform.OS (эмулятор Android = 10.0.2.2)
  constants/   GameConstants.js (зеркалит бэковые enum'ы), mockGameData.js (заглушка ответа бэка,
               использовалась до подключения GameBoardScreen к реальному API)
  context/     AuthContext (обёртка над api/auth + токены)
  hooks/       useAuth (реэкспорт AuthContext), useMercure, useBoardLayout/useBoardScroll/useScrollAnimation/useLockLandscape
  lib/         мелкие утилиты без React (board.js, jwt.js, notify.js, storage.js, eventSource.js,
               hexDirection.js — гекс-соседи клетки, зеркало DirectionService на бэке)
  navigation/  RootNavigator (auth-gate), routes.js
  screens/     AuthScreen, MainMenuScreen, LobbySearchScreen, LobbyScreen, GameBoardScreen
  store/       lobbyReducer.js, runnerGameReducer.js — чистые редьюсеры для Mercure-событий
  theme/       единственный источник colors/spacing/font/radius/shadow
```

Конвенция файла: первая строка — комментарий с путём (`// src/screens/Foo.js`). Комментарии объясняют
**почему**, а не что. Стилей мало (inline объекты через `StyleSheet.create`), Tailwind нет.

## Ключевые архитектурные решения

**api/client.js** — токены в памяти модуля (не в React state), один общий `refreshPromise` на все
конкурентные 401, `credentials: 'include'` для Mercure-cookie. `ApiError` — единая обёртка с
`isNetwork/isUnauthorized/isGone/userMessage`.

**useMercure** (`hooks/useMercure.js`) — протокол «слушаем SSE → буферим → грузим снапшот REST → отсекаем
буфер по version → live». **Важный фикс**: `sync()` (REST-снапшот) запускается сразу в `connect()`, а не по
событию `'open'` у EventSource. Раньше был баг: если Mercure-хаб недоступен, `'open'` не наступает никогда,
и экран висел в вечном спиннере, хотя REST прекрасно работал. Если будешь трогать этот хук — не возвращай
эту гейтировку назад.

`Lobby::toArray()` на бэке **не отдаёт поле `version`** — `normalizeLobby` подставляет 0. На финальный баг
это не влияет (редьюсеры гасят дубли по id), но при реконнекте возможна логически лишняя обработка уже
учтённых событий. Не чинил — не приоритетно, но учитывай при отладке рассинхрона лобби. У `RunnerGame`
такого бага **больше нет** — пользователь сам добавил `version` в `RunnerGame::toArray()` на бэке
(`Entity/RunnerGame.php:420`), перепроверено чтением файла.

**Screen.js** — общий каркас всех обычных экранов: SafeAreaView + `ParallaxBackground` (единый на все
экраны, рендерится тут один раз, а не в каждом экране) + опциональный scroll/KeyboardAvoidingView. Экран
логина/меню/лобби всегда через `<Screen>`. **GameBoardScreen — исключение**, он полноэкранный и рулит
раскладкой сам (без safe-area отступов), но тоже вставляет `<ParallaxBackground />` вручную для
единообразия фона.

**Ориентация GameBoardScreen** — с 2026-08-31 (шестой заход) `orientation` из `useBoardLayout`
(`'landscape'/'portrait'` по фактическому соотношению `screenW`/`screenH`, адаптивно и на вебе через
ресайз окна, и на мобильных через физический поворот) управляет ТОЛЬКО расположением ПАНЕЛИ игрока —
слева на широком окне ('landscape'), снизу на узком ('portrait'). Сама ДОРОГА (`BoardGrid`) — ВСЕГДА
рисуется в портретной геометрии (вертикальные дорожки, скролл по вертикали, стрелки вверх/вниз),
независимо от `orientation` — `<BoardGrid orientation="portrait">` в `GameBoardScreen` захардкожен, не
завязан на хук. До этого захода `orientation` управлял ОБЕИМ осями сразу (была отдельная "альбомная"
геометрия дороги — горизонтальные дорожки) — разделено по прямому запросу пользователя. `useBoardLayout`
считает геометрию дороги ОДНОЙ общей функцией (`computeRoadGeometry`) от прямоугольника, который ей
достаётся (после вычета места под панель — слева или снизу, в зависимости от `orientation`), а не двумя
раздельными ветками. **Движение по трассе идёт снизу вверх** (начало трассы — у панели игрока) —
реализовано в `BoardGrid` через `flexDirection: 'column-reverse'` (чисто layout-свойство, без
transform/отражений — старая версия на `transform: scaleY(-1)` заменена ещё в пятнадцатом-шестнадцатом
заходах 2026-08-13, эта строка была не обновлена вовремя), подробности и вся геометрия разобраны в шапке
`BoardGrid.js`. `useAdaptiveOrientation` (бывший `useLockLandscape`) на мобильных снимает системный лок
ориентации на время экрана сессии и возвращает portrait-лок при уходе — раскладку выбирает сам экран
через `orientation`, не через системный поворот. Координаты ячеек (`cell.id`/`row`/`col`/`blockIndex`,
которые видит `GameBoardScreen.handleCellPress`) от `orientation` не зависят вообще — вся игровая логика
(SELECT/MOVE/SHOOT/подсветка) не в курсе, где сейчас панель.

**GameBoardScreen** — с Фазы 1 (см. запись 2026-08-12, седьмой заход, ниже) подключён к реальному
`GET /api/runner_game` + Mercure-топику `runner_game_{id}` через `useMercure`/`runnerGameReducer`, тем же
протоколом «буфер → снапшот → live», что и `LobbyScreen`. Раскладка: слева `PlayerInfoPanel`
(переключатель игроков, кубики, drag-and-drop на зоны усилений через
`react-native-gesture-handler`+`reanimated`, карточки бегунов), справа `BoardGrid` (сетка 6×8×3 сегмента,
скролл через `useBoardScroll`: PanResponder на мобильных, `useScrollAnimation` на вебе). Геометрия —
`useBoardLayout`, считает так, чтобы один фрагмент 8 колонок помещался в правую зону без обрезки.
`lib/board.js` разворачивает `trackBegin/Middle/End` в плоский список ячеек и резолвит типы клеток
(`danger_2/danger_3` → `danger`, `anomaly` → `danger`, нет данных → `road`). С Фазы 2 (запись
2026-08-12, девятый заход, ниже) select/move/collision/shoot/ability вызывают реальный бэк.
**Принципиально: локального стейта "что назначено" в GameBoardScreen нет** — как только SELECT/ABILITY
проходят, `runner.dice`/`player.ability`/`player.diceN` обновляются live-событиями
(`runnerGameReducer`), и UI просто читает их напрямую (см. `PlayerInfoPanel`/`RunnerCard`). Единственный
переходный локальный стейт — `pendingAbility` (heal/reaper ждут второй тап после дропа кубика на зону,
т.к. бэк принимает `runnerId`/`positionX/Y/segment` одним вызовом `/ability`, а не двумя шагами).
Подсветка легальных клеток (MOVE/SHOOT/reaper-размещение/первый выход на трассу) — `lib/hexDirection.js`
(`forwardNeighbors`), зеркалит `DirectionService::run()`+`canShoot()` на бэке.

**Партия имеет собственный ready-up гейт, отдельный от лобби**: после `game_created` (лобби) игра
рождается со статусом `waiting`, и каждый игрок обязан вызвать `POST /runner_game/start`, прежде чем
статус станет `active` (событие `game_active`). `GameBoardScreen` рисует `GameWaitingRoom` при
`status === GAME_STATUS.WAITING` и сам переключается на доску, когда статус меняется — доп. навигации не
нужно, компонент просто по-другому рендерит тот же live-стейт.

**Коллизия сигналится НЕ через `player.step`** (в бэковом enum `PlayerStep` нет кейса COLLISION) — а через
`game.extraTurnPlayer`/поле `extraTurnPlayer` события `game_turn_changed`. Пока оно не `null` — бэк
блокирует вообще все игровые действия всем игрокам, и никакого таймаута на бэке нет: если игрок-цель не
ответит на `/collision`, партия зависает навсегда. Фронт (Фаза 1) это не резолвит, только показывает
баннер и через 18 сек даёт кнопку ручного `resync()` — см. `GameBoardScreen.COLLISION_STUCK_TIMEOUT`.

`GestureHandlerRootView` обязателен в `App.js` (уже добавлен) — без него драг кубиков молча не работает на
Android.

**Анимации бегунов (Скаут/Солдат/Атлет — `RUNNER_TYPES.SPRINTER/ATHLETE/TANK`)** — начало Фазы 3, см.
`2026-08-31` в «Заметки» ниже за полную историю живого тестирования и найденные баги. Архитектура:
- `constants/runnerAnimations.js` — реестр gif-ассетов на `{type, status}`, только для типов, для которых
  реально есть набор: скаут (`assets/images/runners/scout/`), солдат (`.../trooper/`,
  `RUNNER_TYPES.ATHLETE` — не путать с папкой, названия разъехались после переименования лейблов, см.
  находку 2026-08-31 в «Заметки»), атлет (`.../athlet/`, `RUNNER_TYPES.TANK`) — `getRunnerAnimationImage`/
  `getRunnerAvatarImage` возвращают `null` для остальных типов (пока только Жнец), и `RunnerToken` сам
  откатывается на старую статичную иконку (`RUNNER_DISPLAY[type].icon`) — добавление анимаций следующему
  типу бегуна требует только новую запись в этом реестре, без правок компонентов.
- **Перекраска неона под цвет игрока** (2026-08-31, см. находку в «Заметки»): каждая запись в реестре —
  не один gif, а пара `{base, mask}`. `base` — тот же кадр, но неоновые вставки обесцвечены (десатурация
  с сохранением светлоты) в тон тёмного костюма. `mask` — те же кадры, только неон залит чистым белым на
  прозрачном фоне, всё остальное прозрачно. `RunnerToken` рисует пару ДВУМЯ наложенными `<Image>`: base
  как есть, mask поверх с `tintColor={color}` (цвет игрока, уже приходил в компонент) — так неон красится
  в цвет команды, а сам спрайт (тёмный костюм, метки повреждений и т.п.) не трогается. Обе гифки на пару
  сгенерированы автоматически скриптом (порог по HSL: `hue 150–255°, saturation ≥20%, lightness 35–96%`)
  из оригинальных ассетов пользователя — сам скрипт не сохранён в репозитории (одноразовый, в scratchpad
  сессии), но порог задокументирован здесь на случай, если понадобится перегенерировать. Оригинальные
  (без суффикса) gif оставлены в assets — код их больше не использует, но на случай пересчёта порога они
  нужны как исходники.
- `hooks/useRunnerAnimations.js` — стейт-стор `{ [runnerId]: {kind, direction?/side?, nonce, fromStatus?} }`
  с автооткатом в `idle` по таймауту (`kind`: move/attack/gotShot/fly/destroyed; `destroyed` — терминально,
  без отката). **Очередь по runnerId** (2026-08-31, второй заход): `trigger()` не перезаписывает текущую
  анимацию мгновенно, а кладёт шаг в очередь конкретного бегуна — играются по одному, каждый свою полную
  длительность, прежде чем начать следующий. Нужно для каскадов (отскок от столкновения → аномалия →
  отлёт из неё, несколько `runner_save` почти одновременно) — раньше все промежуточные шаги схлопывались
  в последний trigger (жалоба пользователя: бегун "телепортировался" в клетку аномалии, показывалась
  только финальная анимация). Единственное исключение — `pending`-слияние: `step_move` (транзиентный,
  даёт направление раньше позиции) метит свой шаг `pending: true`, и следующий `runner_save` с той же
  передвижкой ДОПОЛНЯЕТ его позицией на месте, а не заводит второй слот — иначе обычный некаскадный шаг
  играл бы вдвое дольше. Второй экспорт хука — `visualPositions[runnerId]`: пока у бегуна есть
  незавершённая очередь, `BoardGrid` рисует его в позиции ТЕКУЩЕГО шага очереди (проп
  `runnerVisualPositions`), а не в реальной (уже полностью применённой в game-стейте) — иначе
  промежуточные "остановки" каскада не отрисовались бы ни одним кадром, `RunnerTokenSlide` заскользил бы
  сразу к финальной точке. `reset()` — сбрасывает очереди/таймеры/визуальные позиции, вызывается вместе
  с ручным `resync()` (кнопка при зависшей коллизии) — REST-рефетch подменяет game-стейт целиком, минуя
  событийный поток, который двигает очередь, без сброса застрявшая очередь держала бы бегуна в позиции
  старого шага. `lib/runnerAnimTriggers.js` — чистые функции "Mercure-событие → что триггернуть":
  `handleVersionedRunnerAnimEvent` смотрит на versioned-события ДО применения `runnerGameReducer` (нужно
  старое состояние для сравнения), кладёт `toPosition` в каждый `trigger` про перемещение;
  `handleTransientRunnerAnimEvent` — на транзиентные (`step_move`/`step_shoot`/`anomaly`). **Важно про
  knockback/`fly`**: бэк НЕ шлёт отдельного сигнала "этого бегуна оттолкнуло туда-то" (событие `collision`
  несёт `direction`, но не id бегуна — см. находку в «Заметки», 2026-08-31) — используется эвристика: если
  новая позиция из `runner_save` НЕ является forward-соседом старой (`lib/hexDirection#forwardNeighbors`),
  это трактуется как отскок/телепорт → `fly`. Осознанно принятый компромисс, не 100%-надёжный сигнал.
- `components/game/RunnerTokenSlide.js` — токен на доске не телепортируется между клетками, а плавно
  скользит (`Animated.ValueXY` + `useLayoutEffect`, НЕ `useEffect` — иначе на Android на кадр видна
  целевая позиция ДО начала скольжения). React `key` в `BoardGrid` для этой обёртки — id БЕГУНА, не ключ
  клетки, иначе компонент пересоздавался бы на каждый шаг и терял анимационное состояние.
- Столкновение (2 бегуна на одной клетке) — `BoardGrid` рисует их РЯДОМ, лицом друг к другу
  (`collision_east`/`west`), а не "первый + значок +N" (это осталось только для 3+, что не должно
  происходить по правилам). Кто слева/справа — детерминированное правило: текущий активный игрок (тот,
  кто только что "наехал", `game.playerOrder`) — справа (`collision_west`, лицом влево), другой — слева
  (`collision_east`).
- `RunnerToken` — `imageScale` проп независимо масштабирует картинку ВНУТРИ кольца-обводки, не трогая
  само кольцо (`size`) — по прямому запросу пользователя после нескольких итераций (кольцо на доске
  должно остаться исходного размера, а сам спрайт — крупнее его, вылезая за край, это осознанно).

## Известные пробелы / TODO (актуально на момент записи)

- **Накат (select type=ROLL) не реализован.** Дополнительный ход уже двигавшимся бегуном (до 2 раз за
  раунд), доступен только если у игрока не осталось неперемещённых исправных бегунов. Сознательно
  отложено в Фазе 2 (отдельная, самостоятельная проверка готовности по всем раннерам игрока).
- **Атака Жнеца сразу при размещении не реализована.** `ability=reaper` поддерживает опциональный
  `direction` в том же вызове — фронт сейчас всегда шлёт `direction: undefined` (только размещение).
- **Переход бегуна за передний край видимого сегмента не реализован.** Если `positionX` бегуна
  переполняется за пределы 3 загруженных сегментов (см. `lib/hexDirection.js`), клетка для тапа
  физически не отрисована — `forwardNeighbors` такую клетку просто не включает в подсветку. Нужна
  отдельная UI-аффорданса (кнопка/стрелка «съехать с трассы»), не наступали на это ещё живьём.
- **Анимации/очередь transient-событий — Фаза 3, начата 2026-08-31.** `onTransient`
  теперь используется (см. новый абзац про анимации в «Ключевые архитектурные решения» выше) — move/
  attack/gotShot/fly/collision/destroyed для `RUNNER_TYPES.SPRINTER/ATHLETE/TANK` (скаут/солдат/атлет)
  реализованы и частично проверены живьём. Единственный оставшийся тип без набора — Жнец (`REAPER`),
  ассетов под него нет, по-прежнему статичная иконка. **Важно**: один вызов `/move` может сдвинуть бегуна больше чем на 1 клетку за раз (Anomaly/Stupor
  каскад, см. девятый заход в «Заметки») — анимационная система это уже учитывает (`runner_save`-эвристика
  триггерит `move`/`fly` на КАЖДЫЙ такой промежуточный хоп отдельно, не только на финальную позицию), и с
  2026-08-31 (второй заход) эти хопы играются ПО ОЧЕРЕДИ, а не схлопываются в последний — см.
  `useRunnerAnimations`/очередь по runnerId в «Ключевые архитектурные решения». НЕ проверено живьём (нет
  доступа к устройству/докер-бэку в той сессии) — статическая правка по чтению кода и разбору конкретного
  живого бага, который описал пользователь; нужен новый живой каскад-сценарий (столкновение → отскок в
  опасность → аномалия → отлёт), чтобы подтвердить, что все 4 шага теперь видны по отдельности.
- **Найдено 2026-08-31, НЕ ПОЧИНЕНО, ждёт решения пользователя** — `DirectionService::run()` на бэке
  (перепроверено прямым чтением PHP-файла, не по памяти) даёт LEFT_UP/RIGHT_UP чисто боковым шагом
  (тот же `positionX`/сегмент) только когда СТАРЫЙ `positionX` ЧЁТНЫЙ; на НЕЧЁТНОМ `positionX` те же
  LEFT_UP/RIGHT_UP ДОПОЛНИТЕЛЬНО продвигают на сегмент вперёд (диагональ). `lib/hexDirection.js` на
  фронте зеркалит это один-в-один (сверено с пользователем на конкретных живых координатах — совпало
  ровно как в PHP). Пользователь считает такое чередование неверным по правилам игры (ожидает, что
  влево/вправо ВСЕГДА чисто боковой шаг, без изменения сегмента) — если это подтвердится, чинить нужно
  `DirectionService.php` на бэке (у меня туда только чтение) и следом `hexDirection.js` в тон, иначе
  подсветка легальных клеток разойдётся с тем, что реально примет `/move`. Пользователь попросил пока
  НЕ трогать («понял, пока не трогай») — ждём его решения/сверки с текстом правил игры.
- **`Runner.damageTokens` — выдуманное поле.** У бэка `Runner::toArray()` отдаёт только `status`
  (healthy/damaged/broken/destroyed), **без** типа конкретного жетона повреждения. UI по ТЗ должен
  показывать тип — либо просить бэк добавить поле, либо копить типы на фронте из потока событий
  (`danger`/`damage`/`ricochet`/`rocket`/`stupor`/`anomaly`).
- **`RunnerPlayer::toArray()` не отдаёт `activeRunner`.** Это поле есть только в событиях (`player_step`,
  `step_selection`, `ability_*`) — `runnerGameReducer` его туда кладёт по мере поступления событий, но
  сразу после reconnect (до первого такого события) поле будет `undefined`. Осознанный пробел, не баг.
- На бэке подтверждённый (не мой) баг: `ReaperService::validateCell()` сравнивает `Cell::getType()`
  (`string`) с `RoadType::WALL`/`RoadType::ANOMALY` через `===` — это всегда `false` в PHP (enum-кейс не
  равен строке), т.е. сервер реально не запрещает ставить Жнеца на стену/аномалию. Фронт (Фаза 2) сам
  проверяет проходимость и занятость клетки при подсветке легальных мест для Жнеца
  (`GameBoardScreen` — `rawCellType`/`findRunnerAt` в вычислении `highlightedCells`), не полагаясь на бэк.
- **Найденный живьём баг бэка**: `POST /runner_game/ability` с `{"accept":false}` без ключа `details`
  падает `500 Internal Server Error` (нетипизированное свойство `AbilityDto::$details` остаётся
  неинициализированным, если ключ отсутствует в JSON — nullsafe `?->` не спасает от этой ошибки, это не
  то же самое, что `null`). Явный `{"accept":false,"details":null}` работает нормально (проверено
  curl'ом). `api/runnerGame.js` учитывает это — `ability(accept, details = null)`, НЕ `undefined`
  (`JSON.stringify` выкидывает `undefined`-поля из тела, так что дефолт обязателен именно `null`).
- `config/env.js`: `LAN_IP` — плейсхолдер, для теста на физическом устройстве надо подставлять реальный IP
  вручную (закомментированная строка в `devHost()`).
- Ассеты: изображения теперь разложены по подпапкам `assets/images/{road,dice,runner}` — если добавляешь
  новый набор ассетов, тоже заводи подпапку, а не кидай в корень `images/`.

## Заметки

<!-- Веди историю здесь: дата — что сделано / что узнал / на что наткнулся. Не удаляй старые записи,
     только дописывай, чтобы был виден таймлайн решений. -->

- Экран сессии (GameBoardScreen) переработан на раскладку слева-панель/справа-доска с мок-данными,
  drag-and-drop кубиков, тап-плейсментом бегунов. Реальное подключение к бэку и игровая логика (шаги
  select/move/collision/shoot/ability) — сознательно отложены по просьбе пользователя, см. TODO выше.

- 2026-08-12: прогон багфиксов по 7 замечаниям пользователя после первого теста UI.
  - `PlayerSwitcher` — уменьшены паддинги/шрифт/maxWidth пилюль, занимает минимум места.
  - `ParallaxBackground` — убран `zIndex: -1` у контейнера (Android мог вообще не рисовать вью с
    отрицательным zIndex, особенно рядом со сложным деревом GameBoardScreen) — фон пропадал только на
    мобильных.
  - `BoardGrid` — токену бегуна (`tokenLayer`) добавлены явные `zIndex/elevation`, best-effort фикс
    "бегун под сегментом" на Android (не удалось стопроцентно подтвердить причину статическим чтением
    кода — если баг всплывёт снова, нужен скриншот от пользователя).
  - `useBoardLayout.minOffset` — учтён доп. отступ `segmentW/2` на кирпичный сдвиг нечётных рядов
    (`marginLeft` в `BoardGrid`), иначе прокрутка (и веб, и мобильный free-drag) упиралась в границу
    раньше, чем крайняя правая колонка нечётных дорожек долистывалась до края экрана.
    `useBoardScroll.snapToBlock` — для последнего блока target теперь берётся из `minOffsetRef.current`
    напрямую (не кратное `blockWidth`), иначе стрелочная навигация на мобильных всё ещё не долистывала
    до конца даже после фикса `minOffset`.
  - Усиления (`boost/heal/reaper/ghost`) сделаны взаимоисключающими: `GameBoardScreen.handleAssignDice`
    при новом назначении сбрасывает остальные три зоны игрока (раньше можно было держать активными
    несколько сразу — по правилам одновременно доступно только одно).
  - Добавлено назначение кубика хода на бегуна (движение) + накат: новый компонент
    `RunnerDiceSlot.js`, встроен в `RunnerCard.js` (зона `move:<runnerId>`). Хит-тестинг дропа общий с
    зонами усилений в `PlayerInfoPanel` (`zoneLayoutsRef`/`findZoneAt`, ключи `move:` не имеют
    ограничений по номиналу кубика). Состояние — новый `moveAssignments` в `GameBoardScreen`
    (`{ [playerId]: { [runnerId]: diceIndex[] } }`); если на бегуна с уже назначенным кубиком бросить
    ещё один — оба хранятся в массиве и рисуются как накат (бейдж "накат" при length > 1).
  - Жнец: выбор жнеца для тап-плейсмента (`PlayerInfoPanel`, реаперный `TouchableOpacity`) теперь
    заблокирован (`disabled`, приглушённый текст-подсказка), пока на усилении `reaper` не лежит кубик
    (`diceAssignments[playerId].reaper != null`). Само размещение на доску по-прежнему без ограничений
    по типу клетки (как и раньше у всех бегунов) — ограничение только на *доступность* действия.
  - Верификация: babel-parser синтаксис-чек + скрипт кросс-референса импортов/require по всем
    затронутым файлам — оба прогона чистые.

- 2026-08-12 (второй заход): ещё 2 замечания после теста веб-версии.
  - `PlayerSwitcher` переделан из горизонтального ряда пилюль в узкую вертикальную колонку
    (`ScrollView` вертикальный, `flex: 1`) — на вебе широкая панель растягивала горизонтальный ряд
    из 4 именных кнопок почти на пол-экрана. `PlayerInfoPanel` теперь оборачивает переключатель и
    инфо-колонку в `body` (`flexDirection: 'row'`): `PlayerSwitcher` `flex:1`, `infoColumn` (ScrollView
    с содержимым игрока) `flex:4` — даёт ровно пропорцию 1/5 к 4/5 при любой ширине панели. Кнопка
    "Завершить ход" осталась во всю ширину панели под этим рядом, а не внутри одной из колонок.
  - Ручное перемещение бегуна по доске (тап-плейсмент) ограничено соседними клетками:
    `GameBoardScreen.handleCellPress` для уже размещённого бегуна (`segment != null`) разрешает только
    col+1 на той же дорожке (шаг вперёд) или ту же колонку с row±1 (смена дорожки на одну). Диагонали,
    шаги назад и прыжки через клетку — тап просто игнорируется, бегун остаётся выбранным для повторной
    попытки. Верхняя/нижняя дорожка не требует отдельной проверки границ — за пределами `0..rows-1`
    тапабельных клеток на доске физически нет. Первое размещение из резерва (`segment == null`) — все
    ещё без ограничений. Жнец **исключён** из этого правила (`runner.type !== RUNNER_TYPES.REAPER`) —
    по более раннему требованию его можно ставить куда угодно, лишь бы усиление "Жнец" было активно.
  - Верификация: babel-parser синтаксис-чек по всем трём затронутым файлам — чисто.

- 2026-08-12 (третий заход): откат неверной трактовки предыдущего фикса переключателя.
  - Первая версия фикса "переключатель занимает много места" (второй заход выше) сделала переключатель
    боковой колонкой (1/5 ширины) рядом с информацией об игроке — пользователь уточнил, что это не то:
    переключатель должен остаться СВЕРХУ (вертикальный стек секций панели, как было изначально), просто
    ограничен по высоте, а не вынесен вбок. `PlayerSwitcher` возвращён к горизонтальному ряду пилюль
    (`ScrollView horizontal`, стили из первого фикса — компактные пилюли). `PlayerInfoPanel.body` —
    обратно `flexDirection: 'column'`: `switcherBox` (`flex:1`) сверху, `infoColumn` (`flex:4`) под ним —
    даёт переключателю ~1/5 высоты панели (не ширины), а не полосу на пол-экрана.
  - Верификация: babel-parser синтаксис-чек — чисто.

- 2026-08-12 (четвёртый заход): переключатель после третьего фикса центрировался, но зона под него всё
  равно оставалась ~половину экрана на вебе — flex:1/flex:4 между switcherBox и infoColumn не давал
  ожидаемую пропорцию 1/5 к 4/5. Не смог стопроцентно диагностировать статическим чтением, почему именно
  (гипотеза: цепочка height от корня навигатора до панели на вебе не даёт body реальную высоту для
  распределения — но не подтверждено рендером/скриншотом, инструментов для визуальной проверки веб-сборки
  в сессии не было). Вместо продолжения гадания на flex-пропорциях — обходной манёвр, надёжный
  независимо от причины: `useBoardLayout` считает `switcherH = Math.floor(screenH * 0.15)` (тот же приём,
  что уже используется для `arrowBtnSize`/`roadContainerH` — пиксели из `useWindowDimensions`, а не flex-
  доли), прокидывается в `PlayerInfoPanel` как `switcherHeight` и ставится ЖЁСТКИМ `height` на
  `switcherBox` (не `flex`). `infoColumn` — просто `flex: 1`, забирает весь остаток. Фиксированный пиксель
  не зависит от того, работает ли flex-цепочка выше по дереву. Если после этого зона переключателя всё
  ещё будет непропорционально большой — нужен скриншот от пользователя, дальше вслепую чинить
  контрпродуктивно.
  - Верификация: babel-parser синтаксис-чек по `useBoardLayout.js`/`GameBoardScreen.js`/
    `PlayerInfoPanel.js` — чисто.

- 2026-08-12 (пятый заход): 2 замечания после теста на Android-эмуляторе/устройстве.
  - **Белый фон на GameBoardScreen (Android).** У `GameBoardScreen.wrapper` не было своего
    `backgroundColor` — в отличие от `Screen.js`, где `SafeAreaView` всегда подставляет `colors.bg`
    ПОД `ParallaxBackground` на всех остальных экранах. Если Animated-картинка фона по любой причине не
    успевает/не может отрисоваться на Android (тяжёлый экран, много одновременных Image, плюс
    `useLockLandscape` триггерит настоящий поворот экрана после первого рендера —начальный кадр
    считается ещё в portrait-размерах), под ней в GameBoardScreen ничего не было — просвечивал белый фон
    Android Activity по умолчанию. Добавлен `backgroundColor: colors.bg` на `wrapper` как фолбэк
    (`GameBoardScreen.js`), тем же приёмом, что уже используется в `Screen.js`.
  - Дополнительно (не точно установленная, но правдоподобная и точно небесполезная причина): исходный
    фон `auth_background_landscape.png` был 2933×1650, 10.7МБ — в разложенном виде это ~19МБ битмапа,
    ощутимая нагрузка на память специально на самом тяжёлом экране приложения. Пересжат в
    `auth_background_landscape.jpg` (1920×1080, ~900КБ — в ~11 раз меньше файл, decode-память ~в 3 раза
    меньше), альфа-канал не требовался (фон непрозрачный). `ParallaxBackground.js` — require обновлён на
    `.jpg`, старый `.png` удалён.
  - **Токен бегуна всё ещё под сегментом (Android), точечный фикс zIndex/elevation из третьего захода не
    помог.** Переделано архитектурно: раньше токен рисовался оверлеем ВНУТРИ каждой ячейки-
    `TouchableOpacity`, поверх её `Image` (подозрение — Android view-flattening/компоновка
    `Image`+`TouchableOpacity`, не подтверждено статическим чтением). Теперь `BoardGrid.js` рисует все
    токены ОДНИМ отдельным абсолютным слоем НАД всей сеткой целиком (`tokenOverlayLayer`,
    `pointerEvents="none"`, тот же `Animated.View`, что и ряды — едет вместе с прокруткой), с
    пиксельными координатами, посчитанными вручную из ключа `indexRunnersByCell`
    (`segment-row-localCol` → `globalCol = segment*cols + localCol` → `x = globalCol*segmentW +
    (row%2 ? segmentW/2 : 0)`, `y = row*segmentH` — та же геометрия, что и у самих ячеек, включая
    кирпичный сдвиг нечётных рядов). Ячейки теперь рисуют только `Image`, без вложенного токена. Компонент
    получил новый проп `cols` (раньше был только `rows`) — передаётся из `GameBoardScreen`.
  - Честно: ни то, ни другое не проверено визуально (нет доступа к Android/браузеру из сессии) — оба
    фикса обоснованы статическим анализом и структурно надёжнее прежних точечных патчей, но не
    стопроцентно подтверждены. Если не поможет — нужен скриншот.
  - Верификация: babel-parser синтаксис-чек по `GameBoardScreen.js`/`BoardGrid.js`/
    `ParallaxBackground.js` — чисто.

- 2026-08-12 (шестой заход): после пятого захода на мобильном фон стал серым (не белым — фолбэк-цвет
  сработал), но саму parallax-картинку по-прежнему не видно, а токены бегунов вообще пропали с экрана.
  - **Найдена настоящая причина фона: в проекте не было вообще НИКАКОГО babel-конфига** — ни
    `babel.config.js`, ни `.babelrc`, ни поля `"babel"` в `package.json` (проверено `find` по всему
    репо). Без него Metro не подключает `babel-preset-expo`, а именно `babel-preset-expo` при наличии
    `react-native-reanimated`/`react-native-worklets` в зависимостях сам добавляет плагин
    `react-native-worklets/plugin` (см. `node_modules/babel-preset-expo/build/configs/expo.js:107-117`).
    Без этого плагина ворклеты (`useSharedValue`/`useAnimatedStyle`/`withTiming`/`runOnJS`/директива
    `'worklet'` в `ParallaxBackground.js`) не компилируются как надо. Создан `babel.config.js` с
    `presets: ['babel-preset-expo']` — эмпирически проверено через `@babel/core` напрямую (прогнал
    `ParallaxBackground.js` через babel с этим конфигом и `caller: {platform:'android'}` — плагин
    ворклетов подключился и отработал, без конфига — нет). **Нужен рестарт Metro с очисткой кэша**
    (`expo start -c`), иначе возьмётся старый закэшированный бандл без нового babel-конфига.
  - **Токены бегунов пропали совсем** — регрессия от пятого захода (переход на единый оверлей-слой).
    `tokenOverlayLayer` использовал `StyleSheet.absoluteFillObject` (отступы top/left/right/bottom:0)
    внутри `Animated.View`, у которого своего явного размера нет — высота/ширина выводятся из контента
    (строк). Стретч через нулевые отступы рассчитывает на то, что размер родителя уже посчитан к этому
    проходу компоновки — на части рендеров это давало оверлею нулевой размер (токены пропадали целиком,
    а не просто смещались). Заменил на явные пиксельные `width`/`height`
    (`BOARD_LAYOUT.TOTAL_COLS * segmentW` × `rows * segmentH`) — тот же приём, что уже применялся для
    `switcherHeight`: числовой размер не зависит от нюансов auto-layout родителя.
  - Верификация: реальный прогон через `@babel/core` (не просто babel-parser) с `babel-preset-expo` по
    `DiceDie.js`/`ParallaxBackground.js`/`GameBoardScreen.js`/`BoardGrid.js`/`App.js` — все проходят,
    плагин ворклетов подтверждённо подключается. Фикс токенов проверен тем же прогоном на синтаксис —
    чисто (при первой правке была опечатка — незакрытый JSX-тег, — поймана этой же проверкой и
    исправлена сразу).

- 2026-08-12 (седьмой заход): Фаза 1 подключения GameBoardScreen к реальному бэку (bootstrap — переход
  лобби→игра, пассивная синхронизация live-стейта, reconnect). Полный план — в чате/плане сессии, суть:
  - Матчасть перепроверена ЧТЕНИЕМ ИСХОДНИКОВ бэка (не только README, там кое-где неполно/неточно):
    `POST /runner_game/move` — один вызов на одну клетку (`StepMoveService.php`), сервер не строит
    маршрут сам; коллизия сигналится `game.extraTurnPlayer`, не `player.step` (в `PlayerStep` enum нет
    кейса COLLISION); `GameActiveEvent` шлёт `status` через `->name` (`"ACTIVE"`), а не `->value` как
    everywhere-else — редьюсер это обходит константой, не доверяя `e.status`. Полный список
    Mercure-событий и их полей — см. `runnerGameReducer.js`, он покрывает все версионные события.
  - Новое: `api/runnerGame.js` (get/start/select/move/collision/shoot/ability — последние 5 пока не
    вызываются с экрана, это Фаза 2), `normalizeRunnerGame` в `api/normalize.js`, `GAME_STATUS`/
    `PLAYER_STATUS`/`PLAYER_STEP` в `GameConstants.js`, `store/runnerGameReducer.js` (абсолютное
    слияние по версионным событиям, без очереди/анимации — это Фаза 3), `components/game/GameWaitingRoom.js`
    (ready-up экран между `game_created` и `game_active`).
  - `GameBoardScreen` теперь принимает `route.params.gameId`, подписывается через `useMercure` (тот же
    протокол, что `LobbyScreen`), рендерит `GameWaitingRoom` / доску / спиннер по `game.status`. Мок
    (`MOCK_GAME`) больше не используется на этом экране. `handleCellPress` пока не двигает раннера по
    live-данным (это создало бы конфликт с реальными позициями с бэка) — только `notify()`-заглушка,
    сохранён предыдущий adjacency-код закомментирован не был, просто убран (в Фазе 2 его логика
    переедет в реальный вызов `/move`).
  - `MainMenuScreen`: на фокусе экрана сначала проверяется `runnerGameApi.get()` (активная партия
    приоритетнее лобби) — если есть `waiting`/`active` игра, сразу `navigation.reset` на `RUNNER_GAME`
    с `gameId`. Первый резолв блокирует рендер меню (`checkingSession`, спиннер как на
    `RootNavigator`-сплэше), повторные фокусы — нет (как и у лобби-баннера). Кнопка «🧪 Тест игровой
    доски» удалена (вела в никуда без `gameId`/живых данных).
  - Зависшая коллизия (бэк не резолвит сама, таймаута нет) — на фронте просто баннер + через 18 сек
    кнопка ручного `resync()` (полный REST-рефетч + переподписка), без авто-решения за игрока.
  - Фон на мобильном (не только на GameBoardScreen — вообще везде, регрессия относительно шестого
    захода, где хотя бы auth-экран работал): код уже правильный (`babel.config.js` с
    `babel-preset-expo` на месте, `.jpg`-ассет существует, единственная ссылка на него актуальна) —
    диагностировано, но НЕ ПОЧИНЕНО кодом, потому что чинить нечего: `babel.config.js` и переименование
    `.png`→`.jpg` попали в прошлый коммит (`821a9fc`) БЕЗ последующего `expo start -c`/пересборки, и
    Metro/embedded-бандл на устройстве почти наверняка держит старый закэшированный бандл, где ещё нет
    нового babel-конфига и/или ссылка на удалённый `.png`. Нужно на стороне пользователя: `expo start -c`
    (полная очистка кэша Metro), и если это НЕ Expo Go, а dev-client/standalone-сборка — потребуется
    ещё и `expo run:android` (или аналог), т.к. embedded JS-бандл в самой сборке тоже мог закэшироваться.
  - Верификация: `@babel/core` с `babel-preset-expo` (`caller: {platform:'android'}`) по всем новым и
    изменённым файлам — чисто; отдельно перепроверены имена экспортов/путей импортов grep'ом (`useAuth`,
    `runnerGameReducer`, `normalizeRunnerGame`, `runnerGameApi`, `GAME_STATUS`/`PLAYER_STATUS`/
    `PLAYER_STEP`, дефолтный экспорт `GameWaitingRoom`) — все совпадают. Живого прогона на бэке/
    устройстве не было (нет доступа из сессии) — нужен ручной тест по сценарию из плана (лобби → оба
    готовы → `GameWaitingRoom` → оба жмут «Готов» → доска с реальными позициями).

- 2026-08-12 (восьмой заход): баг «игрок 2 в поиске лобби не видит лобби игрока 1» (веб + Android-эмулятор,
  разные аккаунты).
  - Бэк локально поднят в Docker — прогнал живой прогон напрямую через `curl` двумя чистыми тестовыми
    аккаунтами (регистрация → логин → A создаёт лобби → B зовёт `GET /api/lobbies`): бэк корректно вернул
    лобби A вместе со старыми тестовыми. `LobbyRepository::findAvailableLobbies()`/`Lobby::toArray()` —
    без бага. Отдельно подтверждено: если ИЩУЩИЙ сам уже состоит в лобби — `/lobbies` отвечает 409
    `"User already joined"` (ожидаемо по `LobbyService::find()`), это не тот случай (аккаунты разные).
  - Реальная причина — на фронте: `LobbySearchScreen.load()` не отслеживал актуальность ответа (в
    отличие от `MainMenuScreen`/`LobbyScreen`/`useMercure`, где такой guard уже есть). `useFocusEffect`
    дёргает `load()` на каждый фокус экрана, плюс его можно нажать вручную (Retry/pull-to-refresh) поверх
    уже летящего запроса — при пересечении двух вызовов побеждал тот, что резолвился ПОСЛЕДНИМ, а не тот,
    что стартовал последним. Пользователь так и увидел: «не удалось загрузить» → тап «Повторить» →
    «пока пусто» → снова «не удалось загрузить» — без единого изменения на бэке между попытками (сам
    проверил `curl`'ом в этот момент — лобби всё ещё `waiting`). Похоже на нестабильность сети
    Android-эмулятора, из-за которой одни запросы шли быстро, другие — медленно/с ошибкой, и итоговое
    состояние экрана определял просто порядок их завершения.
  - Фикс: `requestIdRef` (монотонный счётчик) в `LobbySearchScreen.js` — `load()` применяет
    `setLobbies`/`setError` только если её `requestId` всё ещё совпадает с последним запущенным. Устаревший
    ответ (успех или ошибка) молча игнорируется. Тот же паттерн, что `genRef` в `useMercure.js`.
  - Верификация: `@babel/core` с `babel-preset-expo` — чисто. Живой прогон (2 чистых тестовых аккаунта,
    `curl` напрямую к локальному Docker-бэку) подтвердил, что сам бэк отдаёт данные верно — фикс на фронте
    не проверялся через реальный Android-эмулятор из сессии (нет доступа), логика гонки запросов проверена
    только чтением кода.

- 2026-08-12 (девятый заход): Фаза 2 — реальная стейт-машина хода (select/ability/move/shoot/collision).
  ПРИМЕЧАНИЕ: PR с Фазой 1 был смёржен пользователем в `main` ДО того, как фикс гонки запросов (восьмой
  заход) успел попасть в тот же PR — пришлось завести новую ветку `runner-game-phase2-turn-logic` от
  свежего `main` и cherry-pick'нуть фикс лобби на неё, прежде чем начинать Фазу 2 (иначе бы потерялся).
  - Матчасть перепроверена чтением `SelectionDto`/`AbilityDto`+все `Details*DTO`/`ShootDto`/`CollisionDto`,
    `StepSelectionService`/`StepAbilityService`/`StepShootService`/`StepCollisionService`,
    `StepSelectionValidator`/`StepAbilityValidator`/`StepShootValidator`, `PlayerStepService` (порядок
    переходов SELECT→ABILITY→MOVE→SHOOT), `DirectionService` (точная гекс-формула соседей). Ключевое:
    `dice` во ВСЕХ вызовах (select/ability-details) — 1-based индекс кубика игрока (1..4), не значение;
    `/ability` — один вызов сразу со всеми деталями (heal нужен `runnerId`, reaper —
    `positionX/positionY/segment`+опц. `direction`), не два шага.
  - Архитектурное решение: **убрали локальный UI-стейт `diceAssignments`/`moveAssignments` полностью**.
    Раз SELECT/ABILITY реально пишут `runner.dice`/`player.ability`/`player.diceN` на бэке и это приходит
    обратно live-событиями (готово с Фазы 1), UI просто читает эти поля напрямую — дублирующего стейта,
    который мог бы разойтись с сервером, больше нет. Единственный оставшийся локальный стейт —
    `pendingAbility` (heal/reaper ждут второй тап: карточка бегуна / клетка доски).
  - Новое: `lib/hexDirection.js` (`forwardNeighbors`/`neighborPosition`/`cellKey` — зеркало
    `DirectionService::run()`+`canShoot()`, используется и для подсветки MOVE, и SHOOT — бэк проверяет
    ровно тех же 3 соседей для обоих). `BoardGrid` получил проп `highlightedCells` (Set ключей ячеек,
    зелёная рамка поверх тайла). `PlayerInfoPanel`/`RunnerCard`/`RunnerDiceSlot`/`DiceTray`/`DiceDie`
    переведены на чтение live-данных вместо локальных assignments; `DiceDie` получил `draggable` (гейтинг
    жеста по тому, чей сейчас ход/шаг — `dragMode` в `PlayerInfoPanel`: `select` только на карточку
    бегуна, `ability` только на зону усиления).
  - **Живой прогон через локальный Docker-бэк (curl, 2 свежих тестовых аккаунта) — не просто чтение кода**:
    полный цикл лобби→ready-up→SELECT→ABILITY(decline)→MOVE(несколько вызовов)→смена хода отработал 1-в-1
    как спроектировано. По пути нашёл и сразу исправил РЕАЛЬНЫЙ баг в собственной реализации ДО того, как
    он попал бы к пользователю: `POST /runner_game/ability {"accept":false}` (без ключа `details`) валит
    бэк `500` — у меня в `GameBoardScreen` изначально не было кнопки "пропустить усиление" вообще (шаг
    ABILITY без активации застрял бы намертво), и `runnerGameApi.ability(accept)` при вызове без второго
    аргумента как раз собрал бы это самое ломающее тело запроса. Добавил кнопку «Пропустить усиление»
    (аналог уже спроектированной «Пропустить выстрел») и `ability(accept, details = null)` — оба фикса
    внесены ДО того, как эта запись писалась, т.е. это уже финальное состояние кода, не TODO.
  - **Побочная находка о механике движения**: один `/move`-вызов может сдвинуть бегуна больше чем на одну
    клетку — если клетка с закрытым жетоном опасности при вскрытии оказывается Anomaly (или другим
    эффектом, запускающим повторный `Action::TYPE_MOVE` на бэке), сервер сам продолжает движение и списывает
    доп. очки в том же запросе (наблюдал живьём: один вызов `direction:UP` сдвинул бегуна на 2 клетки и
    списал 2 очка вместо 1). Фронт это НЕ ломает — `handleCellPress` не пишет позицию локально, всё берётся
    из live-стейта, — но задокументировал как важный нюанс для будущей Фазы 3 (анимация должна уметь
    проигрывать несколько `runner_save` на один тап пользователя).
  - Не проверено живьём (не успел/не было практической возможности за один прогон): SHOOT (raund=0 в
    тестовой партии стрелять запрещает по правилам — не дошёл до раунда 1), collision (нужна намеренная
    подстава двух бегунов на одну клетку), ability accept (boost/heal/reaper) — только чтение кода +
    структурная проверка DTO.
  - Верификация: `@babel/core` с `babel-preset-expo` (android и web caller) по всем новым/изменённым
    файлам — чисто. Живой прогон через реальный бэк — см. выше, покрывает SELECT/ABILITY(decline)/MOVE/
    смену хода. UI на реальном устройстве/эмуляторе не проверялся (нет доступа из сессии).

- 2026-08-12 (десятый заход): первый живой прогон пользователя двумя игроками через реальные
  устройства/браузер — вскрыл UX-пробел, не баг механики. Симптомы: «не ясно чей ход», «нет кнопки
  конца хода», «не размещаются персонажи по очереди». Разбор: у активного игрока кубик перетаскивался
  на карточку бегуна нормально (SELECT реально проходил), но дальше шаг переключался на ABILITY, а
  кнопка «Пропустить усиление» была мелкой плавающей кнопкой внизу экрана без единой подсказки на
  экране, что вообще происходит — игрок застревал там, не понимая, что нужно либо активировать
  усиление, либо явно его пропустить, чтобы открылся тап по доске для MOVE. У второго игрока кубики
  корректно не перетаскивались (не его ход — гейтинг работал правильно), но это тоже никак не
  объяснялось. Отдельно пользователь ожидал увидеть кубики дороги/столкновения/направления/атаки как
  отдельные UI-элементы — в этой цифровой реализации бэк их бросает сам и отдаёт готовый результат
  событием, отдельных интерактивных "физических" кубиков для игрока для них нет по дизайну бэка (кубик
  дороги вообще применяется автоматически на шаге SELECT, см. `StepSelectionService::handleDice`) — не
  баг, но стоит когда-нибудь показать сам факт броска в Фазе 3 (анимация/реплики).
  - Фикс: `GameBoardScreen` — новый `turnBanner` (верх-лево, `stepInstruction()`): всегда показывает,
    чей сейчас ход, и если твой — понятную инструкцию под текущий `player.step`
    (SELECT/ABILITY/MOVE/SHOOT, включая отдельные тексты для pending heal/reaper). Кнопки «Пропустить
    усиление»/«Пропустить выстрел» переехали ВНУТРЬ этого банера (были отдельными плавающими кнопками
    внизу экрана, вне поля зрения) — теперь подсказка и действие в одном месте. `collisionBanner`
    заодно передвинут на верх-право (у обоих банеров раньше было `alignSelf:'center'`, что для
    `position:'absolute'`-элементов в RN не центрирует надёжно — заменил на явные `left`/`right`).
  - Не проверено живьём в этом заходе (нет доступа к устройству из сессии) — статическая проверка:
    перечитал `lib/board.js#flattenTrackSegments` и `lib/hexDirection.js#cellKey` построчно ещё раз,
    сверяя формат ключа ячейки — совпадают (`${segment}-${positionY}-${positionX}` в обе стороны),
    так что сама логика тап-детекции для MOVE/SHOOT не должна быть причиной ("персонажи не
    размещаются" объяснено полностью найденным UX-пробелом выше, отдельного бага в hit-testing не
    нашёл при повторном чтении).
  - Верификация: `@babel/core` с `babel-preset-expo` — чисто.

- 2026-08-12 (одиннадцатый заход): по просьбе пользователя — сворачиваемый лог событий на
  `GameBoardScreen`, чтобы видеть ход партии по мере тестирования (не только конечный live-стейт).
  - `lib/eventLog.js` — чистая `describeEvent(e)`: человеко-читаемая строка под каждый из известных
    типов Mercure-событий партии (`game_*`/`player_*`/`step_*`/`ability_*`/`runner_*`/orchestrator
    danger-collision-damage-ricochet-rocket-stupor-anomaly), `rawEventFallback(e)` — на случай
    неизвестного типа события (сырой JSON без служебных `gameId`/`version`).
  - `components/game/EventLogPanel.js` — свёрнутая по умолчанию плашка правый-низ (счётчик + ▸/▾),
    разворачивается в скроллящийся список с автоскроллом к последней записи.
  - `GameBoardScreen` — `eventLog` (капнут на 200 записей) заполняется ОБОИМИ путями: `reduceAndLog`
    оборачивает `runnerGameReducer` для версионных событий (лог пишется на каждое реально применённое
    событие, дублей после уже отфильтрованных `useMercure` нет), и `onTransient` теперь пишет в лог
    вместо того чтобы просто отбрасывать transient-события (`step_*`/orchestrator-эффекты) — раньше
    (Фаза 1/2) они были no-op. Панель показана и на `GameWaitingRoom`, и на активной доске — видно даже
    `game_active`, до перехода на саму игру.
  - Верификация: `@babel/core` с `babel-preset-expo` — чисто, экспорты/импорты перепроверены grep'ом.
    Живого прогона не было (не проверял пользователь на этом заходе).

- 2026-08-12 (двенадцатый заход): пользователь поймал баг — UI позволил бросить кубик наката на
  бегуна в начале хода (когда это правилами запрещено), и после броска кубик было не забрать назад.
  Разбор + фикс:
  - **Накат в Фазе 2 не был реализован вообще** (`handleDropOnRunner` всегда слал `type:'DICE'`) — то,
    что увидел пользователь, скорее всего был кубик, зависший в незавершённом состоянии из-за
    отсутствия отмены (см. ниже), а не настоящий `type:'ROLL'`. Сама механика наката реализована в
    этом заходе с нуля.
  - Живой прогон через локальный бэк (`curl`, тот же тестовый аккаунт, что и в Фазе 2) — специально
    прогнал несколько полных ходов подряд (включая обработку реальной коллизии, случайно возникшей по
    пути — принял её через `/collision`), чтобы поймать момент, когда накат реально разрешён.
    Подтверждено: `StepSelectionValidator::rollValidate` — `type:ROLL` для бегуна с неисправленным
    (не до конца проехавшим, `dice !== 0`) статусом или пока есть СВОЙ неперемещённый исправный бегун
    (`dice == null`) — бэк отвечает чистой ошибкой `"You have runners that haven't been moved this
    round"`. В обычной партии (3 движимых бегуна, по одному ходу на каждого) накат физически почти не
    возникает — раунд завершается и кубики перебрасываются, как только все раннеры сходили, реального
    "лишнего" хода на накат не остаётся. Он актуален в основном при потере бегуна (уничтожен) до того,
    как раунд исчерпал шаги очереди — тогда СВОИХ движимых раннеров меньше, чем оставшихся кубиков.
  - **Реальный фикс (главная причина жалобы)**: `GameBoardScreen` — SELECT (и обычный DICE, и накат
    ROLL) теперь требует явного подтверждения, а не коммитится в бэк сразу на дроп. Новый стейт
    `pendingSelect: {runnerId, diceIndex, type}` — заполняется дропом (только если
    `canSelectRunner(runnerId)` разрешает — новая функция, зеркалящая `rollValidate` на клиенте),
    показывается на карточке бегуна как pending (пунктирная жёлтая рамка, "выбран — тапни ещё раз для
    отмены"), в баннере хода — кнопки «Подтвердить»/«Отмена». Тот же паттерн, что уже был у heal/reaper
    (`pendingAbility`) — просто раньше SELECT был единственным исключением, которое коммитилось
    мгновенно. `PlayerInfoPanel` — hit-testing зон `move:<runnerId>` теперь зовёт `canSelectRunner`
    вместо голого `dragMode==='select'`, так что подсветка при наведении кубика сама показывает,
    доступен ли дроп именно на ЭТУ карточку (важно для наката — не на любую уже проехавшую).
  - Сознательно НЕ стал делать pending+confirm для boost/ghost (они как коммитились мгновенно на дроп,
    так и коммитятся) — жалоба пользователя была именно про SELECT/накат, heal/reaper уже были
    отменяемые с прошлого захода. Если понадобится то же самое для boost/ghost — отдельная небольшая
    правка по тому же паттерну.
  - Верификация: `@babel/core` с `babel-preset-expo` (android и web caller) по всем изменённым файлам —
    чисто. Живой прогон через реальный бэк подтвердил точные условия наката и заодно проверил
    обработку реальной (не подстроенной руками) коллизии. UI-часть (pendingSelect/canSelectRunner)
    живьём не проверялась — нет доступа к устройству из сессии.

- 2026-08-13 (тринадцатый заход): первый прогон двумя реальными устройствами (Android + web) —
  большой список проблем, разобрал по одной, не всё успел проверить/починить в этом заходе (лимит
  токенов сессии), список ниже — задел на следующую сессию.
  - **Починено**: `runnerGameReducer.js` не обрабатывал `step_selection` (единственное событие,
    возвращающее назначенный `runner.dice` сразу после /select — ⚠ поля игрока там БЕЗ подчёркивания,
    `dice1` не `dice_1`, в отличие от остальных `player_*`-событий, сверено построчно с
    `StepSelectionEvent.php`) и `step_begin` — из-за первого подтверждённый выбор бегуна визуально
    "откатывался" (бэк реально принял SELECT, но фронт не отражал `runner.dice`); второй просто не был
    замаплен в `lib/eventLog.js` (показывался сырым JSON). Оба случая добавлены.
  - **НЕ починено, задел на следующую сессию** (только гипотезы, без живой проверки):
    - Нет подсветки клетки для первого выхода на трассу (MOVE-шаг, `activeRunner.segment == null`) —
      подтверждено, что `myStep` определялся верно (текст подсказки появлялся), но клетки не
      подсвечивались и тапы не срабатывали нигде. Правка `step_selection` выше НЕ должна была на это
      влиять (та подсветка зависит от `activeRunner`/`myStep`, не от `runner.dice`) — причина ещё не
      найдена, нужен живой лог событий/скриншот с этого места партии.
    - Android не видел присоединение игрока №2 (веб) к лобби — список игроков не обновлялся;
      `/runner_game/start` на Android вернул 404. Мог быть связан с тем, что Android вообще не получал
      Mercure-события для лобби (см. следующий пункт) — если список игроков не обновился, `/start` мог
      физически вызываться до появления партии.
    - На вебе `GET /.well-known/mercure?topic=lobby_5` показывался в devtools как "Заблокирован" —
      похоже на настоящую проблему с Mercure-хабом/CORS/сетью в конкретной связке
      Android-эмулятор+веб-браузер одновременно, а не баг конкретно во фронтенд-коде; не
      диагностировано глубже за этот заход.
    - Турн-баннер на Android показывал «Ход игрока: —» (без имени) в ситуации, где по остальным
      признакам ход шёл нормально — не разобрано, `currentTurnPlayer` numeric типы совпадают,
      но не проверено на реальных данных с этого конкретного прогона.
    - Пользователь отдельно уточнил: два подряд "ready-up" (в лобби + `GameWaitingRoom`) выглядят
      избыточно — это осознанное разделение по дизайну бэка (`LobbyController::ready` и
      `RunnerGameController::start` — независимые сущности, лобби и партия), не баг фронта; стоит
      решить, упрощать ли UX (например, автоматически звать `/runner_game/start`, как только оба
      игрока готовы в лобби) в следующей сессии, если это осознанно мешает.
  - Верификация: только `@babel/core` синтаксис-чек по `runnerGameReducer.js`/`eventLog.js` — чисто.
    Живого прогона фикса НЕ было (пользователь тестирует между заходами сам).

- 2026-08-13 (четырнадцатый заход): найдена и почищена настоящая причина пункта "нет подсветки клетки
  для первого выхода на трассу" из тринадцатого захода + два запроса пользователя (закрепить кубики,
  портретная раскладка на Android).
  - **Настоящая причина отсутствия подсветки MOVE**: `RunnerPlayer::$activeRunner` на бэке типизирован
    как `?string` (`Entity/RunnerPlayer.php:33`), а `Runner::$id` — как `?int` (`Entity/Runner.php:19`).
    В JSON это `"25"` против `25` — `GameBoardScreen.activeRunner` искал бегуна строгим `runners.find(r
    => r.id === myPlayer.activeRunner)`, никогда не совпадало, `activeRunner` был всегда `undefined` для
    ЛЮБОГО шага MOVE/SHOOT (не только для ещё не выставленных бегунов — просто это самый первый ход в
    любой партии, поэтому и попался первым). Текст подсказки (`stepInstruction`) при этом не зависит от
    `activeRunner` (только от `step`), поэтому казался рабочим, маскируя баг. Фикс — `String(...)` с
    обеих сторон сравнения, тот же приём, что уже использовался для `playerOrder`/`extraTurnPlayer`.
    Задели тем же паттерном ещё 2 места с той же болезнью: `PlayerInfoPanel` — подсветка активной
    карточки бегуна/Жнеца (`runner.id === activePlayer.activeRunnerId`), тоже была битой, просто менее
    заметно (не блокирует действия, только визуал). НЕ проверено живьём (сессия без доступа к
    браузеру/устройству) — пользователь должен перепроверить после хард-рефреша страницы.
  - **Кубики хода закреплены**: `PlayerInfoPanel` — раньше весь контент (переключатель кроме, кубики,
    усиления, карточки бегунов) был одним `ScrollView`; на низких Android-экранах, чтобы дотянуться до
    зоны "кубик хода" нижней карточки, приходилось скроллить — и вместе со списком уезжал сам кубик,
    драг из которого невозможно было продолжить. Кубики (+заголовок+имя игрока) вынесены из `ScrollView`
    и закреплены сразу под переключателем; скроллится теперь только усиления+карточки бегунов+Жнец.
    Хит-тестинг драга не пострадал — он и так был на оконных координатах (`measureInWindow`), не зависел
    от того, в каком контейнере лежит кубик.
  - **Портретная раскладка (адаптивная, не только Android)** — см. новый абзац в "Ключевые архитектурные
    решения" выше про `useBoardLayout.orientation`/`useAdaptiveOrientation`/переворот `BoardGrid`. Кратко
    что изменилось по файлам:
    - `useAdaptiveOrientation.js` — новый (замена `useLockLandscape.js`, файл удалён), снимает лок
      ориентации на экране сессии вместо жёсткого лока в альбомную, возвращает portrait-лок на выходе.
    - `useBoardLayout.js` — считает `orientation` по `screenH >= screenW`, полностью отдельная ветка
      геометрии для portrait (панель снизу фиксированной высоты `panelH`, доска сверху скроллится по Y).
    - `useBoardScroll.js` — параметризован `axis` ('x'|'y', PanResponder читает dx/vx или dy/vy) и
      переименован (`xOffset`→`offset`, `segmentW`→`segmentSize` как общий параметр,
      `leftButtonProps/rightButtonProps`→`backButtonProps/forwardButtonProps` — семантика "к
      началу"/"вперёд по трассе", не буквально "влево"/"вправо", т.к. в портретной раскладке это
      вверх/вниз). Числовая семантика offset'а (0=начало, minOffset=конец) НЕ менялась — переиспользована
      как есть в обеих раскладках.
    - `BoardGrid.js` — портретная ветка: дорожки — вертикальные полосы (стек по горизонтали, все 6 без
      скролла), скролл по Y. "Снизу вверх" реализовано БЕЗ отдельной системы координат offset'а — контент
      строится в обычной системе (positionX=0 сверху, как X в альбомной раскладке), результат целиком
      отражается `transform: scaleY(-1)`, листовые визуальные элементы (картинка клетки, токен бегуна)
      получают контр-отражение. Второй обязательный кусок — внешний `container` с `justifyContent:
      'flex-end'` (портрет), иначе высокий контент (3 фрагмента) по умолчанию прижимается к ВЕРХУ
      вьюпорта, а нужно к низу (иначе при offset=0 видно дальний конец трассы, а не начало). Подробный
      разбор геометрии (система уравнений на `container_y`) — в шапке файла. **Это самая рискованная
      часть всей правки** — сделана на бумаге, БЕЗ единой визуальной проверки (нет доступа к
      устройству/браузеру из сессии). Если направление скролла, обрезка или что что закрывает выйдут не
      те — смотреть в BoardGrid.js в первую очередь.
    - `ArrowButton.js` — добавлены glyphs `↑`/`↓` (direction 'up'/'down').
    - `GameConstants.js` — новые `LAYOUT.PANEL_MIN_H/MAX_H/HEIGHT_RATIO` (портретный аналог
      `LEFT_PANEL_*`).
    - `PlayerInfoPanel.js` — новый необязательный проп `height` (портрет: `panelH`; альбомная раскладка
      его не передаёт, панель как раньше тянется на всю высоту через flex).
    - `GameBoardScreen.js` — `wrapper`/`roadZone` переключают `flexDirection` по `orientation`,
      `PlayerInfoPanel` рендерится ДО доски в альбомной раскладке и ПОСЛЕ в портретной (порядок в JSX
      определяет визуальный порядок при смене flexDirection). Координаты ячеек (`cell.id`/`row`/`col`/
      `blockIndex`), которые видит `handleCellPress`, от ориентации не зависят — вся игровая логика
      (SELECT/MOVE/SHOOT/подсветка) не тронута, что заметно снижает риск регрессии геймплея от этой
      правки (риск сосредоточен именно в `BoardGrid`, см. выше).
  - Пользователь подтвердил: адаптивно везде (не только Android, по факту соотношения сторон), движение
    по трассе снизу вверх, нижний блок панели — не буквальная копия присланного эскиза (просто набросок),
    а вся существующая функциональность панели (переключатель/кубики/усиления/карточки бегунов), просто
    расположенная под доской — поэтому переиспользован тот же `PlayerInfoPanel` целиком, а не написан
    новый компактный дизайн с нуля.
  - Верификация: `@babel/core` с `babel-preset-expo` по всем новым/изменённым файлам — чисто. Живого
    прогона НЕ было вообще ни по одному из трёх пунктов этого захода (нет доступа к устройству/браузеру
    из сессии) — портретная раскладка особенно нуждается в скриншоте от пользователя перед тем, как
    считать её рабочей.

- 2026-08-13 (пятнадцатый заход): первый живой скриншот портретной раскладки с реального Android
  (см. четырнадцатый заход) — список из 10 конкретных правок по нему.
  - **Настоящая причина "ширина/высота сегмента перепутаны"** (видно на скриншоте — клетки заметно
    шире, чем выше): `useBoardLayout` считал `segmentW` от `boardBudgetW` (почти вся ширина экрана,
    ничем не делится с другим UI) ÷ ROWS(6), а `segmentH` — от `boardBudgetH` (узкий остаток ПОСЛЕ
    вычета panelH+стрелок) ÷ COLS(8): широкий числитель на маленький делитель против узкого числителя на
    большой — систематически давало `segmentW ≫ segmentH`, то есть "альбомные" широкие клетки в
    портретной раскладке. Фикс — единый `segmentSize` (квадратные клетки) от ширины (`boardBudgetW /
    ROWS`), `roadContainerH`/`minOffset` пересчитаны от РЕАЛЬНОГО оставшегося вертикального бюджета
    (`boardBudgetH`), а не наоборот. Заодно `roadContainerH` больше не ограничена ровно одним фрагментом
    (как в альбомной раскладке) — берёт весь доступный вертикальный бюджет целиком, чтобы поместить
    максимум рядов трассы без скролла (по общему пожеланию пользователя "уместить как можно больше").
  - **Зона стрелок вдвое меньше** (`arrowBtnSize`: `screenW*0.15` → `max(28, screenW*0.075)`), а
    освободившееся место отдано ПАНЕЛИ, не доске — `LAYOUT.PANEL_HEIGHT_RATIO` (0.42→0.52) и
    `PANEL_MAX_H` (420→480) подняты явно, `boardBudgetH` считается как и раньше (после вычета panelH+
    стрелок), так что рост панели + уменьшение стрелок в сумме НЕ увеличивают бюджет доски — он
    достаётся панели, как и просили.
  - **Инверсия свайпа** (`useBoardScroll.js`): `delta`/`velocity` для оси Y теперь берут `-dy`/`-vy`
    (было прямое `dy`/`vy`). Причина инверсии в имплементации (после `transform: scaleY(-1)` для
    "снизу вверх") на бумаге получалась ПРОТИВОПОЛОЖНОЙ тому, что увидел пользователь живьём (по моей
    выкладке свайп вверх уже должен был двигать контент вниз) — разбираться, ГДЕ именно разошлась
    теория с реальностью, не стал (нет доступа к устройству), просто откалибровал по факту
    наблюдаемого поведения: это самое дешёвое и надёжное решение независимо от точной причины
    расхождения. Если пользователь скажет, что теперь наоборот — знак минус в `delta`/`velocity`
    убрать, больше нигде трогать не придётся.
  - **"Стрелки вверх/вниз не работают"** — НЕ нашёл однозначный баг в коде `snapToBlock`/
    `useBoardScroll` при повторном построчном чтении (несколько раз перечитал — логика идентична
    штатно работающей альбомной, только параметризована по оси). Сделал две вещи, которые могли быть
    (или косвенно являться) причиной: (1) вышеописанный фикс геометрии — с рабочим `segmentH`
    scrollable-диапазон стал совсем другим (раньше `segmentH` был мелкий баг-артефакт), (2) упростил
    структуру трансформа в `BoardGrid` — раньше были ДВЕ вложенные View (`mirrorY` снаружи +
    `Animated.View` с `translateY` внутри), теперь ОДИН `Animated.View` с обоими трансформами в
    массиве (`[{translateY},{scaleY:-1}]`) — меньше вложенности с `overflow:hidden` могло играть роль
    на Android. Если после этого стрелки всё ещё не реагируют — нужно уточнить у пользователя КАК
    именно не работают: кнопка визуально не реагирует на тап вообще (тогда дело в хит-тестинге/z-order)
    или реагирует (потемнение при нажатии есть), но доска не двигается (тогда дело в `snapToBlock`).
  - **Панель переверстана**: `PlayerSwitcher.js` — редизайн в таб-бар (кружок цвета игрока + имя,
    активный таб подсвечен цветом + полоска снизу) по образцу референса пользователя (нижняя навигация
    вида Alex/Jordan/Sam/Map), общий на обе раскладки. `PlayerInfoPanel` — новые пропы
    `switcherAtBottom` (переключатель рендерится ПОСЛЕ прокручиваемого блока, а не до) и `headerContent`
    (произвольный узел рендерится там, где раньше было крупное имя игрока — Text с именем убран
    совсем, оно и так видно в кнопках переключателя). Жнец переставлен ПЕРЕД карточками обычных
    бегунов (был в самом низу списка). `RunnerCard`/`RunnerDiceSlot` — зона "кубик хода" теперь НАД
    кружочками повреждений (а не в один ряд с ними), на всю ширину карточки, `minHeight` 28→44
    (жалоба "не работает перетаскивание" — хит-тестинг на оконных координатах не был сломан, целиться
    в мелкую узкую зону пальцем на телефоне просто трудно).
  - **GameBoardScreen**: баннер "Твой ход" (`turnBannerInner`) вынесен в переменную и используется В
    ДВУХ МЕСТАХ — в альбомной раскладке как раньше (плавающий `styles.turnBanner` поверх доски), в
    портретной передаётся в `PlayerInfoPanel.headerContent` (там, где раньше было крупное имя игрока,
    по прямому запросу пользователя). `EventLogPanel` — новый проп `position` ('bottom-right' по
    умолчанию, 'top' для портретной раскладки — плашка сверху-слева, разворачивается вниз, а не вверх
    от кнопки).
  - Верификация: `@babel/core` с `babel-preset-expo` по всем изменённым файлам — чисто. Живого прогона
    НЕ было (нет доступа к устройству из сессии) — ждём новый скриншот/тест от пользователя, особенно
    по пункту "стрелки не работают", где нет стопроцентной уверенности в диагнозе.

- 2026-08-13 (шестнадцатый заход): второй живой скриншот (после пятнадцатого захода клетки уже
  квадратные, читаемые) + 5 новых замечаний. По направлению скролла явно спросил пользователя вопросом
  (не гадал третий раз) — подтверждено: "свайп вниз → видно дальше по треку" ("тянешь трассу к себе").
  - **Настоящая причина "драг кубика на карточку не работает"**: `RunnerDiceSlot`/`AbilityZone` мерят
    себя ОДИН раз через `measureInWindow` в `onLayout`. Пока список бегунов+усилений был внутри
    `ScrollView` (см. пятнадцатый заход), любой скролл этого списка делал закешированные оконные
    координаты зон устаревшими относительно реальной прокрутки — хит-тестинг дропа промахивался
    ВСЕГДА, куда бы кубик ни попал (ровно то, что описал пользователь). Фикс — не патч хит-тестинга, а
    architectural: `PlayerInfoPanel` получил `compactColumns` (портретная раскладка) — бегуны+Жнец
    слева, усиления справа, БЕЗ ScrollView вообще, карточки в `compact`-режиме (мельче иконка/шрифты/
    отступы, скрыта строка "в резерве"/"на поле" — по прямому запросу "сократи пространство под
    карточку... чтобы все бегуны поместились без прокрутки"). Раз скроллить нечего — measureInWindow
    остаётся актуальным весь ход. `RunnerCard`/`RunnerDiceSlot` — новый проп `compact`.
  - **Геометрия скролла переделана с нуля** — предыдущая версия (`scaleY(-1)` на всей сетке + контр-
    отражение каждой картинки/токена) на реальном Android дала сломанные стрелки И свайп, открывающий
    пустую зону вместо трассы. Заменено на `flexDirection: 'column-reverse'` для дорожек-колонок —
    ЧИСТО layout-свойство (клетки лежат в массиве по возрастанию globalCol, `column-reverse` сам рисует
    их снизу вверх), никаких transform/контр-отражений не нужно вообще — картинки клеток и токены
    бегунов рендерятся в нормальной ориентации. Вместе с этим **направление offset поменяно на
    положительное** для портретной раскладки (0=начало, minOffset>0=дальше по треку — в отличие от
    альбомной, где minOffset<0) — разобрано построчно на бумаге (система уравнений на container_y от
    globalCol и offset), с высокой уверенностью в этот раз (система проще, меньше отражений, где можно
    ошибиться знаком). `useBoardScroll.js` — `clamp`/`snapToBlock` обобщены под оба знака minOffset
    через `Math.sign`, вместо жёстко зашитого "[minOffset, 0]" как раньше (иначе портретная раскладка с
    положительным minOffset была бы клэмплена в [0,0] — вообще не скроллилась бы). Свайп по оси Y —
    БЕЗ инверсии (сырые `dy`/`vy`), потому что с новой (положительной) системой offset'а "палец вниз →
    dy>0 → offset растёт → дальше по треку" уже само сходится с подтверждённым пользователем
    направлением — предыдущая инверсия (`-dy`, четырнадцатый заход) была попыткой компенсировать
    неправильную geometry, а не реальным требованием к направлению самого жеста.
  - **Веб-путь (`useScrollAnimation.js`) тоже переделан** — раньше был жёстко заточен под отрицательный
    minOffset (`MAX_OFFSET=0` как "ближняя" граница, `minOffset` как "дальняя", в предположении что
    дальняя < 0). Раз портретная раскладка адаптивная (работает и на вебе при узком/высоком окне —
    было явно запрошено в четырнадцатом заходе), это тоже давало бы сломанный веб-скролл в портрете.
    Обобщено через `dirSign = minOffset<0 ? -1 : 1`; контракт `startScroll(direction)` изменён на
    направление ОТНОСИТЕЛЬНО дальнего конца (+1=вперёд/к minOffset, -1=назад/к 0), а не сырой знак
    движения — `useBoardScroll`'s `backButtonProps`/`forwardButtonProps` для веба обновлены под новый
    контракт.
  - **Стрелки вверх/вниз были перепутаны местами** с направлением свайпа — до этого захода "вверх" был
    закреплён за forward (см. пятнадцатый заход), а подтверждённое направление свайпа говорит, что
    вперёд — это ВНИЗ. Поправлено: первая стрелка (left в альбомной / up в портретной) — всегда back,
    вторая (right / down) — всегда forward, без раскладко-зависимого свопа как раньше.
  - **EventLogPanel наезжал на статус-бар/часы телефона** — `top: spacing.md` без поправки на safe-area
    (`GameBoardScreen` сознательно без `SafeAreaView`, см. его шапку). Фикс — `useSafeAreaInsets()` из
    уже используемого в проекте `react-native-safe-area-context`, `top: insets.top + spacing.sm`.
    Заодно тем же способом поправлен `collisionBanner` в `GameBoardScreen` — та же болячка, пользователь
    её не называл явно, но это ровно тот же класс бага и было бы странно чинить один и оставлять другой
    сломанным.
  - Верификация: `@babel/core` с `babel-preset-expo` (web+android caller) по всем изменённым файлам —
    чисто. Живого прогона НЕ было (нет доступа к устройству из сессии) — направление скролла в этот раз
    выведено куда увереннее (проще система, прямой вопрос пользователю вместо угадывания), но всё ещё
    не проверено на реальном экране.

- 2026-08-13 (семнадцатый заход): третий живой скриншот + 9 замечаний. Пользователь дал ПОСТОЯННОЕ
  разрешение на правки frontend без запроса на каждое изменение (git-ветки/PR по-прежнему не создавать,
  бэкенд по-прежнему read-only) — учтено на будущее. По стрелкам спросил явным вопросом второй раз
  (см. AskUserQuestion в истории чата) — подтверждено: у стрелок СВОЯ логика, ОБРАТНАЯ направлению
  свайпа (свайп вниз = вперёд по треку, но верхняя стрелка = вперёд, нижняя = назад), пользователь
  прямо сказал "забыл уточнить" в отдельном сообщении — это НЕ баг, а осознанный дизайн (кнопки и жест
  что называется "по-разному интуитивны").
  - **Верхняя стрелка рисовалась под вырезом камеры/статус-баром, не тапалась**: `roadZonePortrait` —
    первый flow-элемент экрана без `SafeAreaView` (см. шапку `GameBoardScreen`), стрелка попадала в
    физически недоступную зону. Фикс — `useSafeAreaInsets()` (`paddingTop: insets.top` на
    `roadZonePortrait`), и симметрично — `useBoardLayout.js` теперь тоже вызывает `useSafeAreaInsets()`
    и вычитает `insets.top` из `boardBudgetH`, иначе геометрия думала, что места больше, чем реально
    есть, и низ доски вылезал за пределы зоны.
  - **Стрелки/свайп перепутаны по направлению** — направление свайпа (подтверждено в прошлом заходе)
    осталось нетронутым, поправлена только маппинг стрелок: `backButtonProps`/`forwardButtonProps`
    поменяны местами относительно шестнадцатого захода (верхняя=forward, нижняя=back — было наоборот).
  - **Кирпичная кладка (нечётные дорожки) пропала, стала плиткой** — реальный баг после перехода на
    `column-reverse` (пятнадцатый-шестнадцатый заход): `laneRow` (flexDirection:'row') по умолчанию
    `alignItems:'stretch'`, растягивал/пересчитывал высоту лейн-колонок и гасил эффект `marginTop` у
    нечётных лейнов. Фикс — явная `height` на каждой лейн-колонке (`TOTAL_COLS*segmentH`) +
    `alignItems:'flex-start'` на `laneRow`, чтобы margin отсчитывался предсказуемо.
  - **Кубики уменьшены** (`DiceTray`/`DiceDie` — новый проп `size`, 34px в `compactColumns` вместо
    дефолтных 44) — по предложению пользователя, освобождает место карточкам бегунов.
  - **Метки повреждений перенесены вправо на плитку** (в `topRow`, рядом с иконкой/именем, в
    `compactColumns`) — убирает отдельную строку снизу, карточка короче. В альбомной раскладке — без
    изменений (своя строка ниже, как было).
  - **Настоящая причина "перетаскивание на плитку персонажа так и не заработало"** — ДВЕ проблемы
    одновременно: (1) зона дропа была только `RunnerDiceSlot` (маленький вложенный элемент), не вся
    карточка, хотя внешне казалось, что карточка растянута; (2) при живом использовании компактная левая
    колонка (без скролла, шестнадцатый заход) на реальном экране не поместила Скаута целиком — пришлось
    вернуть `ScrollView` для неё (по прямому запросу пользователя: "не думаю, что на маленьких экранах
    всё будет влезать"), а это снова открывало путь к уже чиненному багу "устаревший measureInWindow
    после скролла". Оба решены вместе: измерение зоны `move:<runnerId>` переехало из `RunnerDiceSlot` в
    саму карточку (`RunnerCard`, целиком, через `onLayout`+`measureInWindow` на внешнем
    `TouchableOpacity`) — теперь ВСЯ плитка кликабельна/дропабельна, не только видимый пунктирный
    прямоугольник. `RunnerDiceSlot` стал чисто визуальным (убраны `ref`/`onLayout`/`onMeasured`/
    `zoneKey`). Новый `remeasureTick` проп у `RunnerCard` — `PlayerInfoPanel` инкрементит его на
    `onScrollEndDrag`/`onMomentumScrollEnd` левой `ScrollView`, каждая карточка перемеряет себя заново
    (эффект зависит от `remeasureTick`) — держит оконные координаты актуальными после любой прокрутки,
    а не только на момент первого рендера.
  - **"Возврат кубика обратно на место"** — уже РАБОТАЕТ, ничего нового не писал: тап по карточке, пока
    `pendingSelect` на неё указывает, уже отменяет выбор (`GameBoardScreen.handleRunnerCardPress`,
    существует с двенадцатого захода), плюс кнопка "Отмена" в баннере хода (виден в `headerContent`
    панели в портретной раскладке). Пользователь предлагал "тапни по кубику" — поскольку кубик
    отображается ВНУТРИ той же самой кликабельной карточки (не отдельный `TouchableOpacity`), тап по
    нему И ЕСТЬ тап по карточке — уже покрыто. Просто раньше до этого состояния не удавалось дойти
    из-за бага с дропом выше — теперь, когда дроп реально регистрируется, эта уже существующая механика
    должна стать видна и пользователю.
  - Верификация: `@babel/core` с `babel-preset-expo` (web+android caller) по всем изменённым файлам —
    чисто. Живого прогона НЕ было (нет доступа к устройству из сессии).

- 2026-08-14: подсветка легальной клетки (MOVE/SHOOT/reaper/первый выход) переведена с ручной рамки+
  цветной заливки (`BoardGrid.styles.highlight`, `View` поверх картинки клетки) на ассет
  `assets/images/road/allowed_move.png` (пользователь добавил файл, новая константа
  `GameConstants.HIGHLIGHT_IMAGE`) — рисуется ПОД обычной картинкой типа клетки (та по-прежнему
  `opacity:0.9`, так что подложка просвечивает), а не поверх. `styles.highlight` и импорт `colors` в
  `BoardGrid.js` удалены как более не используемые. Верификация — `@babel/core`, чисто; визуально не
  проверено.

- 2026-08-15 (восемнадцатый заход): **первый в этом проекте живой прогон через реальный браузер**
  (Browser pane заработал — предыдущие сессии он таймаутил на "pane is hidden"; помог локально
  поднятый Docker-бэк пользователя). Проверял живьём 4 жалобы + докопался до настоящей причины
  зависающих обновлений хода — она оказалась НА БЭКЕНДЕ, не во фронтенде.
  - **Ghost-кубик при драге на вебе**: жалоба — сам кубик во время перетаскивания рисуется ЗА
    карточкой бегуна (не поверх), хотя на Android поверх нормально. Не разбирался, ГДЕ именно ломается
    stacking (иначе рисковал третий раз гадать с z-index вслепую) — вместо этого добавил в
    `PlayerInfoPanel.js` независимый ghost-слой: на вебе, во время драга, рисуется КОПИЯ кубика с
    `position:'fixed'` (не зависит ни от какого родительского стекинга, координаты — сырые
    `e.absoluteX/Y` из жеста, пересчёт под какого-то родителя не нужен). Оригинальный `DiceDie` не
    трогал. Живьём в браузере подтверждено: полный цикл драг→дроп→подтверждение на Джаггернауте прошёл
    чисто, карточка подсветилась и приняла кубик с первого раза.
  - **allowed_move.png "впритык", а должен слегка выходить за пределы клетки**: добавил
    `HIGHLIGHT_OVERFLOW = 0.3` в `BoardGrid.js` — картинка теперь на 30% больше сегмента, центрирована
    отрицательными `left`/`top`. Живьём подтверждено скриншотом — свечение видно выходящим за границы
    плитки.
  - **Настоящая причина зависающих обновлений хода ("твой ход" есть, подсказки нет, кубики
    заблокированы, помогает только F5)** — НАЙДЕНА ЖИВЬЮМ, воспроизведена детерминированно: сыграл
    полный ход вторым игроком через `curl` (SELECT→ABILITY skip→MOVE до истощения кубика бегуна), ход
    перешёл первому игроку (веб-браузер) — баннер "Твой ход" показался, а текст-подсказка и кубики
    остались заблокированными БЕЗ F5. После `navigate force` (эквивалент F5) — подсказка сразу
    появилась, кубики разблокировались. **Это бэкенд-баг, не фронтенд**: в
    `TurnService::nextTurn()` (D:\Programming\runner-game-backend, НЕ трогал — read-only) строка
    `$nextPlayer->setStep(PlayerStep::SELECT)` корректно пишет новый шаг в БД (поэтому REST-снапшот
    после F5 всегда отдаёт правильное значение), но публикуется только `GameTurnChangedEvent`
    (только game-уровневые поля — `step`/`round`/`playerOrder`/`extraTurnPlayer`, БЕЗ per-player полей).
    Событие, которое обновило бы `player.step` у ВХОДЯЩЕГО игрока во фронтенд-стейте (`PlayerStepEvent`,
    тот же класс, что уже используется в `PlayerStepService::step()` для абсолютно того же перехода в
    других местах, и `StepBeginService::start()/startNewRound()` для первого хода партии/нового раунда —
    ОБА этих места публикуют `StepBeginEvent`/корректны), в `TurnService::nextTurn()` просто не
    публикуется вообще. Фронт (`runnerGameReducer.js`, `case 'player_step'`) уже готов его принять —
    чинить нечего, ждём фикса на бэке. **Список для бэкенд-разработчика — см. ниже.**
  - **Зоны усилений/карточки бегунов в общем ScrollView (альбомная раскладка) — та же болезнь с
    устаревающим `measureInWindow`, что уже чинил для портретной `compactColumns`** (см. семнадцатый
    заход), просто раньше не заметил, что альбомная раскладка ТОЖЕ держит `AbilityZones` и карточки
    бегунов в одном скроллящемся `infoColumn` без пересчёта после скролла — жалоба пользователя
    ("перетащил на одно усиление, подсветилось то, что снизу") прямо на это указывает (офсет ровно на
    высоту соседней зоны — классический симптом устаревшего кэша после прокрутки). Фикс: тот же паттерн
    — `AbilityZone.js` получил `remeasureTick` (эффект перемеряет себя при изменении), `AbilityZones.js`
    прокидывает проп насквозь, `PlayerInfoPanel.js` — `onScrollEndDrag`/`onMomentumScrollEnd` на
    landscape-варианте `infoColumn` ScrollView теперь тоже дёргают `bumpRemeasure` (общий с
    compactColumns). Не проверено конкретно этим сценарием (скролл + драг на другую зону), но паттерн
    уже подтверждён на RunnerCard.
  - **Лобби-ready для Android не долетает до другого клиента (жалоба пользователя)** — проверил
    `LobbyService::ready()` — публикует `LobbyPlayerReadyEvent` корректно, паттерн ИДЕНТИЧЕН рабочему
    `RunnerGameService::start()`/`PlayerActiveEvent` (который живьём подтверждён — дошёл до
    веб-браузера, хоть и с заметной задержкой ~30-60 сек в этой тестовой среде с шумом от старых
    Android-подключений в логах Mercure-хаба). Бага в этом КОДЕ не нашёл — вероятно, это всё же сетевая
    проблема специфично Android-эмулятор+веб одновременно (см. тринадцатый заход, не диагностировано
    точнее и в этот раз — нет доступа к самому эмулятору из сессии).
  - **Как тестировал**: `docker ps` — бэк пользователя уже был поднят локально. Запустил `expo start
    --web` через Browser pane (`.claude/launch.json` создан), зарегистрировал 2 тестовых аккаунта
    (`claudeweb1`/`claudeweb2`) — ОДИН через сам браузер (не смог открыть второй таб браузера в этой
    среде, второй таб стабильно не рендерился), ВТОРОЙ вёл через `curl` напрямую к `POST
    /api/runner_game/*`, наблюдая реакцию первого (браузерного) игрока живьём. `docker logs
    runner_mercure`/`runner_php` — читал напрямую, чтобы отличить "событие не опубликовано" от "событие
    опубликовано, но не дошло/не обработано".
  - **Список правок для бэкенд-разработчика** (не трогал сам — read-only):
    1. `TurnService::nextTurn()` (D:\Programming\runner-game-backend\src\Service\Game\RunnerGame\Service\TurnService.php:45-51) —
       после `$nextPlayer->setStep(PlayerStep::SELECT)` не хватает
       `$this->mercureService->queue(new PlayerStepEvent($nextPlayer))` (или эквивалентного события,
       несущего `player.step`) ПЕРЕД `$this->publishTurnChanged($game)`. Без этого фронт входящего
       игрока навсегда остаётся с устаревшим `step`, пока не сделает полный ре-фетч (F5/переоткрытие
       экрана) — это касается КАЖДОЙ смены хода в рамках раунда (самый частый путь), не только новых
       раундов (те через `StepBeginService`, которые событие публикуют правильно).
  - Верификация: живой браузерный прогон (регистрация, лобби, ready-up, полный ход curl-игроком,
    переход хода, драг-н-дроп кубика, подтверждение выбора, выход бегуна на трассу, подсветка) —
    подтверждено скриншотами на каждом шаге. `@babel/core` с `babel-preset-expo` по всем изменённым
    файлам — чисто.

- 2026-08-15 (девятнадцатый заход): подсветка `allowed_move.png` из прошлого захода залезала на
  соседние сегменты (клетки в сетке стоят впритык, без зазора — увеличенная подложка неизбежно
  перекрывала соседа). По уточнению пользователя — развернул логику: подложка теперь РОВНО размера
  слота клетки (без overflow), а сама картинка типа клетки (road/sand/...) стала МЕНЬШЕ слота на
  `SEGMENT_INSET = 0.15` (15%, отцентрована) — "свечение" получается за счёт зазора между уменьшенной
  картинкой типа и границей слота, а не за счёт того, что подложка больше слота. Живьём (тот же
  докер-бэк, авто-релогин в уже сохранённую сессию/партию из прошлого захода) проверил через
  `getBoundingClientRect()` по всем тайлам в ряду: слот подсветки (`allowed_move.png`) шириной ровно
  41px кончается там же, где начинается слот следующей (не подсвеченной) клетки — наложения нет.
  Верификация — `@babel/core` чисто + живой скриншот/JS-замер, не только код.

- 2026-08-15 (двадцатый заход): жалоба на дубль кубика при драге (веб) + запрос на переверстку карточки
  бегуна (два квадрата "Ход"/"Накат" вместо одной широкой пунктирной зоны, вся карточка — зона дропа,
  колонка усилений уже, кубики переехали в правую колонку над усилениями в портретной раскладке). По
  пути найдена и почищена НАСТОЯЩАЯ причина многолетнего (13–17 заходы) "кубик прилипает не туда"/
  "подсветка чуть выше зоны" — не в хит-тестинге зон (тот код был верен с самого начала), а в самом
  `DiceDie`: кубик двигался через `e.translationX/Y` ОТ ТОЧКИ ХВАТА, поэтому если схватить его не
  строго по центру, видимая позиция кубика и хит-тестируемая точка (`e.absoluteX/Y`, которую слал
  `onDragMove`) расходились на этот сдвиг.
  - **Дубль кубика (веб)**: в восемнадцатом заходе поверх добавили независимый "призрак"-кубик
    (`position:fixed`, следует за курсором), но исходный кубик (двигался через `transform` в
    `DiceTray`) не спрятали — во время драга было видно оба. Фикс: `DiceDie` — `opacity:0` на
    оригинал во время `dragging.value` именно на вебе (на native призрака нет, там оригинал и есть
    обратная связь).
  - **Переписан `DiceDie` целиком**: кубик во время драга теперь визуально "прилипает" ЦЕНТРОМ ровно к
    текущей `e.absoluteX/Y` (не сохраняет смещение точки хвата) — `transform: translate(absoluteX/Y −
    origin)`, где `origin` — координаты кубика в состоянии покоя (`measureInWindow` на `onLayout`, не
    на каждый кадр). Хит-тест (`onDragMove`/`onDrop`) шлёт ТУ ЖЕ `e.absoluteX/Y` — визуальная позиция
    и хит-тест гарантированно совпадают по построению, без отдельного вычисления смещения.
  - **Диагностика жила дольше, чем сам фикс** — первая попытка (origin + `e.translationX/Y`) и вторая
    (origin + смещение, захваченное в `onStart` через `e.absoluteX/Y`) обе на вид корректны, но обе
    сломались при живой проверке через автоматизированный драг (`computer.left_click_drag` в Browser
    pane): дебаг-логами подтвердил, что `e.translationX/Y` у `react-native-gesture-handler` (web,
    v3.0.1) застревает в 0 при быстром программном вводе (мало промежуточных `pointermove`, событие
    "прыгает" сразу к финальной точке — реальный человек/палец физически не может дать такой ввод, это
    артефакт автоматизации), а `onStart` в тех же условиях улавливает УЖЕ финальную позицию курсора
    вместо истинной точки нажатия. `e.absoluteX/Y` в `onUpdate`/`onEnd` — единственное поле, которое
    во всех трёх попытках стабильно отражало реальную позицию, поэтому финальная версия строится
    только на нём (плюс `origin` из независимого `measureInWindow`, не связанного с самим жестом).
  - **Карточка бегуна (`RunnerCard`/`RunnerDiceSlot`) переверстана**: убрана пунктирная зона-приманка
    "перетащи сюда кубик хода" (путала — казалось, что цель именно она, хотя технически ловила вся
    карточка ещё с семнадцатого захода) — заменена на два маленьких квадрата "Ход"
    (`runner.dice`)/"Накат" (`runner.rollDice`), чисто отображение, всегда видны (по выбору
    пользователя — не гасить, даже если накат сейчас недоступен по правилам). Подсветка "сюда можно
    бросить" переехала на рамку ВСЕЙ карточки (`hoverState` prop → `cardHoverValid`/`cardHoverInvalid`)
    вместо мелкого элемента внутри. Тип (обычный ход/накат) по-прежнему решает `canSelectRunner` в
    `GameBoardScreen` по состоянию бегуна, не то, в какой квадрат навели кубик — там ничего не менял.
    Применено и в альбомной, и в портретной раскладке (по выбору пользователя — единообразие важнее).
  - **Портретная раскладка (`PlayerInfoPanel.compactColumns`)**: колонки 3:2 → 2:1 (бегунам больше
    места), `DiceTray` переехал из полосы над всем списком в правую колонку, НАД усилениями (не над
    бегунами) — по прямому запросу пользователя. `DiceTray` — добавлен `flexWrap` на случай, если 4
    кубика не влезают в узкую колонку в один ряд.
  - **Живая проверка через Browser pane + локальный докер-бэк** (два свежих тестовых аккаунта,
    `claudetest1`/`claudetest2`, `claudetest1` — в браузере, `claudetest2` — через `curl`, тот же
    паттерн, что в восемнадцатом заходе): полный цикл SELECT (драг кубика на карточку) → подтверждение
    → ABILITY (драг кубика на "Буст", тот же `DiceDie`) → MOVE (тап по подсвеченной клетке, выход на
    трассу) — всё отработало 1-в-1 на реальном бэке, скриншотами на каждом шаге. Портретная раскладка
    подтверждена ТОЛЬКО визуально (скриншот на 375×812) — драг там не проверен, `computer.left_click_
    drag` стабильно вешался (таймаут) при touch-эмуляции мобильного вьюпорта (похоже на ограничение
    самого инструмента автоматизации под touch-режимом, не на баг кода — та же логика `DiceDie`/
    `RunnerCard` уже подтверждена в десктоп-режиме). Если пользователь словит баг именно на
    портретной раскладке на реальном телефоне — начинать разбор не с `DiceDie` (эта часть теперь
    хорошо проверена), а с самой геометрии колонок/scroll в этом режиме.
  - Верификация: `@babel/core` с `babel-preset-expo` (web+android caller) по всем изменённым файлам —
    чисто. Живой прогон — см. выше, самый полный за всю историю проекта (первый раз реально
    отработавший драг-н-дроп кубика через автоматизацию, не только статика).

- 2026-08-15 (двадцать первый заход): первый реальный прогон на физическом Android-устройстве (4
  скриншота от пользователя) вскрыл то, что не могла поймать автоматизация из двадцатого захода — сама
  Browser pane автоматизация только что была подтверждена рабочей ТОЛЬКО в вебе, для Android live-теста
  по-прежнему нет доступа из сессии, весь разбор ниже — по скриншотам + статическому чтению кода.
  - **Кубик при драге на Android оказывается смещён от пальца/курсора, хит-тест карточки бегуна
    срабатывает только в узкой невидимой полосе, а не по всей видимой карточке**. Гипотеза (НЕ
    подтверждена физическим устройством из сессии): `e.absoluteX/Y` от `react-native-gesture-handler` и
    `measureInWindow` могут разъезжаться на Android на системный сдвиг (вероятный кандидат —
    edge-to-edge/статус-бар — в `app.json` нет explicit-настроек под это, значит действуют дефолты
    Expo SDK 56, которые на новых Android врубают edge-to-edge принудительно). Фикс: `DiceDie.js` —
    развёл стратегию по платформе. На вебе осталось как в двадцатом заходе (`e.absoluteX/Y` напрямую,
    подтверждено живым прогоном). На native — вернул `origin (measureInWindow) + e.translationX/Y`
    (первая версия из двадцатого захода, которую тогда откатили): `translationX/Y` — ЧИСТАЯ ДЕЛЬТА с
    начала жеста, не завязанная на оконные координаты вообще, поэтому иммунна к ЛЮБОМУ константному
    сдвигу между `measureInWindow` и координатами жеста, каким бы ни был его источник. Проблема
    "`translationX/Y` залипает в 0", из-за которой эту версию откатили в двадцатом заходе, была
    подтверждённо специфична для web-реализации gesture-handler под синтетическим автоматизированным
    вводом (мало promise pointermove-событий) — на native (нативный код, не JS PointerEventManager) её
    ждать нет оснований, это устоявшийся, десятилетиями обкатанный в RN-экосистеме механизм. **Не
    проверено живьём** — гипотеза правдоподобна и хорошо объясняет ОБА симптома разом (смещение кубика
    И узкую полосу хит-теста — оба чинятся одним и тем же переходом на дельта-модель), но
    подтверждения с реального устройства ещё не было.
  - Заодно (независимо от гипотезы выше, доп. подстраховка): `RunnerCard.js`/`AbilityZone.js`/
    `DiceDie.js` — `measure()` обёрнут в `requestAnimationFrame` перед `measureInWindow` — известный
    паттерн для Android, где `measureInWindow`, вызванный СРАЗУ в `onLayout`, иногда возвращает
    координаты до того, как реальный layout-проход до конца устаканился (особенно после смены
    шрифта/картинок/родительского флекса за один кадр). Тоже не проверено живьём.
  - **Переверстка карточки бегуна** (по прямому запросу пользователя, чтобы карточка была уже):
    квадраты "Ход"/"Накат" переехали из отдельной строки на всю ширину в узкую колонку СПРАВА, друг под
    другом (Ход сверху, Накат под ним); кружки повреждений переехали из верхней строки (рядом с
    иконкой) под имя/статус. Применено в обеих раскладках. Живьём проверено на вебе (десктоп и
    375×812) — рендерится как задумано.
  - **Переключатель игроков ещё уже**: `useBoardLayout.js` — `switcherH` в портретной раскладке
    `panelH * 0.15` → `max(36, panelH * 0.09)` (тот же приём, что уже применялся к `arrowBtnSize` в
    пятнадцатом заходе) — освободившееся место достаётся списку карточек бегунов. Живьём подтверждено
    скриншотом (375×812).
  - **Имя бегуна в тексте подтверждения выбора**: `GameBoardScreen.js` — `stepInstruction` принимает
    новый параметр `pendingRunnerName` (вычисляется через `RUNNER_DISPLAY[runner.type].label` по
    `pendingSelect.runnerId`), текст теперь `«Штурмовик» выбран — подтверди...` вместо безличного
    `Бегун выбран...`. Не проверено живьём в этом заходе (не успел довести тестовую партию до состояния
    pendingSelect после рестарта dev-сервера — предыдущая тестовая партия успела уйти дальше шага
    SELECT), но правка тривиальна и по коду очевидно корректна.
  - Верификация: `@babel/core` с `babel-preset-expo` (web+android caller) по всем изменённым файлам —
    чисто. Живой прогон — только веб (макет карточки + переключатель, скриншотами), Android-специфичная
    часть (позиционирование кубика, RAF-фикс measure) — ждёт следующего теста пользователя на реальном
    устройстве, это самая рискованная и неподтверждённая часть этого захода.

- 2026-08-16: найдена и починена НАСТОЯЩАЯ причина многозаходного бага "фон/анимация пропадают на
  Android" (полигон `DevPlaygroundScreen`, но баг был в `ParallaxBackground` — общий компонент, значит
  затрагивал вообще все экраны). Пользователь чётко помнил, что анимация на экране авторизации когда-то
  работала на Android так же, как на вебе — это оказалось решающей зацепкой: git-история файла показала,
  что он пришёл в проект уже AI-написанным с самого первого коммита (`src/screens/ParallaxBackground.js`
  → позже переехал в `src/components/ui/`), и есть независимая копия ВСЕГО проекта на диске
  (`D:\Programming\runner-game-frontend` — старая, без игровой логики, но с работавшим фоном), что дало
  чистый эталон для сравнения байт-в-байт.
  - **Настоящая причина**: способ планирования следующего цикла бесконечной parallax-анимации.
    Не работает (ломает видимость `Animated.Image` на Android под непрерывной анимацией, хотя сама
    анимация продолжает честно крутиться — подтверждено логами shared value):
    ```js
    offsetX.value = withTiming(x, config, (finished) => {
      'worklet';
      if (finished) runOnJS(animateToRandom)();
    });
    ```
    Работает (проверено живым прогоном на эмуляторе + подтверждено пользователем на реальном
    OnePlus 15) — чистый JS `setTimeout` вместо колбэка ворклета:
    ```js
    setTimeout(() => { if (isMounted.current) animateToRandom(); }, 4100);
    ```
    Найдено методом бисекции: поочерёдно тестировал `zIndex:-1` vs без него, `useWindowDimensions`
    (реактивно) vs статичный `Dimensions.get('window')`, `Animated.Image` напрямую vs `Image` внутри
    `Animated.View`-обёртки (в процессе поиска даже поставил `expo-image`/Glide вместо RN
    `Image`/Fresco — тоже не помогло, значит дело вообще не в библиотеке картинок) — НИ ОДНО из этого
    не влияло на результат при живой проверке на эмуляторе. Единственная переменная, которая
    ГАРАНТИРОВАННО меняла результат при прочих равных, — способ рекурсии. Точная причина на уровне
    Reanimated/Fabric не установлена (не нашёл официального issue, только эмпирика): вероятный
    кандидат — асимметрия между `offsetX` (с колбэком) и `offsetY` (без колбэка) в паре с тем, что
    колбэк выполняется НА UI-потоке и тут же переприсваивает ОБА shared value новым `withTiming`,
    отчего ломается Fabric-коммит именно для поддерева с `Image`-потомком.
  - **Побочный баг, найденный при переносе на `setTimeout`**: `runOnJS(animateToRandom)()` внутри
    уже-JS `setTimeout`-колбэка не нужен (`runOnJS` предназначен для обратного перехода
    UI-поток→JS-поток ИЗ ворклета, вызывать его напрямую из JS — плохая идея) и на практике не
    срабатывал повторно — картинка проявлялась, но застывала после первого цикла (~4 сек), что тоже
    поймано живым прогоном. Исправлено на прямой вызов `animateToRandom()`.
  - **НЕ стоит завязывать `useEffect` монтирования на `[screenW, screenH]`**, чтобы ловить ресайз —
    пробовал добавить реактивность к границам параллакса именно так (плюс через `boundsRef`,
    обновляемый на каждый рендер без пересоздания эффекта) — оба варианта ломали Android тем же
    образом, что и колбэк-рекурсия (переисполнение эффекта = повторный старт анимации =```` та же
    проблема). `useWindowDimensions()` без затей, с `useEffect(() => {...}, [])` (эффект строго один
    раз при монтировании) — реактивен САМ ПО СЕБЕ на новый рендер (значения `screenW/screenH` в
    замыкании `animateToRandom` всё равно свежие на КАЖДЫЙ НОВЫЙ вызов этой функции, а не только на
    момент монтирования, потому что функция переопределяется на каждый рендер и текущий вызов из
    `setTimeout` — это всегда самая свежая версия) и этого достаточно — разделение по платформам
    (`Platform.OS==='web'` ветка на `Dimensions.get` для native) не потребовалось вообще, только
    усложняло и один раз случайно сломало (типо в процессе экспериментов).
  - **Методологическая заметка на будущее**: в этом заходе несколько раз ловил ложные срабатывания
    "фон снова сломан" из-за устаревшего Metro-бандла (сервер физически отдавал старую версию файла,
    подтверждено прямым `curl` по `/index.bundle` и `grep` на маркеры кода) и из-за путаницы с
    таймингом скриншота относительно момента реального холодного старта приложения (после
    `am force-stop` + `monkey`-релонча нужно дожидаться нового `"Running \"main\""` в `adb logcat` С
    ПРОВЕРКОЙ, что это НЕ старая запись из буфера, а не просто спать фиксированное время). Каждое
    последующее изменение файла проверялось строго через: `git diff`/полный `Write` (не полагаться на
    "я же только что читал файл") → babel-синтаксис-чек → **полный холодный релонч** (не Fast Refresh,
    он тоже пару раз давал ложноположительный/ложноотрицательный результат в этой сессии) →
    подтверждение по свежему таймстампу в logcat → скриншот через `adb exec-out screencap`.
  - Финальная версия `ParallaxBackground.js` — `useWindowDimensions()` универсально (без
    platform-веток), `setTimeout`-рекурсия, `Animated.Image` напрямую (без обёртки), без `zIndex`.
    Подтверждено рабочим: на Android-эмуляторе (несколько прогонов, разные скриншоты) И на реальном
    устройстве пользователя (OnePlus 15, "воо, вижу анимацию на экране авторизации" / "всё работает
    на андроиде").

- 2026-08-29: сегменты дороги (`BoardGrid`/`SEGMENT_IMAGES`) — квадратные клетки, случайные варианты
  ассета на тип, платформенный сплит тяжёлых/лёгких файлов, точечная подсветка/поворот/прозрачность.
  Бэк в этой сессии не поднимался (Docker недоступен) — всё проверено живьём через веб (`expo start
  --web` + Browser pane), но БЕЗ реального игрового состояния: временно подменял `AuthScreen` на
  `screens/__BoardPreview.js` (мок-сегменты всех 6 типов через `lib/board.flattenTrackSegments`,
  без бэка), снимал геометрию/стили не скриншотом (compositing Browser pane не поднимался в этой
  сессии — таймаут "pane is hidden", тот же класс проблемы, что и в прошлых заходах), а через
  `javascript_tool`: `getBoundingClientRect()` по всем `<img>`, computed style/`outerHTML` слоёв —
  РН-веб рендерит `Image` как `div[style*=background-image]` (видимый слой) + скрытый
  `img.css-accessibilityImage` (opacity:0, только для a11y/семантики), поэтому проверять нужно
  computed-стили ИМЕННО первого, не `<img>`. После каждой проверки правил `RootNavigator` и удалял
  временный файл — в финале `git status` чистый, PID процесса `expo start` убивал вручную через
  `Get-CimInstance Win32_Process`/`Stop-Process` (сам процесс запускался как `cmd.exe → node`, обычный
  `Ctrl+C`/kill по одному PID не брал дочерний node).
  - **Квадратные сегменты**: `useBoardLayout.js` — в альбомной раскладке `segmentW`/`segmentH` раньше
    считались независимо (ширина ÷ COLS, высота ÷ ROWS), почти никогда не давая квадрат. Теперь один
    `segmentSize = min(roadBudgetW/COLS, roadBudgetH/ROWS)` — не вылезает ни за один из бюджетов,
    остаток по нелимитирующей оси просто пустой (roadZone в GameBoardScreen центрирует контейнер
    флексом, так не ломает раскладку). Портретная раскладка уже была квадратной с 15-го захода
    (2026-08-13) — тут не трогал.
  - **Случайный (но стабильный) вариант ассета на тип**: `SEGMENT_IMAGES` в `GameConstants.js` — теперь
    массив вариантов на тип, не один файл. `lib/board.js#pickSegmentImage(type, cellId)` — детерминированный
    выбор по хэшу id клетки (свой `hashString`, НЕ `Math.random()` — иначе картинка "прыгала" бы на
    каждый ре-рендер экрана/live-обновление стейта партии, а gif ещё и перезапускал бы анимацию).
    `flattenTrackSegments` кладёт готовый `cell.image` сразу при разворачивании сегментов —
    `BoardGrid` просто рендерит `cell.image`, сам не выбирает.
  - **`anomaly` получил собственный ассет** (`black_hole_1/2/3.gif`) — раньше в `lib/board.js`
    `resolveCellVisual` схлопывал `anomaly` в `danger` (своей картинки не было). Сопоставление
    подтверждено явным вопросом пользователю (не угадывал) — "это визуал типа anomaly".
  - **Подсветка легальной клетки**: пользователь удалил `allowed_move.png` без замены (это ломало
    сборку — `require()` на несуществующий файл валит Metro) и явным ответом попросил вернуть прежний
    подход (был до 2026-08-14) — тонкая рамка+полупрозрачная заливка вместо ассета-подложки, только не
    толстую. `HIGHLIGHT_COLOR` (`colors.success`, зелёный) в `GameConstants.js`, `BoardGrid` рисует
    `View` с `borderWidth: 1.5`/`backgroundColor: color+'55'`. Заодно уменьшен `SEGMENT_INSET`
    (0.15→0.06, картинка типа клетки чуть меньше слота, отцентрована) — по прямому запросу "расстояние
    между сегментами сократи до минимума, только чтоб уместить заливку".
  - **Платформенный сплит "тяжёлый/лёгкий" ассет**: пользователь сам добавил облегчённые дубликаты с
    припиской `-ez` для gif-типов (wall/danger/anomaly/mud) и позже для road/sand (png) — на реальном
    Android тяжёлые файлы ощутимо тормозили (например `wall_base_1.gif` 2.2МБ vs `wall_base_1-ez.gif`
    252КБ, `sand_base_1.png` 420КБ vs `sand_base_1-ez.png` 92КБ). `GameConstants.js` — два полных набора
    `SEGMENT_IMAGES_WEB`/`SEGMENT_IMAGES_MOBILE` (все `require()` статические — Metro не умеет
    динамический путь), экспортируемый `SEGMENT_IMAGES` выбирается ОДИН раз на уровне модуля через
    `Platform.OS === 'web' ? ... : ...`. Не по кадру/рендеру — не нагружает лишним сравнением.
  - **road/sand несколько раз меняли форму по ходу сессии** (пользователь сам правил ассеты между
    запросами) — финальное состояние: `road` — один вариант (`road_base.png`/`road_base-ez.png`),
    `sand` — три варианта (`sand_base_1/2/3.png` + `-ez`-пары), выбор варианта — тот же
    `pickSegmentImage`, что и у остальных многовариантных типов.
  - **Поворот на 90° по часовой — только веб, только альбомная (горизонтальная) раскладка, только
    road/sand/mud** (dirt) — по цепочке уточнений пользователя (сперва просил все сегменты, следующим
    сообщением сузил до этих трёх типов; wall/danger/anomaly не трогать). `BoardGrid.js` —
    `rotateEligible = Platform.OS==='web' && !isPortrait`, `ROTATE_TYPES = new Set(['road','sand','mud'])`,
    `transform: [{rotate:'90deg'}]` добавляется в стиль `Image` только когда оба условия выполнены.
    Клетки квадратные (см. выше) — поворот без искажений/подмены width↔height, чистый CSS-transform.
  - **road/sand — непрозрачные** (`opacity: 1`, было общее `0.9` для всех типов) — по прямому запросу,
    у остальных типов `0.9` осталась (чтобы заливка подсветки снизу была видна по краю).
  - Верификация: `@babel/core` (`babel-preset-expo`, web И android caller) по всем изменённым файлам на
    каждом шаге — чисто. Кросс-референс: скриптом сверял каждый `require('../assets/images/road/...')`
    в `GameConstants.js` с реальным списком файлов в папке (`diff` двух отсортированных списков) — после
    каждой правки ассетов совпадение 1-в-1, ни одного "битого" require. Живой прогон в браузере (см.
    вводный абзац) — подтверждены измерением DOM: точный квадрат 71×71px на всех 144 клетках, каждый
    тип тянет ассет только из своей группы (полный список `distinctSrcs` сверен глазами), подсветка —
    именно тонкая рамка+заливка нужного цвета, поворот/прозрачность — по типу и раскладке именно так,
    как просил пользователь (в частности перепроверено, что после сужения списка типов для поворота
    `wall_base_*`/`danger_base_*`/`black_hole_*` вернулись к `transform: ""`, а `road_base`/`sand_base_*`/
    `dirt_base_1` остались повёрнуты). Реальной партии на бэке не было — если после следующего живого
    теста на устройстве пользователь увидит расхождение, начинать разбор с `BoardGrid.js` (там
    сосредоточена вся эта правка), не с `useBoardLayout`/`lib/board.js` (те тронуты только по геометрии
    квадрата и выбору варианта, оба уже перепроверены отдельно).

- 2026-08-29 (продолжение того же дня): sci-fi рамка + кнопки навигации для мобильного приложения
  (`assets/images/ui/mobile/` — `frame.png`, `road_map_button_up/down.png`, добавлены пользователем).
  По прямому уточнению пользователя ("думаю это пока для мобильного приложения сделать, не совсем
  понимаю, как это будет выглядеть для браузера") — вся эта фича гейтится `Platform.OS !== 'web'` (И
  портретной раскладкой) через `showMobileFrame` в `GameBoardScreen.js`, на вебе/альбомной раскладке
  ничего не меняется — старая круглая `ArrowButton`, без рамки.
  - **Новые компоненты**: `MobileFrameOverlay.js` — декоративная рамка, растёт НАРУЖУ вокруг уже
    готового блока (RoadArea/PlayerInfoPanel), а НЕ сжимает контент внутрь себя. Осознанный выбор:
    альтернатива (вписать контент ВНУТРЬ рамки через inset-паддинг) потребовала бы уменьшать
    `roadContainerW/H`/`segmentSize`, а это трогает `useBoardLayout` — геометрию, на которую ушло
    огромное количество отдельных заходов (13-21, см. выше) и которую страшно ломать полурабочей
    правкой без возможности живого теста на устройстве. Overlay-подход — ноль изменений в
    BoardGrid/PlayerInfoPanel/useBoardLayout, чисто декоративный слой поверх (`pointerEvents:'none'`).
    `RoadNavButton.js` — замена круглой `ArrowButton` на ассет пользователя (рамка+глиф уже в
    картинке), анимация нажатия — `Animated.spring` на scale (0.88→1), тот же паттерн, что уже
    доказанно работает в проекте (DiceDie/ParallaxBackground).
  - **Математика рамки**: у `frame.png` (900×900) непрозрачная кайма занимает РАЗНУЮ долю с каждой
    стороны — измерено скриптом (`jimp-compact`, скан альфа-канала по центральным осям с проверкой на
    "устойчивую" прозрachность, чтобы не поймать декоративный паз-заглушку у кромки): top 6.7%,
    bottom 9.3%, left 8.3%, right 8.4% (`MOBILE_FRAME_INSET` в `GameConstants.js`). Чтобы прозрачное
    окно рамки ПОСЛЕ растяжения (`resizeMode:'stretch'`) село ровно на contentWidth×contentHeight,
    сама рамка должна быть больше на `outset` с каждой стороны, где `outset` — доля НЕ от контента, а
    от итогового размера рамки (`frameSize = content + outsetA + outsetB`) — решается системой
    `sum = outsetA+outsetB = (ratioA+ratioB)*(content+sum)`. Проверено изолированным Node-скриптом
    (не только чтением кода) — `frameSize*(1-ratioA-ratioB) === content` сходится с точностью до
    floating-point.
  - **Реальный баг, пойманный живым прогоном**: react-native-web у `<Image>` ВСЕГДА выставляет явные
    `width`/`height` на обёртку (подтверждено чтением computed style в браузере) — CSS-приём "ширина
    через противоположные `left`/`right` без `width`" НЕ работает, откатывается на интринсик-размер
    самой картинки (900×900) независимо от `top/left/right/bottom`. Первая версия `MobileFrameOverlay`
    полагалась именно на этот приём и рендерила рамку в неправильном (нативном) размере — исправлено:
    считаем `frameWidth/frameHeight` явно и задаём их напрямую в style, `top`/`left` — только для
    позиции (без `right`/`bottom`).
  - **Кнопка лога специально не трогал**: `EventLogPanel` (portrait, `position='top'`) уже сидит на
    `insets.top + spacing.sm` — это и есть примерно верхняя кромка рамки дороги, так что по факту она
    уже "на рамке" без доп. правок; координаты не перепроверял пиксель-в-пиксель (нет живого
    устройства), если будет заметно мимо — поправить `wrapperTop` в `EventLogPanel.js`.
  - **Живой прогон**: бэка нет (Docker недоступен), плюс сама фича на вебе не включается по гейту —
    временно форсировал `MobileFrameOverlay`/`RoadNavButton` рендериться в веб-браузере через отдельный
    `__BoardPreview.js` (как в прошлых заходах — подменял `AuthScreen`, удалил после), чтобы
    провизуализировать именно ГЕОМЕТРИЮ (она платформо-независима, гейт `Platform.OS` — только в
    `GameBoardScreen`, не в самих компонентах). Подтверждено: рамка растёт наружу без обрезки (родитель
    `roadFrameWrap`/`panelFrameWrap` — `position:'relative'` БЕЗ `overflow:'hidden'`), оба ассета
    (`frame.png` дважды — дорога/панель, `road_map_button_up/down.png`) грузятся без 404/broken image,
    консоль браузера чистая. Анимацию нажатия (scale) НЕ удалось подтвердить синтетическими
    `PointerEvent` через `javascript_tool` — RN Web's Pressability не реагирует на программно
    отправленные события так же надёжно, как на реальный ввод (тот же класс проблемы, что был с
    `react-native-gesture-handler` в 20-м заходе, см. выше) — код-ревью подтверждает корректность
    (идентичный паттерн `Animated.spring` уже проверен живьём в других местах проекта), но сам
    scale-эффект на реальном экране не видел. **Не проверено на реальном мобильном устройстве вообще**
    (ни рамка, ни кнопки, ни анимация) — это самая рискованная часть захода, нужен скриншот/тест
    пользователя с телефона прежде чем считать фичу законченной.
  - Верификация: `@babel/core` (`babel-preset-expo`, web И android caller) по всем новым/изменённым
    файлам — чисто.

- 2026-08-29 (тот же день, продолжение): первый в этом проекте живой прогон через РЕАЛЬНЫЙ Android-
  эмулятор из сессии (не веб-имитацию) — на хосте оказался уже запущенный `emulator-5554` (`adb devices`)
  и локальный докер-бэк (`docker ps` — контейнеры были подняты 12+ часов, не мной). `npm run android`
  (`expo run:android`) собрал и поставил debug-APK за ~9 сек (почти всё UP-TO-DATE), дальше правки JS
  ловились через Metro. **Важная методологическая находка**: `adb shell am force-stop` + `monkey`-релонч
  НЕ гарантирует свежий бандл — Metro в логе показывал `Android Bundled Nms index.js (1 module)` вместо
  полного `(1408 modules)`, то есть тихо отдавал Fast Refresh/инкрементальный патч поверх старого JS
  (тот же класс проблемы, что уже описан в 2026-08-16 про ложные срабатывания). Единственный надёжный
  способ гарантировать реально свежий код — `adb shell pm clear <package>` (полный сброс данных
  приложения, включая закэшированный бандл) перед каждым релончем; после него — логин заново.
  - **Живой прогон вскрыл РЕАЛЬНЫЕ баги первой версии MobileFrameOverlay** (из записи выше, где всё было
    "не проверено на реальном устройстве"), которых веб-имитация не поймала:
    1. Рамка дороги и рамка панели **налезали друг на друга** — `roadZonePortrait` и панель стояли
       впритык (0px), а рамка растилась НАРУЖУ на измеренную толщину каймы без всякого знания о соседях.
    2. Кнопки навигации оказывались **визуально перекрыты** выросшей рамкой — Android не изолирует
       порядок отрисовки по границам компонентного дерева (тот же класс бага, что уже был с токеном
       бегуна под сегментом, пятый заход, 2026-08-12).
    3. **Перетаскивание кубиков перестало работать** — та же перекройка рамки заезжала на область
       кубиков/карточек в панели.
    4. Диагональный скос угла `frame.png` при растяжении квадратного ассета на узкую высокую зону
       размазывался в длинную кривую полосу почти на весь левый край экрана.
  - Первый раунд фиксов (по месту, всё ещё в рамках "рамка нужна"): убрал outward-growth (рамка теперь
    заполняет РОВНО свою зону, не больше — три исходных бага исчезли структурно), сделал 9-slice
    (нарезал `frame.png` скриптом на jimp-compact на 4 угла + 4 грани, углы без растяжения, грани тянутся
    только вдоль своей длинной оси — убрал диагональный артефакт), толщину рамки сделал равной
    `arrowBtnSize` (по прямому запросу — рамка была "слишком большая"), добавил `ROAD_PANEL_GAP` (10dp) в
    `useBoardLayout` между дорогой и панелью (иначе рамки визуально сливались по бокам даже без
    outward-growth — фикс с картинки, отмеченной пользователем красными кружками). По пути поймал ещё
    один реальный баг живьём: углы рендерились с `resizeMode` по умолчанию `'cover'`, который у
    НЕквадратного исходника (75×60 и т.п.) обрезал контент вкривь — исправлено на `'stretch'`.
  - Точное измерение через `adb exec-out screencap`+`jimp-compact` (пиксельный анализ, не на глаз)
    показало: даже после фикса "толщина=`arrowBtnSize`" реальная отрисованная толщина рамки заметно НЕ
    совпадала с кнопкой (~50px рамка vs ~75-79px кнопка на одном и том же скриншоте) — не успел
    диагностировать причину до того, как пользователь принял решение остановиться.
  - **Пользователь решил откатить всю рамку целиком, оставив кнопки навигации** ("ладно, откати все
    изменения по попыткам вставить рамку, но кнопки оставь") — после нескольких раундов правок стало
    ясно, что рамка на имеющихся ассетах (единственный квадратный `frame.png` без честного 9-patch от
    дизайнера) не даётся малой кровью. Сделано: `MobileFrameOverlay.js` и нарезанные
    `assets/images/ui/mobile/frame_slices/*` удалены; `GameBoardScreen.js` — убраны `roadFrameWrap`/
    `panelFrameWrap`-обёртки, `navBtnOverlap` (отрицательные отступы под кнопки), сама рамка;
    `showMobileFrame` переименован в `useMobileNavButtons` (условие то же — портрет+native, но теперь
    буквально про кнопки, не про рамку); `useBoardLayout.js` — `ROAD_PANEL_GAP` убран, `boardBudgetH`
    вернулся к исходной формуле (файл после отката побайтово идентичен `git diff` — подтверждено
    `git diff --stat` пустым выводом). `RoadNavButton.js` (кнопки-стрелки на ассете пользователя,
    press-анимация) — ОСТАВЛЕН как есть, работает независимо от рамки. `frame.png` остался в
    `assets/images/ui/mobile/` (загружен пользователем) просто больше нигде не используется в коде.
  - Заодно на этом же живом прогоне подтверждено (случайно, тестируя рамку): **drag-and-drop кубика на
    карточку бегуна реально работает на настоящем Android** — впервые в истории проекта подтверждено НЕ
    имитацией/чтением кода, а настоящим `adb shell input swipe` (1200ms, имитирует медленный зажатый
    жест) от кубика к карточке «Джаггернаут»: кубик встал, карточка получила `pendingSelect`-рамку,
    баннер хода показал «Джаггернаут» выбран — подтверди/отмени». Значит история сомнений про
    android-драг (18-21 заходы) закрыта: drag на native работает штатно через `measureInWindow`+
    `translationX/Y`, никаких доп. правок не потребовалось.
  - Финальный живой прогон (после отката, `pm clear`+релогин) подтверждён скриншотом: рамки нет вообще,
    кнопки навигации (ассет пользователя, анимация нажатия) на месте и работают, раскладка дороги/панели
    вернулась к исходному виду без зазоров/искажений.
  - Верификация: `@babel/core` по всем изменённым файлам — чисто; `git diff --stat` подтвердил, что
    `useBoardLayout.js` вернулся байт-в-байт к состоянию до этого захода. Живой прогон — на реальном
    Android-эмуляторе (не веб-имитация), несколько полных циклов pm clear→логин→скриншот.

- 2026-08-29 (тот же день, третий заход): рамку вернули — пользователь подготовил ПРАВИЛЬНЫЙ набор
  ассетов вместо одного квадратного `frame.png` (см. запись выше про откат): `frame_corner.png`
  (верхний правый угол, 200×200), `frame_gorizont.png` (верхняя горизонтальная планка, 121×47),
  `frame_vert.png` (правая вертикальная планка, 49×175) — измерено скриптом (jimp-compact): толщина
  каймы угла СОВПАДАЕТ с толщиной планок (47px сверху = высота гор. планки, 49px справа = ширина
  верт. планки) — швы сходятся без подгонки. `MobileFrameOverlay.js` пересобран с нуля: 4 угла и обе
  стороны обеих планок получены зеркалированием (`scaleX:-1`/`scaleY:-1`) ОДНОГО угла и ОДНОЙ пары
  планок — отдельных файлов под остальные 6 кусков не нужно. Несколько раундов живых фиксов на
  реальном Android-эмуляторе (снова `emulator-5554`+локальный докер-бэк, `pm clear`+релогин
  `frametest1`/`frametest2` перед каждой проверкой — см. методологическую заметку в записи про откат
  выше, force-stop без pm clear ненадёжен):
  - **Угол просвечивал искажённым** — забыл `resizeMode` на угловых `Image`, RN дефолтил на `'cover'`,
    который у НЕквадратного паддинга обрезал контент вкривь. Добавлен `resizeMode="stretch"` — угол не
    квадратный по факту (чуть-чуть), но искажение уже незаметное, не как с прежним `frame.png`.
  - **Планка рендерилась в своём нативном размере (121px) вместо растяжения на весь зазор** —
    `left`+`right` без явного `width` на Image не работает НАДЁЖНО не только на react-native-web (уже
    ловил раньше), но и на native Android — тот же баг, другая платформа. Фикс — `onLayout` на
    внешнем View меряет реальный размер родителя, `width`/`height` планок считаются явным числом от
    этого измерения, не от `left`/`right`.
  - **Анимированные gif-тайлы опасности на доске пролезали ПОВЕРХ рамки** несмотря на то, что рамка
    объявлена позже в дереве — тот же класс бага, что уже был с токеном бегуна под сегментом (пятый
    заход, 2026-08-12): Android не всегда уважает порядок объявления для elevated контента глубоко во
    вложенном дереве. Фикс — явный `zIndex:10, elevation:10` на рамке (выше, чем `tokenOverlayLayer`
    у BoardGrid, там максимум 2).
  - **Пользователь: "не надо было объединять рамки в одну вертикальную планку"** — первая версия убирала
    скруглённые углы на шве дороги/панели (`corners` prop у `MobileFrameOverlay`, чтобы избежать
    просвета фона между двумя выпуклыми кривыми) — пользователю не понравилось, вернули округление на
    все 4 угла ОБЕИХ рамок (`corners` prop остался в компоненте, просто не используется сейчас — на
    случай, если понадобится снова).
  - **Верхний край рамки не доставал до истинного верха экрана (проступал под статус-баром, а не за
    ним)** — добавлен `bleed` prop (сколько px рамка растёт НАРУЖУ за родителя, только с ИСТИННЫХ
    внешних краёв — верх/лево/право у дороги, низ/лево/право у панели, НИКОГДА на внутреннем шве между
    ними). Реализовано через отрицательные `top/left/right/bottom` на внешнем View — `onLayout` меряет
    уже УВЕЛИЧЕННЫЙ размер, доп. пересчёта не требуется. В отличие от прошлой (откаченной) версии
    outward-growth, здесь bleed идёт ТОЛЬКО на явно перечисленных сторонах, не на всех 4 сразу — не
    повторяет баг "рамки налезают друг на друга".
  - **Кнопки навигации — самая дорогая часть по числу итераций.** Три попытки: (1) отрицательный
    `margin` тянет кнопку "на рамку" — визуально ок, НО margin не просто двигает элемент, а ещё
    "освобождает" его место в расчёте flex-высоты соседей (Yoga), из-за чего `roadFrameWrap` получал
    БОЛЬШЕ места, чем реально резервирует `useBoardLayout` под доску — в этом лишнем пространстве
    появлялась дыра-зазор между рамками (жалоба пользователя, со скриншотом). (2) `transform:
    translateY` вместо margin — чисто визуальный сдвиг, НЕ должен влиять на layout, но пользователь
    сообщил регресс на реальном устройстве ("стало только хуже") — не смог отличить живьём, было ли
    дело в transform+elevation на Android или в том, что verification зацепил не до конца
    пересобранный билд (см. методологическую заметку про `pm clear`). (3) Финально — убрал смещение
    ВООБЩЕ и **перенёс верхнюю кнопку вниз, рядом с нижней** (по прямому запросу пользователя, который
    сам это предложил, когда увидел, что зазор между рамками ровно по размеру кнопки) — оба нав-кнопки
    теперь в одном `flexDirection:'row'` ряду в нижнем слоте. Верхний слот стал ПУСТЫМ распорным `View`
    той же высоты (`arrowBtnSize`) — `useBoardLayout` не трогали вообще (`git diff --stat` пустой),
    место всё ещё зарезервировано под старую геометрию, просто там больше ничего не рисуется, и
    `bleed.top` увеличен до `insets.top + arrowBtnSize + MOBILE_FRAME_BLEED` — рамка перекрывает весь
    этот пустой слот и достаёт до истинного верха БЕЗ риска перекрыть настоящую кнопку (её там больше
    нет). Это одновременно решило и "верх рамки не у края", и "дыра между рамками с кнопкой внутри" —
    один структурный фикс на две жалобы.
  - **Известный остаточный дефект (пользователь считает несущественным)**: на стыке угла с
    вертикальной планкой видно смещение в пару пикселей — не нашли причину (позиционирование
    математически завязано на один и тот же `cornerSize` для обоих кусков, по коду расхождения быть не
    должно), скорее всего сам ассет угла не идеально квадратный по факту отрисовки. Не чинили — если
    возвращаться, начинать с точного пиксельного сравнения `frame_corner.png` и `frame_vert.png` в месте
    стыка (jimp-compact, как измеряли толщину каймы).
  - Финальное подтверждённое состояние (живой скриншот, `pm clear`+релогин, `@babel/core` чисто по
    всем изменённым файлам): верх рамки дороги — вплотную к истинному краю экрана (за статус-баром),
    шов дорога/панель — без зазора, обе кнопки навигации в одном ряду в этом шве, углы округлены на
    обеих рамках. `useBoardLayout.js` не тронут (полностью сохранил исходную геометрию).
  - **Отложено на следующий заход (пользователь подтвердил переносить отдельно)**: статичная
    прокрутка — вместо анимированного сдвига сетки (`translateX`/`translateY` в `BoardGrid`/
    `useBoardScroll`) показывать ФИКСИРОВАННУЮ сетку 6×8 сегментов, где кнопки вверх/вниз (уже стоят
    рядом внизу, см. выше) мгновенно ПОДМЕНЯЮТ содержимое ячеек на данные следующего/предыдущего
    фрагмента трассы, а не двигают камеру. Это переделка `BoardGrid.js` (убрать `column-reverse`/
    `translateX`/`translateY` анимацию) и `useBoardScroll.js` (заменить непрерывный/дискретный скролл
    на просто "текущий индекс блока" + мгновенную смену), НЕ трогая `useBoardLayout.js` (геометрия
    ячеек не меняется). Подтверждённое понимание пользователя: "да, должна быть мгновенная подмена".

- 2026-08-30: статичная прокрутка реализована и подтверждена живьём на Android-эмуляторе — задел из
  записи выше выполнен.
  - **`useBoardScroll.js` переписан с нуля**: вместо `Animated.Value`+`PanResponder`+`useScrollAnimation`
    (веб) теперь простой `useState(0)` для `blockIndex` (0..`totalBlocks`-1) — `backButtonProps`/
    `forwardButtonProps` просто клэмпят `setBlockIndex` на ±1, без анимации вообще. Сигнатура хука
    сократилась до `{ totalBlocks }` (убраны `minOffset`/`segmentSize`/`axis`/`cols`/`webScrollSpeed` —
    больше не нужны без непрерывного/дискретного скролла). `hooks/useScrollAnimation.js` (веб-зажатие
    стрелки) удалён целиком — единственный потребитель. `BOARD_LAYOUT.WEB_SCROLL_SPEED` тоже удалён
    (был нужен только для него).
  - **`BoardGrid.js` переписан**: `gridData` теперь фильтруется по `blockIndex` (`cell.blockIndex ===
    blockIndex`) — рендерится ровно один фрагмент трассы (`rows`×`cols`, обычно 6×8), а не все
    `TOTAL_COLS` колонок сразу. Убран внешний `Animated.View`+`transform`/`offset`, убран
    `containerHandlers` (PanResponder драга доски — весь жест целиком не нужен, кнопки — единственный
    способ навигации теперь). `column-reverse` для портретной раскладки ("снизу вверх") ОСТАВЛЕН — это
    чисто layout-приём для порядка укладки внутри ОДНОГО блока, не связан с анимацией/скроллом.
    Токен-оверлей и его пиксельные координаты (`tokenOverlay`) тоже переведены на локальные координаты
    одного блока (`localCol`, не `globalCol`/`TOTAL_COLS`) и отфильтрованы по `runner.segment ===
    blockIndex` — бегуны из других фрагментов физически не отрисовываются, пока пользователь не
    переключится на их блок кнопкой.
  - **`GameBoardScreen.js`**: `useBoardScroll({ minOffset, segmentSize, axis, cols, totalBlocks,
    webScrollSpeed })` → `useBoardScroll({ totalBlocks })`, деструктурит `blockIndex` вместо
    `offset`/`containerHandlers`, `<BoardGrid>` получает `blockIndex` вместо `offset`/
    `containerHandlers`. `minOffset` из `useBoardLayout()` (сам хук НЕ трогал, как и договаривались) в
    этом экране теперь просто не используется — убран из деструктуризации.
  - **Живой прогон на реальном Android-эмуляторе** (`emulator-5554`+локальный докер-бэк, `pm clear`+
    релогин `frametest1`/`frametest2` — застал уже готовую активную партию с прошлой сессии, не пришлось
    заводить новую) — цикл проверки: скриншот блока 0 → тап "вперёд" → скриншот блока 1 (контент
    мгновенно другой, сетка/рамка/кнопки не сдвинулись ни на пиксель) → тап "вперёд" → блок 2 → тап
    "вперёд" ещё раз на границе (`blockIndex` уже 2, `totalBlocks`=3) → скриншот идентичен предыдущему
    (клэмп работает, не падает) → 3× тап "назад" → блок 1 → блок 0 → тап "назад" на границе (`blockIndex`
    уже 0) → скриншот идентичен блоку 0 (клэмп в обе стороны подтверждён). `adb logcat` за весь прогон —
    ни одной JS-ошибки/warning. Кнопки/рамка/панель/кубики — без регрессий, всё как было до этого захода.
  - Верификация: `@babel/core` (`babel-preset-expo`, web И android caller) по всем изменённым файлам —
    чисто. Живой прогон — см. выше, самый полный тест именно этой фичи (не статика, а настоящее
    устройство, оба граничных случая явно проверены скриншотами).

- 2026-08-30 (тот же день, продолжение): пользователь уточнил — прыжок сразу на весь фрагмент (8
  колонок) за одно нажатие слишком грубый, попросил посегментный шаг (1 колонка за тап) с повтором
  каждые 0.5с при удержании.
  - **`useBoardScroll.js` переписан второй раз за день**: `blockIndex` (0..`totalBlocks`-1) заменён на
    `windowStart` — глобальный индекс (0..`TOTAL_COLS-cols`, т.е. 0..16 при `TOTAL_COLS`=24/`cols`=8)
    ЛЕВОГО/НИЖНЕГО края видимого окна из `cols` подряд идущих колонок, БЕЗ привязки к границам блока.
    `backButtonProps`/`forwardButtonProps` теперь несут `onPressIn`/`onPressOut` вместо `onPress`:
    `onPressIn` сразу делает 1 шаг (`setWindowStart` ±1, клэмп) И запускает `setInterval` на 500мс,
    `onPressOut` его останавливает (`clearInterval`) — один и тот же механизм для обеих раскладок и
    платформ, никакого Platform-ветвления не потребовалось (в отличие от старого до-08-30 кода, где
    веб и мобильные вели себя по-разному). `totalBlocks` в сигнатуре хука заменён на `cols` (нужен для
    расчёта `maxStart`); константа `TOTAL_COLS` берётся из `BOARD_LAYOUT` напрямую внутри хука.
  - **`BoardGrid.js`**: `blockIndex`-фильтр (`cell.blockIndex === blockIndex`) заменён на диапазонный
    (`cell.col >= windowStart && cell.col < windowStart + cols`) — `cell.col` в gridData УЖЕ глобальный
    столбец (`col + blockIndex*cols`, см. `lib/board#flattenTrackSegments`), отдельно пересчитывать
    ничего не нужно. Токен-оверлей аналогично: `globalCol = segment*cols + positionX` (segment/positionX
    — из ключа `runnersByCell`, оба локальные величины) сравнивается с окном напрямую. Порядок отрисовки
    внутри дорожки (`column-reverse` для портрета) не тронут — это тот же приём, просто теперь скользит
    по произвольному диапазону колонок, а не только по границам блока.
  - **`RoadNavButton.js` (реальный баг, пойман РАНЬШЕ, чем попал к пользователю)**: раньше
    `{...handlers}` спредился ПОСЛЕ собственных `onPressIn`/`onPressOut` компонента (анимация
    нажатия — уменьшение scale) — пока `handlers` нёс только `onPress` (старая семантика), конфликта не
    было. С переходом на `onPressIn`/`onPressOut` в useBoardScroll спред просто ЗАТЁР бы анимацию нажатия
    полностью (осталась бы только логика шага, без scale-эффекта). Исправлено — оба обработчика
    компонуются явно (`animateTo(...); handlers?.onPressIn?.(e)`), а не через слепой спред пропсов.
    `ArrowButton.js` (веб/альбомная раскладка) трогать не пришлось — там нет собственных
    onPressIn/onPressOut, `{...handlers}` и так работает с новым контрактом как есть.
  - **Живой прогон на Android-эмуляторе с временным `console.log` в `step()`** (добавлен на время
    диагностики, убран после — итоговый файл его не содержит): подтверждено, что нажатие/удержание даёт
    именно задуманный паттерн — немедленный первый шаг на onPressIn, дальше шаг РОВНО раз в ~500-520мс,
    пока держится, честный `clearInterval` на onPressOut без "убегающего" таймера. Единственная
    странность прогона — при повторном тесте лог начался заново с `windowStart -> 1` вместо продолжения
    с прошлого значения; не стал глубоко копать (не нашёл recycle/remount в логах, но и не нашёл
    объяснения) — по имеющимся в проекте заметкам (см. "рабочие заметки по среде" в начале сессий) ADB-
    инъекция тача на этом эмуляторе и раньше вела себя нестабильно по таймингу (даже одиночный `input
    tap` один раз растянулся на ~3.9 реальных секунды вместо мгновенного тапа) — решил не тратить время
    на дальнейшую диагностику именно ADB-артефакта, логика кода перепроверена чтением и полностью
    соответствует ТЗ (клэмп `Math.max(0, Math.min(maxStart, s+dir))`, интервал 500мс, останов на release).
    Если пользователь на реальном устройстве заметит расхождение с ожидаемым поведением — начинать
    разбор с `useBoardScroll.js` (весь механизм сосредоточен там, `BoardGrid`/`RoadNavButton` его просто
    потребляют).
  - Верификация: `@babel/core` (`babel-preset-expo`, web И android caller) по всем изменённым файлам —
    чисто. Живой прогон на Android-эмуляторе — чистый релогин, доска рендерится без ошибок в
    `adb logcat`, посегментный шаг подтверждён по логам (не только визуально/по коду).
  - **Скорость повтора при удержании удвоена** тем же днём, отдельным маленьким запросом —
    `REPEAT_INTERVAL_MS` в `useBoardScroll.js` 500 → 250мс. Однократный тап по-прежнему двигает ровно
    на 1 сегмент (immediate step на `onPressIn` не менялся), меняется только частота повтора при
    удержании. Проверено только `@babel/core` — тривиальная правка одной константы, живой прогон не
    делал.

- 2026-08-30 (тот же день, четвёртый заход): по жалобе пользователя на пустую полосу между мобильными
  рамками (дорога/панель) — там сидели кнопки навигации вверх/вниз в собственном flex-слоте
  (`navBtnRow`, высотой `arrowBtnSize`), которого сама `MobileFrameOverlay` не закрывала (bleed только
  наружу, не в шов) — рамки визуально не соприкасались. Два раунда правок в один день:
  - **Первая версия (неверно истолкованное "сомкнём")**: убрал слот кнопок из потока (roadFrameWrap
    сам растянулся, забрав освободившуюся высоту — flex:1 в колонке без соседа автоматически съедает
    его место), кнопки стали абсолютным оверлеем ВНУТРИ roadFrameWrap (на новой свободной полосе у его
    низа), и заодно передал `corners={{bl:false,br:false}}`/`corners={{tl:false,tr:false}}` в
    `MobileFrameOverlay` дороги/панели, чтобы у шва не было двух отдельных полукруглых скруглений.
    Пользователь поправил: "сомкнём" означало ТОЛЬКО убрать зазор между рамками, они должны остаться
    двумя отдельными рамками (каждая — все 4 скруглённых угла), просто впритык. Откатил именно
    `corners`-пропы (убрал оба, вернул дефолт — все 4 угла), сама механика "рамки впритык без
    отдельного слота под кнопки" осталась, это пользователь не просил менять.
  - **Второй запрос в тот же день**: расположить кнопки вверх/вниз И кнопку лога СОБЫТИЙ в один ряд,
    отцентрированный ровно НА стыке рамок (половина ряда на дорожной рамке, половина на панельной), а
    не внутри одной из них. Реализовано:
    - Кнопки навигации убраны из `roadFrameWrap` совсем (там раньше был `navBtnOverlay`, привязанный к
      низу дорожной рамки, — удалён).
    - Новый `styles.seamRow` — `position:'absolute'`, sibling ОБЕИХ зон на уровне корневого `wrapper`
      (не внутри `roadFrameWrap`/`panelFrameWrap`), `bottom: panelH - arrowBtnSize/2` с явной
      `height: arrowBtnSize` — так как `panelFrameWrap` фиксированной высоты `panelH` и прижата к
      самому низу экрана (последний child колонки), шов от низа экрана = ровно `panelH`, а `bottom`
      абсолютного элемента задаёт положение его НИЖНЕГО края — вычитание половины высоты ряда даёт его
      ЦЕНТР ровно на шве. `zIndex/elevation: 30` — выше рамок (10) и выше обычных
      `EventLogPanel`-плашек в других раскладках (25).
    - `EventLogPanel.js` — новый вариант `position="seam"`: тоггл рендерится БЕЗ собственного
      `position:'absolute'` wrapper'а (обычный inline-элемент, кладётся вызывающим кодом внутрь
      `seamRow` вместе с кнопками), список при раскрытии — `seamListWrap`, абсолютный дропдаун
      `bottom:'100%'` НАД тогглом (не раздувает высоту ряда), центрированный под ним через
      `left:'50%'` + `transform:[{translateX:-150}]` (половина фиксированной ширины панели 300 — не
      привязано к реальной ширине тоггла, которая заранее не известна без onLayout).
    - Применяется ТОЛЬКО при `useMobileNavButtons` (мобильная рамка, портрет, native) — на вебе/
      альбомной раскладке `EventLogPanel` рендерится по-старому (`position={isPortrait?'top':
      'bottom-right'}`), кнопки — прежние `ArrowButton` в потоке, эта фича их не касается.
  - Живой прогон на Android-эмуляторе (`pm clear`+релогин `frametest1`) после каждого раунда правок —
    подтверждено скриншотами: рамки без зазора, каждая с 4 скруглёнными углами (после отката corners),
    финально — единый ряд (▲/▼/лог) сидит точно на стыке, наполовину на каждой рамке. Пользователь
    подтвердил результат живьём как раз в процессе финальной пиксельной проверки ("вижу результат,
    получается круто, как я и хотел") — сам не успел закончить пиксель-точный `jimp-compact`-замер,
    но раз подтверждено визуально самим пользователем на реальном экране — этого достаточно.
  - Верификация: `@babel/core` (`babel-preset-expo`, web И android caller) по `GameBoardScreen.js`/
    `EventLogPanel.js` — чисто на каждом шаге. Живой прогон — см. выше, включая подтверждение
    пользователем финального результата.

- 2026-08-30 (тот же день, пятый заход): скорость посегментной прокрутки, обрезка "кирпичных"
  сегментов (портрет И альбомная веб-раскладка), консолидация кнопок навигации в вебе, и полная
  переверстка карточки бегуна + цветовая консистентность игрока — насыщенный заход с быстрым
  туда-обратно фидбеком пользователя (смотрел на живой Fast Refresh параллельно с моими правками).
  - **Скорость удержания для прокрутки удвоена**: `REPEAT_INTERVAL_MS` в `useBoardScroll.js` 500→250мс
    (однократный тап по-прежнему двигает на 1 сегмент, меняется только частота повтора при удержании).
  - **Настоящая причина "верхние сегменты дороги обрезаны частично"** (жалоба со скриншотом, сначала
    Android, потом подтверждено и в вебе) — НЕ про общий недостаток вертикального бюджета, а именно
    про "кирпичный" сдвиг нечётных дорожек (`marginTop`/`marginLeft` на segmentH/2 или segmentW/2 в
    `BoardGrid.js`): реальная высота/ширина контента для СДВИНУТЫХ дорожек — `COLS*segment + segment/2`,
    а не `COLS*segment` — без этого запаса `overflow:'hidden'` срезал ровно половину последнего
    сегмента у сдвинутых дорожек (у несдвинутых обрезки не было/была не так заметна). Симметрично
    почищено в `useBoardLayout.js`:
    - Портрет: `segmentSize = floor(min(boardBudgetW/ROWS, boardBudgetH/(COLS+0.5)))`,
      `roadContainerH = segmentH*COLS + segmentH/2` (было `boardBudgetH` целиком, без учёта сдвига).
    - Landscape (веб, тот же баг на горизонтальной оси — подтверждено скриншотом пользователя с
      обведённым обрезанным правым краем): `segmentSize = floor(min(roadBudgetW/(COLS+0.5),
      roadBudgetH/ROWS))`, `roadContainerW = segmentW*COLS + segmentW/2`.
    - Мёртвый `minOffset`/`totalContentH` (утиль от дореформенного скролла, ничем больше не читается)
      убран из обеих веток `useBoardLayout.js` целиком.
    - `RoadArea.js` — `outer` получил `alignItems:'center'`: раз segmentSize теперь может упираться в
      ЛЮБУЮ из двух осей (не только в ширину, как раньше в портрете), на нелимитирующей оси появляется
      слack, и без центрирования сетка/рамка прилипали бы к левому краю вместо центра.
    - `GameBoardScreen.js` — `roadZonePortrait` лишился `alignItems:'center'` (иначе `roadFrameWrap`
      сжимался бы до ширины ЗАУЖЕННОЙ сетки вместо растяжения на весь экран, и мобильная рамка не
      доставала бы до боковых краёв) — двум ArrowButton веб-портрета возвращено центрирование adресно
      через новый `styles.selfCenter`/проп `style` у `ArrowButton.js`.
    - Верификация: `@babel/core` (обе платформы) — чисто. Живой прогон — Android-эмулятор (визуально,
      без клиппинга) И веб (`javascript_tool`, DOM-замеры `getBoundingClientRect` — screenshot
      compositing в этой сессии не поднимался, "pane hidden" — измерено: 48 ячеек = 6×8, чётные/
      нечётные дорожки сдвинуты РОВНО на segmentH/2, обе группы целиком внутри `overflow:hidden`
      контейнера без обрезки, сетка отцентрована в доступной ширине с точностью до 1px).
  - **Консолидация кнопок навигации в вебе (альбомная раскладка)** — по прямому запросу пользователя
    ("дать больше пространства под дорогу"): обе кнопки (назад/вперёд) теперь ОДНОЙ колонкой слева от
    дороги (`styles.navBtnColumn`), не по разным краям — `useBoardLayout.js` резервирует под них ОДИН
    `arrowBtnSize`, не два (`roadBudgetW = roadZoneW - arrowBtnSize - spacing*2`). Кнопки — тот же
    ассет `RoadNavButton` (ранее только для мобильной рамки), не кружочная `ArrowButton` — тоже по
    прямому запросу. `RoadNavButton.js` — добавлены направления `'left'`/`'right'`: переиспользуют
    ассеты `up`/`down` с поворотом на 90° (`right`=up+90°, `left`=down+90°, сохраняет семантику
    "up-ассет=вперёд, down-ассет=назад", уже принятую для портрета) — отдельных файлов под них нет.
    `arrowBtnSize` в альбомной ветке `useBoardLayout.js` уменьшен вдвое (0.15→0.075 от screenH, с
    ассетом-глифом кнопка стала размером с сам сегмент дороги — жалоба пользователя сразу после
    консолидации).
  - **Переверстка карточки бегуна** (`RunnerCard.js`/`RunnerDiceSlot.js`/`AbilityZone.js`/
    `AbilityZones.js`/`PlayerInfoPanel.js`) — серия мелких правок одна за одной по живому фидбеку:
    - Статус текстом ("Исправен"/"Повреждён"...) убран целиком (оба режима, compact и обычный) —
      кружки повреждений уже однозначно показывают то же самое, отдельная строка избыточна. Заодно
      убрана мёртвая `statusColor()`/импорты `RUNNER_STATUS`/`RUNNER_STATUS_LABEL` из `RunnerCard.js`.
    - Квадраты "Ход"/"Накат" — теперь РЯДОМ (row), не друг под другом (было). Иконка бегуна в compact
      увеличена (24→30).
    - `PlayerInfoPanel.compactColumns`: `leftColumnContent`/`rightColumn` получили
      `paddingHorizontal` — карточки/кубики+усиления больше не растянуты впритык к границам своих
      колонок (по запросу "уменьшить ширину" обеих зон — ширина самих колонок 2:1 не менялась, только
      внутренний инсет).
    - `AbilityZone.js` — новый `compact` проп (уже прокинут из `compactColumns`): без hint-подсказки
      или, после следующего уточнения, С короткой подсказкой БЕЗ слова "кубик" — новое поле
      `shortHint` в `PLAYER_ABILITIES` (GameConstants.js: `'1–3'`/`'6'`/`'любой'`/`'3–5'` вместо
      `'кубик 1–3'` и т.п.).
    - "—"-плейсхолдер в `AbilityZone` убран — строка значения рендерится, только когда зона реально
      занята. Галочка ('✓' для усилений с диапазоном — буст/жнец/призрак) тоже убрана следующим
      уточнением пользователя ("подсветки рамки достаточно") — число оставлено только там, где оно
      РЕАЛЬНО информативно (Лечение, `min===max`, фиксированный номинал).
    - **Цветовая консистентность игрока** (последнее уточнение того же захода): рамка выбранной
      карточки (`RunnerCard.cardSelected`), заливка занятого квадрата "Ход"/"Накат"
      (`RunnerDiceSlot.squareFilled`) и заливка занятой зоны усиления (`AbilityZone.zoneFilled`) были
      захардкожены в `colors.primary` (фиолетовый) — визуально не совпадали с реальным цветом игрока
      (кубики в трее/обводка бегунов на доске/пилюля переключателя, всё уже было в цвете игрока, напр.
      красный). Все три места переведены на проп `color` (тот же `activePlayer.color`, уже
      прокидывался вниз по цепочке `PlayerInfoPanel`→`RunnerCard`/`AbilityZones`→`AbilityZone`, только
      `RunnerDiceSlot` пришлось научить принимать `color` новым пропом) — прозрачность через тот же
      приём, что уже даёт `primaryTranslucent` в теме (`${color}b8`, hex+альфа-суффикс), не через
      именованную константу (у произвольного игрока нет заранее заданного "translucent"-варианта).
      Статические стили `cardSelected`/`zoneFilled` удалены как мёртвый код.
    - **Переименование**: `RUNNER_DISPLAY[TANK].label` "Джаггернаут"→"Атлет",
      `RUNNER_DISPLAY[ATHLETE].label` "Штурмовик"→"Солдат" (только строки отображения в
      `GameConstants.js` — внутренние ключи `RUNNER_TYPES.TANK`/`.ATHLETE`, ассеты и вся игровая логика
      не тронуты).
    - Верификация: `@babel/core` (обе платформы) по всем изменённым файлам — чисто на каждом шаге.
      Живой прогон на Android-эмуляторе (`pm clear`+релогин) — подтверждён скриншотом: имена
      переименованы, цвет карточки/квадрата "Ход"/зоны "Буст" — красный (цвет frametest1), без
      галочки, без "—" на пустых зонах, короткие подсказки без слова "кубик", `adb logcat` без
      JS-ошибок. Пользователь подтвердил результат живьём в процессе.

- 2026-08-30 (тот же день, шестой заход): "нижняя рамка (панель) стала меньше, дорога больше" — по
  жалобе пользователя со скриншотом: снизу под карточками бегунов оставалась пустая полоса фона
  (обведена на скриншоте), а дорога сверху выглядела мельче, чем могла бы. Три раунда правки одной
  константы `LAYOUT.PANEL_MAX_H`/`PANEL_HEIGHT_RATIO` (GameConstants.js) — панель снизу (portrait,
  `useBoardLayout`) считается фиксированной высотой (`panelH`), и `boardBudgetH` (реальный бюджет под
  дорогу) вычитает её целиком — чем меньше `panelH`, тем крупнее дорога, БЕЗ дополнительных правок:
  `roadContainerH`/`segmentSize` уже пересчитываются от `boardBudgetH` (см. пятый заход того же дня).
  - **Первая попытка (480/0.52 → 340/0.37)** — обмерено по реальному скриншоту (`jimp-compact`,
    сканирование колонки пикселей `x=150` на цвет карточки `colors.bgLight`): контенту левой колонки
    (карточки бегунов) нужно ~310dp вместо ~480dp, взял с запасом 340. **Оказалось СЛИШКОМ агрессивно**
    — обмер учёл только ЛЕВУЮ колонку (карточки), не правую (кубики+усиления, которая на деле требует
    БОЛЬШЕ высоты из-за заголовков "Кубики"/"Усиления" + 2×2 сетки) и не высоту самого переключателя
    игроков (`switcherAtBottom`, рендерится ПОСЛЕ `columns` в той же колонке `body`). Правая колонка
    (`rightColumn` в `PlayerInfoPanel.js`) была ГОЛЫМ `View` (не `ScrollView`, в отличие от левой,
    которая уже имела свой скролл с прошлых заходов) — когда контент не поместился, он не
    заскроллился, а просто визуально вылез за пределы своей колонки и НАЛОЖИЛСЯ на переключатель
    игроков под ней (жалоба пользователя со скриншотом — красный кружок игрока и "frametest1" поверх
    зон "Жнец"/"Призрак"). Отдельная жалоба — "Скаут" (последняя карточка левой колонки) тоже обрезан.
  - **Структурный фикс (не зависит от точной цифры panelH)**: `rightColumn` переведён на `ScrollView`
    (новый `rightColumnContent` style, тот же `remeasureTick`-паттерн — `onScrollEndDrag`/
    `onMomentumScrollEnd` → `bumpRemeasure`, что уже было у `leftColumn`) — теперь избыток контента
    правой колонки на ЛЮБОМ экране просто скроллится, а не накладывается на соседей. Это устраняет
    САМ КЛАСС бага независимо от того, насколько точно подобрана высота панели.
  - **Вторая попытка (340/0.37 → 420/0.45)** — с запасом на обе колонки+переключатель. Пользователь:
    "слишком сильно поднял наверх, теперь опять пустота" — перелёт в другую сторону.
  - **Третья попытка (420/0.45 → 375/0.4)** — компромисс между первой и второй. Пользователь
    подтвердил результат живьём ("во, теперь как надо" / "всё круто") — без обрезки/наложения и без
    заметной пустоты. Финальные константы: `PANEL_MIN_H: 260` (не менялся), `PANEL_MAX_H: 375`,
    `PANEL_HEIGHT_RATIO: 0.4`.
  - Ряд кнопок навигации/лога на стыке рамок (`seamRow`, см. пятый заход) НЕ потребовал отдельных
    правок — его позиция (`bottom: panelH - arrowBtnSize/2`) уже считается ОТ `panelH` динамически,
    поэтому автоматически отслеживала новый стык на каждой из трёх итераций, как и просил пользователь
    ("не забудь, что кнопки должны располагаться на изменённом месте стыка рамок").
  - Верификация: `@babel/core` (обе платформы) по `GameConstants.js`/`PlayerInfoPanel.js` — чисто на
    каждом шаге. Живой прогон — Android-эмулятор (`pm clear`+релогин, скриншот на каждой итерации,
    `adb logcat` без JS-ошибок) ПАРАЛЛЕЛЬНО с собственным Fast Refresh пользователя на реальном
    устройстве — обратная связь приходила от пользователя быстрее, чем успевал перезапускаться
    эмулятор, финальное состояние подтверждено на обоих.

- 2026-08-31: два больших куска работы за один длинный заход — доводка портретной геометрии дороги
  (фрагменты) и с нуля анимации Скаута (первый тип бегуна с реальными спрайтами). Плюс важная находка
  про хекс-геометрию движения, оставленная НЕ починенной по просьбе пользователя.
  - **Портретная геометрия дороги, продолжение вчерашнего**: `useBoardLayout` — сетка раньше считалась
    `min(ширина,высота)`, из-за чего высота почти всегда лимитировала и по бокам сетки оставались пустые
    поля (жалоба пользователя, обведено на скриншоте). Теперь размер клетки — ТОЛЬКО от ширины (сетка
    занимает её целиком), а число реально видимых КОЛОНОК трассы (`viewportCols`, отдельная величина от
    фиксированных 8 колонок одного фрагмента данных, `cols`) подстраивается под остаток по высоте — можно,
    т.к. прокрутка и так посегментная, не обязательно показывать целый фрагмент разом. Освободившийся слева
    зазор отдан под новый `FragmentLabelStrip.js` — полоса с именем текущего фрагмента(ов), текст развёрнут
    на 90°. Если окно прокрутки пересекает стык двух фрагментов — полоса делится на пропорциональные секции
    (`lib/board#computeFragmentBands`) СВОИМ сплошным цветом на каждую (`FRAGMENT_COLORS` — неон
    красный/оранжевый/зелёный, после того как первая версия палитры показалась пользователю "кислотной").
    На самой сетке — то же самое как непрерывная "змейка" (`BoardGrid#fragmentBoundarySegments`):
    горизонтальный отрезок на каждой дорожке + отрезок-перемычка до соседней (компенсирует кирпичный сдвиг)
    — раньше были несвязанные отрезки на разной высоте, жалоба "хочу единую линию". По пути дважды словил и
    починил офф-бай-один: (1) линия стыка сначала рисовалась на ВЕРХНЕМ крае клетки в портрете — из-за
    `column-reverse` верхний край граничит со СЛЕДУЮЩИМ (большим col), а не предыдущим — стык казался между
    9 и 10 сегментом вместо 8 и 9, поймано и подтверждено пользователем на конкретных числах, поправлено на
    нижний край; (2) декоративная рамка (`MobileFrameOverlay`) визуально перекрывала правый край сетки —
    её кайма (`arrowBtnSize` толщиной, `MOBILE_FRAME_BLEED` наружу) заходила глубже вовнутрь, чем сетка
    оставляла пустого места — добавлен `frameEdgeClearance` в `useBoardLayout`, сетка теперь чуть мельче,
    чтобы не прятаться под рамкой.
  - **Подложка под клетки danger/anomaly/wall/mud**: по прямому запросу пользователя — `road` под
    danger/anomaly/wall, случайный (детерминированно по id клетки) `sand` под mud/dirt
    (`lib/board#pickBaseImage`, `cell.baseImage`), сама картинка типа клетки поверх стала прозрачнее
    (`CELL_OPACITY`, 0.9→0.65 суммарно за 2 итерации), чтобы подложка просвечивала.
  - **Анимации Скаута** — пользователь сам добавил ассеты в `assets/images/runners/scout/{healthy,damaged}/`
    (idle/avatar/move×5напр./attack×5напр./got_shot/fly/collision_east|west/broken/destroyed) и подробно
    описал, что для чего: move — после реального события о перемещении, idle — стоит, avatar — только в
    плитке панели игрока, fly — неконтролируемый перелёт (аномалия/отскок после столкновения), collision —
    двое бегунов на одной клетке стоят РЯДОМ лицом друг к другу. North в названиях файлов — направление
    "вперёд" (подтверждено пользователем явно). Полная архитектура — см. новый абзац в «Ключевые
    архитектурные решения» выше (`constants/runnerAnimations.js`, `hooks/useRunnerAnimations.js`,
    `lib/runnerAnimTriggers.js`, `RunnerTokenSlide.js`). Не покрыто явными Mercure-событиями (knockback от
    столкновения) — решалось через AskUserQuestion, пользователь выбрал эвристику (сравнение позиций) как
    временное решение, а не откладывать функциональность.
  - **Живой прогон вскрыл 3 реальных бага на Android**, все исправлены и не проверены повторно самим Claude
    (пользователь тестировал на собственной сборке параллельно):
    1. Направления move/attack влево/вправо были перепутаны — в портретной раскладке дорожки идут слева
       направо по возрастанию `positionY`, а `LEFT_UP` как раз УВЕЛИЧИВАЕТ `positionY` (см.
       `hexDirection#neighborPosition`) — то есть визуально ведёт бегуна ВПРАВО, не влево.
       `GAME_DIRECTION_TO_ASSET_KEY` в `runnerAnimations.js` был `LEFT_UP→northWest` — поправлено на
       `LEFT_UP→northEast, RIGHT_UP→northWest`.
    2. В момент начала скольжения на мгновение была видна КОНЕЧНАЯ позиция токена, потом прыжок в начальную
       и только затем анимация — `RunnerTokenSlide` использовал `useEffect` (выполняется ПОСЛЕ отрисовки
       кадра, компенсирующий transform ставился на кадр позже, чем менялись left/top). Заменено на
       `useLayoutEffect`.
    3. Персонаж на мгновение пропадал в начале каждой анимации — `RunnerToken` форсировал ремонт
       `<Image>` (через смену `key`) на КАЖДЫЙ триггер анимации, даже когда `source` реально менялся на
       другой файл — полный размонт/монтаж на Android даёт заметный пустой кадр, пока новый gif
       декодируется с нуля. Теперь ремонт только если `source` буквально ТОТ ЖЕ ассет, что был (нужно
       для перезапуска повторной одинаковой анимации, напр. 2 шага UP подряд при многошаговом ходе) —
       обычная смена `source` перезапускает gif сама, без разрушения нативной view.
  - **Скорость анимаций удвоена** по прямому запросу после первого живого теста — `ANIM_DURATION_MS`
    (useRunnerAnimations) 900/900/700/1100 → 1800/1800/1400/2200, `SLIDE_DURATION_MS`
    (RunnerTokenSlide) 850→1700 (держатся в паре, чтобы скольжение и pose-анимация не расходились).
  - **Размер токена на доске** — 3 итерации по прямому запросу пользователя: сначала весь токен ×1.5
    ("почти не видно"), затем откат кольца-обводки к исходному размеру + рост ТОЛЬКО картинки внутри ещё
    на 20% (итого 0.68×1.8 от size, см. `imageScale` в архитектурном абзаце выше) — "окружность вокруг
    персонажа не увеличивай".
  - **Находка про хекс-геометрию, НЕ ПОЧИНЕНО**: пользователь заметил, что подсветка легальных клеток
    MOVE для LEFT_UP/RIGHT_UP иногда чисто боковая (тот же сегмент), а иногда с продвижением на сегмент
    вперёд — посчитал багом. Перечитал `DirectionService::run()` на бэке напрямую (не по памяти/старым
    комментариям) — подтверждено, что это ЗАЛОЖЕННОЕ бэком поведение (чётность `positionX` СТАРОЙ клетки
    определяет, диагональ это или чисто бок), и `lib/hexDirection.js` на фронте его точно зеркалит —
    сверено с пользователем на конкретных числах (1-й сегмент = чётный `positionX`=0 → бок,
    2-й = нечётный `positionX`=1 → бок+вперёд) — совпало. Это не баг фронта, а либо осознанное поведение
    бэка, либо баг бэка (у меня туда только чтение). Пользователь сказал пока не трогать — см. новый
    пункт в «Известные пробелы / TODO» выше, нужна сверка с текстом правил игры или решение
    бэкенд-разработчика.
  - **Забавная деталь сессии**: где-то в середине эмулятор/Metro/Docker Desktop все разом легли (не по
    моей вине, похоже сама машина что-то перезапускала) — пришлось поднимать все три с нуля (заново
    качать AVD `Pixel_10`, разбираться, что `expo start --android` запускает другой рантайм и рвёт
    worklets, гонять `Docker Desktop.exe`, который сам падал несколько раз, пока пользователь не поднял
    его вручную). Отдельно позже пользователь несколько раз сам поднимал свой `expo run:android`
    параллельно с моими попытками — если видишь чужой процесс на 8081, ЛУЧШЕ ПРОВЕРИТЬ владельца
    (`Get-CimInstance Win32_Process`) прежде чем его убивать или запускать свой поверх.
  - Верификация: `@babel/core` (обе платформы) по всем новым/изменённым файлам на каждом шаге — чисто.
    Живой прогон — Android-эмулятор (несколько раз, включая специально созданную через API вторую
    тестовую партию для гарантированного первого хода Скаутом) плюс параллельно собственная сборка
    пользователя на реальном устройстве, где и были пойманы 3 бага выше. Коллизионная пара
    (`collision_east/west` рядом) НЕ подтверждена визуально живьём — в тестовом прогоне бэк разрешил
    столкновение атомарно внутри одного запроса, 2 бегуна на одной клетке ни разу не оказались в
    состоянии, которое дошло бы до фронта отдельным кадром.

- 2026-08-31 (второй заход): по живой жалобе пользователя — каскад "столкновение → отскок в клетку
  опасности → вскрытие аномалии → отлёт из неё" схлопывался в одну финальную анимацию (бегун
  "телепортировался" в клетку аномалии, показывался только последний `fly`). Причина: `trigger()` в
  `useRunnerAnimations` (см. предыдущий заход) сразу перезаписывал текущую анимацию — если несколько
  Mercure-событий про ОДНОГО бегуна приходили быстрее её длительности (обычное дело для многошагового
  каскада в одном ответе `/move`), все промежуточные шаги гасились последним `trigger`.
  - **Фикс — очередь по runnerId**: `trigger()` теперь кладёт шаг в очередь конкретного бегуна вместо
    немедленной перезаписи, очередь играет по одному шагу за раз (полная `ANIM_DURATION_MS[kind]`),
    прежде чем начать следующий. Разные бегуны анимируются независимо (своя очередь на каждого).
  - **Риск, который сразу учёл, не дожидаясь живой поимки регрессии**: обычный (некаскадный) шаг
    движения и так описывается ДВУМЯ событиями подряд про одно и то же перемещение — транзиентным
    `step_move` (направление, без позиции) и версионным `runner_save` (реальная позиция) следом. До
    очереди это было безобидно (второй `trigger` просто перезаписывал первый), с очередью стало бы
    играть ОДНО перемещение дважды подряд — двойная длительность на самый частый случай в игре. Поэтому
    `step_move` метит свой шаг `extra.pending: true` ("заготовка, ждущая подтверждения позиции") —
    следующий `trigger` для того же бегуна с `toPosition` дополняет ЭТОТ шаг на месте вместо отдельного
    слота, если заготовка ещё играет/стоит в очереди. Самостоятельные шаги каскада (отскок/аномалия/
    финальный отлёт из неё) `pending` не выставляют — они синтезируются из `runner_save`/`anomaly`
    напрямую (не парные с `step_move`), поэтому всегда встают в очередь по отдельности, как и нужно.
  - **`visualPositions[runnerId]`** — вторая необходимая часть, без которой очередь была бы бессмысленна:
    реальная позиция бегуна в `game.runners` обновляется мгновенно (как и раньше, `runnerGameReducer`
    применяет ВСЕ события каскада сразу — это не трогал, игровая логика не должна ждать анимацию). Но
    `BoardGrid` теперь берёт позицию для рендера через `runnerVisualPositions` (новый проп) — пока у
    бегуна есть незавершённая очередь, доска рисует его в позиции ТЕКУЩЕГО играемого шага, а не реальной
    финальной — иначе `RunnerTokenSlide` (скольжение между клетками) заскользил бы сразу к финальной
    точке одним прыжком, минуя промежуточные "остановки" каскада, ради которых всё это затевалось.
    `BoardGrid.effectiveRunners` — новый `useMemo`, подменяет `segment/positionX/positionY` у бегунов с
    активным переопределением перед индексацией по клеткам (`indexRunnersByCell`); вся остальная игровая
    логика (`GameBoardScreen` — select/move/highlight/collision-детект) по-прежнему читает РЕАЛЬНЫЙ
    `game.runners` напрямую, не через этот слой — переопределение чисто визуальное, изолировано в
    `BoardGrid`.
  - Заодно — `runnerAnim.reset()` (новый экспорт хука: чистит очереди/таймеры/анимации/визуальные
    позиции) подключён к кнопке ручного `resync()` при зависшей коллизии (`GameBoardScreen`): REST-рефетч
    подменяет весь game-стейт целиком в обход событийного потока, который двигает очередь — без сброса
    застрявшая очередь держала бы бегуна в позиции старого, уже неактуального шага после такого рефетча.
  - **Не проверено живьём** (нет доступа к Android-эмулятору/докер-бэку в этой сессии) — правка сделана
    по чтению кода и разбору конкретного живого бага, который описал пользователь текстом (не
    скриншотом/видео), включая продумывание риска регрессии для обычного шага (см. `pending` выше) ДО
    того, как она случилась бы в реальности, а не по факту пойманного бага. Нужен новый живой прогон с
    реальным каскадом (столкновение → отскок в опасность → аномалия → отлёт), чтобы подтвердить, что все
    4 шага теперь видны по отдельности с паузами, а обычный одиночный шаг не стал заметно медленнее.
  - Верификация: `@babel/core` (`babel-preset-expo`, web И android caller) по всем изменённым файлам
    (`useRunnerAnimations.js`, `runnerAnimTriggers.js`, `BoardGrid.js`, `GameBoardScreen.js`) — чисто.

- 2026-08-31 (третий заход): пользователь поймал живьём два новых симптома после очереди анимаций —
  (1) токены в столкновении (`collision_east/west`) выглядят заметно мельче обычных, (2) после
  разрешения столкновения оба бегуна остаются стоять на одной клетке в позе коллизии, ход переходит к
  следующему игроку, никто никуда не отлетает.
  - **(1) Почищено, фронтенд-баг**: `BoardGrid.js` — пара `RunnerToken` в рендере столкновения не
    получала проп `imageScale` вообще, откатываясь на дефолт компонента (0.68), тогда как одиночный
    токен на доске использует `BOARD_TOKEN_IMAGE_SCALE` (0.68×1.8×1.15×1.15 после правок этого же дня) —
    пара была почти вдвое мельче обычного бегуна. Добавлен `imageScale={BOARD_TOKEN_IMAGE_SCALE}` на оба
    токена пары. Не проверено живьём в этом заходе (нет запущенного Android-эмулятора/докер-бэка на
    момент правки) — тривиальный проп, тот же паттерн, что уже используется для одиночного токена.
  - **(2) Найден и ЖИВЬЁМ ПОДТВЕРЖДЁН настоящий бэкенд-баг** (не фронтенд) — редкий случай в этой сессии,
    когда реально удалось поднять `docker ps` (бэк пользователя уже был запущен) и `adb devices`
    (эмулятор тоже уже был поднят) одновременно, что дало возможность живого расследования вместо чтения
    кода вслепую. Ход расследования:
    - `docker exec runner_db psql` — прямой запрос к БД активной игры пользователя (id 20, не трогал её
      состояние, только читал) подтвердил: оба спринтера (163 игрока 39, 167 игрока 40) РЕАЛЬНО стоят на
      одной клетке (`segment=0, position_x=0, position_y=1`) на бэкенде — это не рассинхрон
      фронта/анимации, бэк сам считает их обоих там.
    - Перечитал `Collision.php`/`Move.php`/`StepCollisionService.php` — по коду видно, что столкновение
      ОДИНАКОВЫХ по размеру бегунов (спринтер vs спринтер — тот самый случай) должно разрешаться СРАЗУ,
      без вызова `/collision` игроком: `Collision::collision()` всегда добавляет `Action::TYPE_MOVE` на
      одного из двух бегунов в случайном направлении (`DirectionDiceRoller`, полный 6-направленный бросок,
      не только 3 "вперёд"). Разобрал и ОТБРОСИЛ гипотезу "увело в стену за старт трассы" (`DirectionService`
      действительно возвращает "wall"-клетку со СТАРЫМИ координатами при уходе за `segment<0` — сам по
      себе тоже похож на баг, но он вызывает `destroyHandler`, а оба бегуна в БД — не `destroyed`, значит
      эта ветка тут не сработала) — не поленился проверить гипотезу, а не просто предположил.
    - **Живая репродукция вместо дальнейшего чтения вслепую**: завёл 2 одноразовых тестовых аккаунта
      (`clrepro1`/`clrepro2`), провёл партию через `curl` напрямую к локальному бэку. Сначала воспроизвёл
      столкновение через ОБЫЧНЫЙ ход (`direction`, не первый выход на трассу) — сработало ПРАВИЛЬНО, один
      бегун корректно отлетел на новую клетку. Затем воспроизвёл столкновение через ПЕРВЫЙ ВЫХОД НА
      ТРАССУ (оба игрока выбирают одну и ту же стартовую полосу `firstPosition`) — вот тут и поймал баг:
      оба бегуна остались на одной клетке НАВСЕГДА, `dice` у "приехавшего" не обнулился (как должно быть
      по `Move::collision()`), шаг остался `MOVE`, ход спокойно перешёл дальше — 1-в-1 симптом
      пользователя, воспроизведено детерминированно (не один раз повезло — специально прогнал оба сценария
      подряд в одной партии для контраста).
    - **Настоящая причина**: `RunnerStartMoveService::run()`
      (`D:\Programming\runner-game-backend\src\Service\Game\RunnerGame\Service\Runner\RunnerStartMoveService.php`,
      только чтение) — путь для ПЕРВОГО хода бегуна с резерва на трассу (`$onStart` в
      `StepMoveService::handleDice()`) НЕ идёт через `Orchestrator`/`Move::handle()` вообще, просто
      `setPositionX/setPositionY/setSegment` + `flush()` + `RunnerSaveEvent`. Проверка "а не занята ли
      уже эта клетка другим бегуном" (`if ($otherRunner = $cell->getRunner()) { return
      $this->collision(...); }`) живёт ТОЛЬКО в `Move::handle()`, которым первый выход на трассу в
      принципе не пользуется — столкновение на самом первом шаге партии физически не может быть замечено
      бэком. Это объясняет, почему баг так легко ловится живьём: "оба игрока выбрали одну и ту же
      стартовую полосу для спринтера" — совершенно естественный, частый сценарий на первом ходу партии.
    - **Предложение для бэкенд-разработчика** (не трогал сам — read-only): в
      `RunnerStartMoveService::run()` перед `setPositionX/setPositionY` проверить `$cell->getRunner()`
      на клетке `(0, $firstPosition)` сегмента 0 и, если занята, направить в ту же `collision()`-ветку,
      которой уже пользуется `Move::handle()`, вместо безусловной установки позиции.
    - **На фронтенде чинить нечего для симптома (2)** — `BoardGrid`'s рендер пары (`collision_east/west`)
      корректно отражает то, что реально прислал бэк (два бегуна на одной клетке) — это не баг подсветки/
      анимации, это баг создания самого игрового состояния, лечится только на бэке.
  - Тестовые аккаунты/партия (`clrepro1`/`clrepro2`, игра id 21) — одноразовые, оставлены как есть в
    докере пользователя (не удалял, не влияют на реальную партию пользователя id 20 — её не трогал вообще,
    только читал через прямой SQL).
  - Верификация: `@babel/core` по `BoardGrid.js` — чисто. Проблема (2) верифицирована ЖИВЬЁМ (не
    статикой) — полный цикл регистрация→лобби→ready→start→SELECT→ABILITY skip→MOVE(обычный, сработал)→
    MOVE(первый выход, воспроизвёл баг) через прямые вызовы API к локальному докер-бэку пользователя, с
    промежуточными `GET /api/runner_game` на каждом шаге для проверки реального состояния.

- 2026-08-31 (четвёртый заход): перекраска неоновых вставок под цвет игрока + подключение анимаций
  Солдата и Атлета (пользователь добавил ассеты в `assets/images/runners/{trooper,athlet}/`, тот же
  формат имён, что у скаута).
  - **Находка про несовпадение имён**: `RUNNER_DISPLAY[TANK].label` = "Атлет", `RUNNER_DISPLAY[ATHLETE]
    .label` = "Солдат" (переименовано в тридцатом с чем-то заходе 2026-08-30 — см. запись пятого захода
    того дня), но ключи `RUNNER_TYPES` не переименовывались. Пользователь назвал папки ассетов по НОВЫМ
    (отображаемым) именам: `trooper/` (англ. "солдат") — на самом деле для `RUNNER_TYPES.ATHLETE`,
    `athlet/` (транслит "атлет") — для `RUNNER_TYPES.TANK`. Сопоставил по лейблам, не по названию папки
    — перепутать легко, если подойти формально (`athlet`≠`ATHLETE`, это ловушка).
  - **Перекраска неона — метод**: неоновые вставки на всех трёх наборах (скаут/солдат/атлет, healthy И
    damaged) — это ЕДИНСТВЕННЫЙ по-настоящему насыщенный цвет во всей палитре спрайта (сам костюм —
    тёмный/десатурированный) и лежит в одном hue-диапазоне (сине-голубой) — проверено гистограммой HSL
    по нескольким кадрам каждого набора (`node`-скрипт, ЖИВОЙ анализ пикселей, не на глаз). Это сделало
    возможным чистое разделение "костюм / неон" одним порогом: `hue 150–255°, saturation ≥20%, lightness
    35–96%` (нижняя граница lightness — самое важное число: без неё порог по одному только hue+saturation
    ловил ещё и тёмно-синий сам костюм, весь силуэт красился целиком вместо только линий — первая попытка
    так и получилась, поймано визуально на превью ДО того, как побежал по всем ассетам). Установлен через
    `gifwrap`+`jimp` (npm-пакеты, поставлены в отдельный scratch-проект в сессионном scratchpad, НЕ в
    основной `package.json` — это одноразовый build-time инструмент, не рантайм-зависимость приложения).
  - **Генерация**: для каждого исходного gif — `<name>_base.gif` (неон десатурирован в тон костюма,
    светлота сохранена) и `<name>_mask.gif` (неон залит белым на прозрачном фоне, остальное прозрачно),
    те же кадры/задержки/`loops`, что у оригинала (сверено декодированием обратно после кодирования).
    105 исходных gif → 210 новых файлов, кросс-референс скриптом подтвердил, что каждый `require()` в
    `runnerAnimations.js` резолвится в реально существующий файл (0 missing из 210). Оригиналы (без
    суффикса) оставлены в assets как исходники на случай пересчёта порога, кодом больше не используются.
  - **`RunnerToken.js`**: для типов с набором анимаций `source` теперь объект `{base, mask}` (не голый
    require-ассет) — рендерятся ДВА наложенных `<Image>` (обёрнуты в `View` с абсолютным позиционированием
    через `StyleSheet.absoluteFillObject`): base как есть, mask поверх с `tintColor={color}` (проп уже
    приходил в компонент, использовался для кольца-обводки). Для типов без набора (Жнец) — старый
    однослойный путь, `isDualLayer` проверяет `'base' in raw && 'mask' in raw`, а не просто `typeof
    raw === 'object'` — на вебе react-native-web тоже отдаёт объект для обычных require-ассетов картинок
    (`{uri,width,height}`), плоская проверка на "объект" ложно сработала бы и там.
  - **Живая проверка на вебе (Browser pane, `npm run web`)** — не просто чтение кода: временно подменил
    `AuthScreen` на `screens/__RunnerPreview.js` (сетка RunnerToken на 3 типа × 2 статуса × 3 позы
    (idle/move/attack) × 4 тестовых цвета + строка аватаров), убрал файл и откатил `RootNavigator.js`
    после теста (`git status`/`git diff --stat` подтвердили чистый откат, файла нет). Скриншотами
    подтверждено: неон красится в цвет кольца-игрока на ВСЕХ трёх типах, обоих статусах, во всех позах
    (включая "взрыв" анимации атаки — тоже красится, тематически согласованно, не баг), тёмный костюм и
    метки повреждений НЕ перекрашиваются. Консоль браузера — чисто, без ошибок.
  - **Не проверено живьём**: реальный Android/iOS (`tintColor` на анимированный gif — стандартный RN-
    механизм, но именно с animated gif на native в этом проекте раньше не тестировался; веб — через
    react-native-web CSS-маску, которая сработала, native использует другой механизм тайлинга, теоретически
    должен работать так же, но не подтверждено устройством/эмулятором в этой сессии).
  - Верификация: `@babel/core` (`babel-preset-expo`, web И android caller) по `runnerAnimations.js`/
    `RunnerToken.js` — чисто. Живой прогон на вебе — см. выше.

- 2026-08-31 (пятый заход): пользователь прислал скриншот (Android-эмулятор + браузер side by side) с
  реальной активной партией и тремя новыми замечаниями по итогам первого совместного теста скаута/солдата/
  атлета. Два из трёх — реальные фронтенд-баги, оба найдены и почищены живым измерением DOM (не на глаз).
  - **Настоящая причина "персонажи вне сегментов дороги"**: `BoardGrid.tokenOverlay` пересчитывал
    глобальную колонку бегуна как `segment * cols + positionX`, где `cols` — ПРОП компонента, то есть
    `viewportCols` (сколько колонок реально видно на экране прямо сейчас, динамическая величина в
    портретной раскладке), а НЕ `BOARD_LAYOUT.COLS` (всегда 8 — реальных колонок в одном сегменте данных
    с бэка, то, во что упаковано `positionX` бегуна). Совпадало только пока `viewportCols` случайно
    равнялся 8 — на любом экране, где видно меньше/больше 8 колонок (обычное дело в портрете на телефоне),
    токен получал неверный `globalCol` и рисовался в случайном месте, не совпадающем с его настоящей
    клеткой. В альбомной раскладке БАГ БЫЛ, но не проявлялся — там `viewportCols` всегда жёстко равен 8
    (см. `useBoardLayout`), так что `cols === BOARD_LAYOUT.COLS` случайно совпадали. Фикс — заменить
    `cols` на импортированный `BOARD_LAYOUT.COLS` именно в этой строке (остальные использования `cols` в
    файле — про ширину ВЬЮПОРТА, не про упаковку данных, их трогать не нужно).
  - **Настоящая причина "персонажи больше своей клетки" (второй слой той же жалобы)**: `RunnerToken.js` —
    оба слоя (`base`/`mask`) получали `style={StyleSheet.absoluteFillObject}` (top/left/right/bottom:0,
    БЕЗ явных width/height). На native это работает, но на react-native-web `<Image>` без явного
    числового width/height ИГНОРИРУЕТ absolute-fill и откатывается на НАТУРАЛЬНЫЙ пиксельный размер
    самого gif-файла — тот же баг класса, что уже ловили на `MobileFrameOverlay` (см. запись 2026-08-29,
    "ширина через противоположные left/right без width" не работает у Image на вебе). Проверено
    измерением DOM: `<img>` токена рендерился 204×204/208×208/196×196px — это ТОЧНЫЕ исходные пиксельные
    размеры gif-файлов (сверено с `analyze.js`), а не расчётный размер тайла (~60px). Фикс — `imgLayerStyle
    = { position:'absolute', top:0, left:0, width, height }` с явными числами (те же, что у
    родительского `imgBoxStyle`) вместо `StyleSheet.absoluteFillObject`.
  - Заодно — `BOARD_TOKEN_IMAGE_SCALE` в `BoardGrid.js` пересчитан с накопленных трёх `×1.15` (итог
    ×1.619, из-за чего картинка была на ~34% ШИРЕ самого сегмента — качественно другой эффект, чем
    "крупный персонаж", как выглядело раньше на глаз, до того как размер картинки стал реально измеряться)
    на фиксированный `1.25` — итоговая картинка (`tokenSize(0.72×segment) × 1.25 ≈ 0.9×segment`) немного
    МЕНЬШЕ сегмента, по прямому запросу пользователя, вместо больше. `tintColor` для mask-слоя заодно
    вынесен из `style` в отдельный проп (`Image: style.tintColor is deprecated` — предупреждение в
    консоли, RN рекомендует проп).
  - **Живая проверка — ТОЧНЫМ измерением DOM, не скриншотом**: собран одноразовый `screens/__RoadPreview.js`
    (BoardGrid + `constants/mockGameData.MOCK_GAME` напрямую, без бэка, с одним искусственно добавленным
    бегуном на клетку игрока Евы для проверки рендера пары) — временно подменял `AuthScreen` в
    `RootNavigator.js`, тестировал через `javascript_tool` (`getBoundingClientRect()` по тайлам без фона
    и по токенам с фоном, поиск ближайшего тайла к центру каждого токена), откатил оба файла сразу после
    (`git status` — чисто). Подтверждено число в число: одиночный токен — `centerDist: 0` (идеально по
    центру своей клетки), пара — оба члена на равном расстоянии (15.5px) от центра ОДНОЙ общей клетки по
    разные стороны, картинка одиночного токена (57.5px) заметно меньше тайла (61–65px), как и просил
    пользователь. **Важно про метод**: `<Image>` на react-native-web рендерит и видимый слой (div с
    `background-image`), и скрытый accessibility-`<img>` (opacity:0) — при поиске "ближайшего тайла"
    нужно ИСКЛЮЧАТЬ элементы ВНУТРИ токен-оверлея (`zIndex:2`) из списка кандидатов, иначе токен находит
    сам себя (свой же видимый слой) и даёт ложный `centerDist: 0` для всех, включая пару — поймано и
    исправлено в процессе измерения, не сразу.
  - **Осторожность с dev-сервером**: порт 8081 в этой сессии оказался НЕ моим — принадлежал уже
    запущенному `expo run:android` пользователя (PID подтверждён через `Get-CimInstance Win32_Process`,
    `CommandLine` показал `expo run:android`, а не `npm run web`/launch.json). Правки в `RootNavigator.js`
    (подмена экрана) физически лежат в общем для Android- и веб-сборки исходнике — оценил риск (экран
    Auth монтируется только при `!isAuthenticated`, пользователь уже был залогинен и в активной партии,
    значит подмена не задевала ничего реально смонтированного у него) и продолжил, но откатывал файл
    СРАЗУ после каждого измерения, не оставляя правку висеть. Отдельная попытка поднять ИЗОЛИРОВАННЫЙ
    второй сервер на другом порту (временная вторая запись в `.claude/launch.json`) не удалась (инструмент
    preview_start всё равно упирался в занятый 8081 неясным образом) — не стал разбираться глубже,
    убрал временную запись и просто переиспользовал уже открытую вкладку через `navigate`.
  - **Решено пользователем** (в чате, не в этой сессии кода) — оба открытых вопроса закрыты:
    (1) браузерная версия должна быть АДАПТИВНОЙ по расположению ПАНЕЛИ (слева на широком окне, снизу на
    узком — "как сейчас"), но сама ДОРОГА должна быть ВСЕГДА вертикальной (как на телефоне) независимо от
    формы окна — реализовано отдельным заходом в тот же день, см. запись ниже; (2) подозрение про
    перепутанные лево-право у Атлета/Солдата — пользователь попросил НЕ трогать, снять подозрение (не
    нашёл расхождений ни в коде, ни в ассетах при статическом сравнении, а живого теста, который бы что-то
    доказал, не было) — считать текущий маппинг верным, пока живой тест не покажет обратное.
  - Верификация: `@babel/core` (web И android caller) по `BoardGrid.js`/`RunnerToken.js` — чисто. Оба
    подтверждённых фикса проверены точным измерением DOM живого рендера (см. выше), не только чтением
    кода.

- 2026-08-31 (шестой заход): по итогам решений из предыдущего захода — дорога сделана ВСЕГДА вертикальной
  (независимо от формы окна), плюс убраны цветные кольца-обводки у токенов на доске (по прямому запросу
  пользователя, неон уже показывает цвет игрока — обводка стала избыточной).
  - **Кольца на доске убраны**: `RunnerToken.js` — новый проп `showRing` (по умолчанию `true`, карточка в
    панели игрока по-прежнему рисует кольцо — там оно всё ещё нужно для быстрой идентификации в списке).
    `BoardGrid.js` передаёt `showRing={false}` во всех трёх местах рендера токена на доске (одиночный,
    пара east/пара west). Белая рамка `selected` (сейчас выбран для хода/выстрела) — независимая сущность,
    рисуется как и раньше вне зависимости от `showRing`.
  - **Дорога всегда вертикальная**: до этого захода `useBoardLayout` считал ДВЕ полностью разные геометрии
    дороги — портретную (вертикальные дорожки) и альбомную (горизонтальные дорожки), выбор зависел от
    соотношения сторон окна (`orientation`). Теперь есть ОДНА геометрия (`computeRoadGeometry(roadZoneW,
    roadZoneH, arrowReserveH)`, общая функция) — считает вертикальные дорожки (ROWS по ширине зоны, COLS
    скроллится по высоте) от ЛЮБОГО прямоугольника, который ей достаётся. `orientation` в хуке больше НЕ
    управляет геометрией дороги — теперь это ИСКЛЮЧИТЕЛЬНО переключатель расположения ПАНЕЛИ игрока (слева
    на широком окне / снизу на узком, "как сейчас" по прямому решению пользователя), панель и дорога стали
    независимыми осями решения. `GameBoardScreen.js`: `<BoardGrid orientation="portrait">` теперь ЖЁСТКО
    захардкожен (не зависит от `orientation` хука); блок стрелок/сетки/подписи фрагмента (`roadZonePortrait`
    style, раньше применялся только при `isPortrait`) используется ВСЕГДА; стрелки — всегда вверх/вниз
    (убрана альбомная ветка с `RoadNavButton`-колонкой влево/вправо, `navBtnColumn`-стиль удалён как
    мёртвый); `FragmentLabelStrip` — раньше только в портрете, теперь всегда. Расположение ПАНЕЛИ
    (`!isPortrait && <PlayerInfoPanel width=leftPanelW>` вверху / `isPortrait && <panelFrameWrap>` внизу),
    `useMobileNavButtons`/`MobileFrameOverlay` (мобильная sci-fi рамка, только родное приложение+портрет) —
    НЕ тронуты, всё ещё завязаны на `orientation`/`isPortrait`, как и просил пользователь ("как сейчас").
  - **Живая проверка ТОЧНЫМ измерением DOM** (тот же метод, что в прошлом заходе — временный
    `screens/__RoadPreview.js` с мок-данными вместо реального бэка, `AuthScreen` временно подменён в
    `RootNavigator.js`, откачено сразу после теста) — на широком окне (1400×800): панель-заглушка слева
    шириной 430px (leftPanelW), дорога справа ВЕРТИКАЛЬНАЯ, одиночный токен `centerDist: 0` (идеально по
    центру клетки), пара — симметрично по 35px от центра общей клетки. На узком окне (420×800): панель
    снизу (top:480, во всю ширину, высота 320 = panelH), дорога сверху, токены тоже точно на клетках
    (17.4px для пары — тот же симметричный сдвиг, что и раньше, геометрия портретной ветки не менялась).
    Оба случая подтверждают: панель — "как сейчас" (адаптивна по форме окна), дорога — всегда вертикальная.
  - **Осторожность с dev-сервером, повторно**: к моменту этого захода сервер пользователя на 8081 (тот же
    `expo run:android`, что и в прошлом заходе) уже сам отключился (процесс подтверждённо не найден в
    `Get-Process`) — стартовал СВОЙ собственный сервер через `preview_start` штатно (не пришлось повторно
    рисковать использованием чужого), после теста остановил его (`preview_stop`).
  - Верификация: `@babel/core` (web И android caller) по `useBoardLayout.js`/`GameBoardScreen.js`/
    `RunnerToken.js`/`BoardGrid.js` — чисто. Живой прогон — см. выше, оба сценария (широкое/узкое окно)
    подтверждены точным DOM-измерением, не на глаз.
