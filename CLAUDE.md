# CLAUDE.md

Заметки по фронтенду Runner для себя (Claude) — чтобы не перечитывать весь проект на каждый запрос.
Правь этот файл при значимых изменениях архитектуры и веди раздел «Заметки» внизу.

## Проект

React Native (Expo 56, RN 0.85, React 19) клиент игры Runner — мультиплеер, пошаговая, 2-4 игрока.
Бэкенд — отдельный репозиторий `D:\Programming\runner-game-backend` (Symfony), доступ у меня **только на
чтение**. Контракты — `D:\Programming\runner-game-backend\README.md`, правила игры — загруженный
пользователем «Правила игры.txt» (мультиплеерная настолка про гонку отрядов по трассе). Полезно свериться
с бэковым `CLAUDE.md` в его репозитории — там расписана архитектура Orchestrator/Step-сервисов и Mercure.

Запуск: `npm run start` (Expo), `npm run web`, `npm run android`. Тестов нет.

## Дисциплина ведения CLAUDE.md (2026-09-24, прямое требование пользователя)

Файл разросся настолько (1.25МБ на момент первой архивации), что грузился в контекст ЦЕЛИКОМ каждую
сессию и съедал добрую половину бюджета ещё до начала работы. Три файла с разной ролью:
- **CLAUDE.md** (этот файл) — актуальная архитектура (может быть сколь угодно длинной — код меняется,
  знание нужно) + список ОТКРЫТЫХ задач («Известные пробелы / TODO») + «Заметки» СЖАТО: одна строка на
  закрытую задачу («дата — проблема → итог», без деталей расследования).
- **CLAUDE_DONE_TASKS.md** — полная история недавно (последние ~2-3 недели) закрытых задач: сюда
  переезжает всё, что раньше писалось многоабзацным разбором в «Заметки» — как искал, что не сработало,
  как проверял живьём.
- **CLAUDE_ARCHIVE.md** — то же самое, но старше ~2-3 недель — переносится СЮДА из CLAUDE_DONE_TASKS.md,
  когда та тоже разрастётся.

**При закрытии КАЖДОЙ задачи** (не откладывать на потом): не писать длинный абзац прямо в «Заметки» —
туда одна строка («дата — проблема → итог, детали см. CLAUDE_DONE_TASKS.md»), а сам разбор (гипотезы,
что не сработало, как проверял) — сразу в CLAUDE_DONE_TASKS.md. Если у задачи остался НЕЗАКРЫТЫЙ хвост
(«живого прогона не было», «нужно проверить X») — не хоронить его внутри записи, выносить отдельным
пунктом в TODO, чтобы не потерялся при следующем сжатии.

**Раз в 1-2 недели (или когда CLAUDE.md/CLAUDE_DONE_TASKS.md ощутимо разрослись)** — архивировать:
перенести записи CLAUDE_DONE_TASKS.md старше ~2-3 недель в CLAUDE_ARCHIVE.md тем же приёмом, что и при
первой архивации 2026-09-24 (найти по датам границу через `grep -n "^- 202"`, вырезать/склеить куски
файла через `sed -n`, проверить, что границы совпадают построчно — см. `git log`/эту сессию за точный
рецепт, если понадобится повторить).

## Правило работы (2026-09-12, прямое требование пользователя)

**Решай вопросы точечно.** Если правка технически может быть сделана и узким изменением
(одна константа/одно место), и более широким (рефакторинг общего механизма, правка кода,
используемого несколькими фичами) — по умолчанию узкая, если явно не попросили шире.

**Никогда не меняй код, который влияет не только на текущий запрос и потенциально может
сломать другие места, — без явного вопроса пользователю заранее.** Это касается и
"улучшений", сделанных попутно, пока чинишь что-то другое (пример нарушения этого правила
в этой же сессии: несколько раз подряд трогал общие тайминги/константы, использующиеся сразу
в нескольких местах, вместо того чтобы сначала спросить, можно ли). Прежде чем такую правку
делать — спроси через AskUserQuestion или явно опиши в тексте, что именно и где может
задеть, и дождись подтверждения.

## Структура

```
src/
  api/         клиент + эндпоинт-модули (client.js — токены/refresh/retry, ApiError, normalize.js,
               lobby.js, runnerGame.js, me.js — GET /api/me, слим-сводка "где сейчас пользователь")
  components/
    ui/        общие виджеты (Screen, Button, Input, ParallaxBackground)
    menu/      главное меню
    game/      всё про игровую доску (см. ниже), включая GameWaitingRoom (пред-игровой ready-up)
  config/      env.js — API_URL/MERCURE_URL, зависят от Platform.OS (эмулятор Android = 10.0.2.2)
  constants/   GameConstants.js (зеркалит бэковые enum'ы), mockGameData.js (заглушка ответа бэка,
               использовалась до подключения GameBoardScreen к реальному API)
  context/     AuthContext (обёртка над api/auth + токены)
  hooks/       useAuth (реэкспорт AuthContext), useMercure, useBoardLayout/useBoardScroll/useScrollAnimation/useLockLandscape
  lib/         мелкие утилиты без React (board.js, jwt.js, notify.js, storage.js, eventSource.js,
               hexDirection.js — гекс-соседи клетки, зеркало DirectionService на бэке)
  navigation/  RootNavigator (auth-gate), routes.js
  screens/     AuthScreen, MainMenuScreen, LobbySearchScreen, LobbyScreen, GameBoardScreen
  store/       lobbyReducer.js, runnerGameReducer.js — чистые редьюсеры для Mercure-событий
  theme/       единственный источник colors/spacing/font/radius/shadow
```

Конвенция файла: первая строка — комментарий с путём (`// src/screens/Foo.js`). Комментарии объясняют
**почему**, а не что. Стилей мало (inline объекты через `StyleSheet.create`), Tailwind нет.

## Ключевые архитектурные решения

**api/client.js** — токены в памяти модуля (не в React state), один общий `refreshPromise` на все
конкурентные 401, `credentials: 'include'` для Mercure-cookie. `ApiError` — единая обёртка с
`isNetwork/isUnauthorized/isGone/userMessage`.

**useMercure** (`hooks/useMercure.js`) — протокол «слушаем SSE → буферим → грузим снапшот REST → отсекаем
буфер по version → live». **Важный фикс**: `sync()` (REST-снапшот) запускается сразу в `connect()`, а не по
событию `'open'` у EventSource. Раньше был баг: если Mercure-хаб недоступен, `'open'` не наступает никогда,
и экран висел в вечном спиннере, хотя REST прекрасно работал. Если будешь трогать этот хук — не возвращай
эту гейтировку назад.

**ИСПРАВЛЕНО на бэке, статус пересмотрен 2026-09-25**: `Lobby::toArray()` теперь ОТДАЁТ `version`
(`Entity/Lobby.php:134`, `private int $version = 0` + инкремент-метод на месте) — перепроверено чтением
текущего файла, старая заметка ("не отдаёт") устарела. `normalizeLobby` (`api/normalize.js:13`) уже
готов к этому без правок — `raw.version ?? 0` сам подхватит реальное значение, дефолт остаётся чистой
подстраховкой на случай регресса. У `RunnerGame` version был добавлен раньше, тем же способом
(`Entity/RunnerGame.php:420`, добавил сам пользователь).

**Screen.js** — общий каркас всех обычных экранов: SafeAreaView + `ParallaxBackground` (единый на все
экраны, рендерится тут один раз, а не в каждом экране) + опциональный scroll/KeyboardAvoidingView. Экран
логина/меню/лобби всегда через `<Screen>`. **GameBoardScreen — исключение**, он полноэкранный и рулит
раскладкой сам (без safe-area отступов), но тоже вставляет `<ParallaxBackground />` вручную для
единообразия фона.

**Ориентация GameBoardScreen** — с 2026-08-31 (шестой заход) `orientation` из `useBoardLayout`
(`'landscape'/'portrait'` по фактическому соотношению `screenW`/`screenH`, адаптивно и на вебе через
ресайз окна, и на мобильных через физический поворот) управляет ТОЛЬКО расположением ПАНЕЛИ игрока —
слева на широком окне ('landscape'), снизу на узком ('portrait'). Сама ДОРОГА (`BoardGrid`) — ВСЕГДА
рисуется в портретной геометрии (вертикальные дорожки, скролл по вертикали, стрелки вверх/вниз),
независимо от `orientation` — `<BoardGrid orientation="portrait">` в `GameBoardScreen` захардкожен, не
завязан на хук. До этого захода `orientation` управлял ОБЕИМ осями сразу (была отдельная "альбомная"
геометрия дороги — горизонтальные дорожки) — разделено по прямому запросу пользователя. `useBoardLayout`
считает геометрию дороги ОДНОЙ общей функцией (`computeRoadGeometry`) от прямоугольника, который ей
достаётся (после вычета места под панель — слева или снизу, в зависимости от `orientation`), а не двумя
раздельными ветками. Кнопки навигации вверх/вниз (не mobileNav-случай, см. ниже) — КОЛОНКОЙ СЛЕВА от
дороги (седьмой заход того же дня, было сверху/снизу), ассет `RoadNavButton` (тот же, что у mobileNav),
размер `navBtnSize` — ВСЕГДА половина размера сегмента, возвращается прямо из `computeRoadGeometry`
(параметр `reserveNavColumn` — учитывает ширину колонки кнопок ПРИ расчёте самого сегмента, замкнутая
форма: делит бюджет ширины на `ROWS+0.5`, а не на `ROWS`, см. комментарий в файле). **Движение по трассе
идёт снизу вверх** (начало трассы — у панели игрока) —
реализовано в `BoardGrid` через `flexDirection: 'column-reverse'` (чисто layout-свойство, без
transform/отражений — старая версия на `transform: scaleY(-1)` заменена ещё в пятнадцатом-шестнадцатом
заходах 2026-08-13, эта строка была не обновлена вовремя), подробности и вся геометрия разобраны в шапке
`BoardGrid.js`. `useAdaptiveOrientation` (бывший `useLockLandscape`) на мобильных снимает системный лок
ориентации на время экрана сессии и возвращает portrait-лок при уходе — раскладку выбирает сам экран
через `orientation`, не через системный поворот. Координаты ячеек (`cell.id`/`row`/`col`/`blockIndex`,
которые видит `GameBoardScreen.handleCellPress`) от `orientation` не зависят вообще — вся игровая логика
(SELECT/MOVE/SHOOT/подсветка) не в курсе, где сейчас панель.

**GameBoardScreen** — с Фазы 1 (см. запись 2026-08-12, седьмой заход, ниже) подключён к реальному
`GET /api/runner_game` + Mercure-топику `runner_game_{id}` через `useMercure`/`runnerGameReducer`, тем же
протоколом «буфер → снапшот → live», что и `LobbyScreen`. Раскладка: слева `PlayerInfoPanel`
(переключатель игроков, кубики, drag-and-drop на зоны усилений через
`react-native-gesture-handler`+`reanimated`, карточки бегунов), справа `BoardGrid` (сетка 6×8×3 сегмента,
скролл через `useBoardScroll`: PanResponder на мобильных, `useScrollAnimation` на вебе). Геометрия —
`useBoardLayout`, считает так, чтобы один фрагмент 8 колонок помещался в правую зону без обрезки.
`lib/board.js` разворачивает `trackBegin/Middle/End` в плоский список ячеек и резолвит типы клеток
(`danger_2/danger_3` → `danger`, `anomaly` → `danger`, нет данных → `road`). С Фазы 2 (запись
2026-08-12, девятый заход, ниже) select/move/collision/shoot/ability вызывают реальный бэк.
**Принципиально: локального стейта "что назначено" в GameBoardScreen нет** — как только SELECT/ABILITY
проходят, `runner.dice`/`player.ability`/`player.diceN` обновляются live-событиями
(`runnerGameReducer`), и UI просто читает их напрямую (см. `PlayerInfoPanel`/`RunnerCard`). Единственный
переходный локальный стейт — `pendingAbility` (heal/reaper ждут второй тап после дропа кубика на зону,
т.к. бэк принимает `runnerId`/`positionX/Y/segment` одним вызовом `/ability`, а не двумя шагами).
Подсветка легальных клеток (MOVE/SHOOT/reaper-размещение/первый выход на трассу) — `lib/hexDirection.js`
(`forwardNeighbors`), зеркалит `DirectionService::run()`+`canShoot()` на бэке.

**Партия имеет собственный ready-up гейт, отдельный от лобби**: после `game_created` (лобби) игра
рождается со статусом `waiting`, и каждый игрок обязан вызвать `POST /runner_game/start`, прежде чем
статус станет `active` (событие `game_active`). `GameBoardScreen` рисует `GameWaitingRoom` при
`status === GAME_STATUS.WAITING` и сам переключается на доску, когда статус меняется — доп. навигации не
нужно, компонент просто по-другому рендерит тот же live-стейт.

**Коллизия сигналится НЕ через `player.step`** (в бэковом enum `PlayerStep` нет кейса COLLISION) — а через
`game.extraTurnPlayer`/поле `extraTurnPlayer` события `game_turn_changed`. Пока оно не `null` — бэк
блокирует вообще все игровые действия всем игрокам, и никакого таймаута на бэке нет: если игрок-цель не
ответит на `/collision`, партия зависает навсегда. Фронт (Фаза 1) это не резолвит, только показывает
баннер и через 18 сек даёт кнопку ручного `resync()` — см. `GameBoardScreen.COLLISION_STUCK_TIMEOUT`.

`GestureHandlerRootView` обязателен в `App.js` (уже добавлен) — без него драг кубиков молча не работает на
Android.

**Анимации бегунов (Скаут/Солдат/Атлет — `RUNNER_TYPES.SPRINTER/ATHLETE/TANK`)** — начало Фазы 3, см.
`2026-08-31` в «Заметки» ниже за полную историю живого тестирования и найденные баги. Архитектура:
- `constants/runnerAnimations.js` — реестр gif-ассетов на `{type, status}`, только для типов, для которых
  реально есть набор: скаут (`assets/images/runners/scout/`), солдат (`.../trooper/`,
  `RUNNER_TYPES.ATHLETE` — не путать с папкой, названия разъехались после переименования лейблов, см.
  находку 2026-08-31 в «Заметки»), атлет (`.../athlet/`, `RUNNER_TYPES.TANK`) — `getRunnerAnimationImage`/
  `getRunnerAvatarImage` возвращают `null` для остальных типов (пока только Жнец), и `RunnerToken` сам
  откатывается на старую статичную иконку (`RUNNER_DISPLAY[type].icon`) — добавление анимаций следующему
  типу бегуна требует только новую запись в этом реестре, без правок компонентов.
- **Перекраска неона под цвет игрока** (2026-08-31, см. находку в «Заметки»): каждая запись в реестре —
  не один gif, а пара `{base, mask}`. `base` — тот же кадр, но неоновые вставки обесцвечены (десатурация
  с сохранением светлоты) в тон тёмного костюма. `mask` — те же кадры, только неон залит чистым белым на
  прозрачном фоне, всё остальное прозрачно. `RunnerToken` рисует пару ДВУМЯ наложенными `<Image>`: base
  как есть, mask поверх с `tintColor={color}` (цвет игрока, уже приходил в компонент) — так неон красится
  в цвет команды, а сам спрайт (тёмный костюм, метки повреждений и т.п.) не трогается. Обе гифки на пару
  сгенерированы автоматически скриптом (порог по HSL: `hue 150–255°, saturation ≥20%, lightness 35–96%`)
  из оригинальных ассетов пользователя — сам скрипт не сохранён в репозитории (одноразовый, в scratchpad
  сессии), но порог задокументирован здесь на случай, если понадобится перегенерировать. Оригинальные
  (без суффикса) gif оставлены в assets — код их больше не использует, но на случай пересчёта порога они
  нужны как исходники.
- `hooks/useRunnerAnimations.js` — стейт-стор `{ [runnerId]: {kind, direction?/side?, nonce, fromStatus?} }`
  с автооткатом в `idle` по таймауту (`kind`: move/attack/gotShot/fly/destroyed; `destroyed` — терминально,
  без отката). **Очередь по runnerId** (2026-08-31, второй заход): `trigger()` не перезаписывает текущую
  анимацию мгновенно, а кладёт шаг в очередь конкретного бегуна — играются по одному, каждый свою полную
  длительность, прежде чем начать следующий. Нужно для каскадов (отскок от столкновения → аномалия →
  отлёт из неё, несколько `runner_save` почти одновременно) — раньше все промежуточные шаги схлопывались
  в последний trigger (жалоба пользователя: бегун "телепортировался" в клетку аномалии, показывалась
  только финальная анимация). Единственное исключение — `pending`-слияние: `step_move` (транзиентный,
  даёт направление раньше позиции) метит свой шаг `pending: true`, и следующий `runner_save` с той же
  передвижкой ДОПОЛНЯЕТ его позицией на месте, а не заводит второй слот — иначе обычный некаскадный шаг
  играл бы вдвое дольше. **`anomaly`-заготовка (2026-09-01)** — тот же pending-приём, но с `kind:'fly'`
  вместо `'move'`, и мердж СПЕЦИАЛЬНО НЕ даёт входящему `trigger()`-вызову (реальный `runner_save` отлёта)
  подменить `kind` — если заготовка уже была `'fly'`, результат мерджа тоже `'fly'`, даже если следующий
  `runner_save` по эвристике forwardNeighbors формально выглядел бы как обычный шаг вперёд (`'move'`).
  Раньше выход из аномалии мог случайно отрисоваться как ходьба вместо перелёта — по прямому запросу
  пользователя зафиксировано жёстко. Второй экспорт хука — `visualPositions[runnerId]`: пока у бегуна есть
  незавершённая очередь, `BoardGrid` рисует его в позиции ТЕКУЩЕГО шага очереди (проп
  `runnerVisualPositions`), а не в реальной (уже полностью применённой в game-стейте) — иначе
  промежуточные "остановки" каскада не отрисовались бы ни одним кадром, `RunnerTokenSlide` заскользил бы
  сразу к финальной точке. `reset()` — сбрасывает очереди/таймеры/визуальные позиции, вызывается вместе
  с ручным `resync()` (кнопка при зависшей коллизии) — REST-рефетch подменяет game-стейт целиком, минуя
  событийный поток, который двигает очередь, без сброса застрявшая очередь держала бы бегуна в позиции
  старого шага. `lib/runnerAnimTriggers.js` — чистые функции "Mercure-событие → что триггернуть":
  `handleVersionedRunnerAnimEvent` смотрит на versioned-события ДО применения `runnerGameReducer` (нужно
  старое состояние для сравнения), кладёт `toPosition` в каждый `trigger` про перемещение;
  `handleTransientRunnerAnimEvent` — на транзиентные (`step_move`/`step_shoot`/`anomaly`). **Важно про
  knockback/`fly`**: бэк НЕ шлёт отдельного сигнала "этого бегуна оттолкнуло туда-то" (событие `collision`
  несёт `direction`, но не id бегуна — см. находку в «Заметки», 2026-08-31) — используется эвристика: если
  новая позиция из `runner_save` НЕ является forward-соседом старой (`lib/hexDirection#forwardNeighbors`),
  это трактуется как отскок/телепорт → `fly`. Осознанно принятый компромисс, не 100%-надёжный сигнал.
  **Первый выход из резерва на трассу (2026-09-01) — БЕЗ анимации вообще**: `handleVersionedRunnerAnimEvent`
  для `prev.segment == null` просто `return`, `trigger()` не вызывается — бегуна до этого момента нигде не
  было нарисовано (скользить неоткуда), по прямому запросу пользователя он появляется сразу в `idle`, не
  "шагая на месте".
- **Направление ассета — ОДНА функция на move И attack** (`constants/runnerAnimations
  #resolveMoveAssetDirection(direction, depthChanged, targetLaneOdd)`, 2026-09-01, живой скриншот с
  Android): LEFT_UP/RIGHT_UP не всегда "вперёд по экрану" — дорожки через одну смещены на пол-сегмента
  "назад" (см. `BoardGrid` — `row % 2 !== 0 → marginTop/marginLeft segment/2`, кирпичная кладка), и ЧИСТО
  боковой шаг/выстрел (глубина/`positionX` не меняется — см. известный TODO ниже про чётность) на такую
  смещённую (нечётную) дорожку визуально идёт south-*, не north-*; если же глубина ВСЁ-ТАКИ продвинулась
  (диагональ), полный сегмент продвижения вглубь всегда перевешивает пол-сегментный сдвиг дорожки — итог
  всегда north-*. Стрельба использует ЭТУ ЖЕ функцию, что и ходьба (изначально была ошибочно исключена —
  считалось, что раз бегун не двигается, "целевой дорожки" нет, но `canShoot()` на бэке проверяет ровно
  тех же соседей, что и движение, целится через ту же hex-клетку). Для стрельбы `depthChanged`/
  `targetLaneOdd` считаются в `handleTransientRunnerAnimEvent`'s `step_shoot` через
  `hexDirection#neighborPosition(shooter, direction)` (нужна ТЕКУЩАЯ позиция стрелка из `gameRef.current`,
  не дельта, стрелок не двигается).
- **`components/game/RunnerTokenSlide.js`** — токен на доске не телепортируется между клетками, а плавно
  скользит (`Animated.ValueXY` + `useLayoutEffect`, НЕ `useEffect` — иначе на Android на кадр видна
  целевая позиция ДО начала скольжения). React `key` в `BoardGrid` для этой обёртки — id БЕГУНА, не ключ
  клетки, иначе компонент пересоздавался бы на каждый шаг и терял анимационное состояние. **КРИТИЧНО
  (найдено 2026-09-01, настоящая причина многозаходного "телепорта" при коллизии)**: компонент ТАКЖЕ
  сравнивает `width`/`height` между рендерами — если они ИЗМЕНИЛИСЬ, считает это сменой раскладки экрана
  (поворот/ресайз, см. `windowStart`/`resized`-ветку в файле) и МГНОВЕННО телепортирует БЕЗ слайда, в
  обход анимации вообще. Значит **бокс (`width`/`height`), который передаётся ЭТОМУ компоненту для
  КОНКРЕТНОГО runnerId, ОБЯЗАН быть одинаковым во ВСЕХ состояниях этого бегуна** (соло на клетке, пара при
  коллизии, "приезжающий" и т.п.) — иначе ЛЮБОЙ переход между состояниями ложно триггерит resize-защиту
  и убивает слайд ровно в момент, когда он важнее всего (приезд/отскок). Раньше пара при коллизии
  использовала СВОЙ маленький бокс (`pairSize`) вместо полного тайла (`segmentW×segmentH`, как у соло) —
  каждый вход/выход из коллизии рвал анимацию. Правило теперь: бокс у ВСЕХ элементов `tokenOverlay`
  (см. ниже) — всегда `segmentW×segmentH`, а горизонтальный разъезд пары — ЧИСТО `transform:translateX`
  на самом `RunnerToken` (`innerOffsetX` в `BoardGrid`), размер бокса это не трогает. **Держи это в уме
  при ЛЮБОЙ будущей правке позиционирования токенов** — разный box size между рендерами того же
  бегуна снова сломает слайд, даже если сама x/y-логика идеальна.
- **Столкновение (2 бегуна на одной клетке) — `BoardGrid#tokenOverlay`, ПЛОСКИЙ список** (переписан
  2026-08-31/09-01, несколько заходов подряд по живым багам): один элемент СПИСКА — один БЕГУН (не клетка),
  `key` на верхнем уровне ВСЕГДА `runner.id`, независимо от того, солирует он или в паре — раньше пара
  оборачивалась в `<React.Fragment key={cellKey}>`, и React видел это как "другой элемент списка" при
  переходе солист↔пара (ключ менялся с числа на строку клетки), полностью размонтируя/монтируя поддерево и
  теряя Animated-состояние `RunnerTokenSlide` — тот же класс телепорта, что и box-size-баг выше, но на
  уровне СПИСКА, а не одного компонента; чинились оба, по отдельности, в разных заходах. Рендер пары —
  `collision_east`/`west`, не "первый + значок +N" (это осталось только для 3+, не должно происходить по
  правилам). Кто слева/справа — детерминированное правило: текущий активный игрок (тот, кто "наехал",
  `currentTurnPlayerId`) — справа (`collision_west`), другой — слева (`collision_east`); фолбэк по id, если
  ни один не совпал (не должно случаться в норме). Два доп. требования пользователя, 2026-09-01, четвёртый
  заход:
  1. **Коллизионная поза не должна появляться раньше, чем "приезжающий" бегун доиграет СВОЮ анимацию
     ходьбы/перелёта** — пока у кого-то из двух `runnerAnims[id].kind === 'move'|'fly'` ("arriving"), ОБА
     рисуются как обычные независимые solo-токены (визуально один едет в клетку, где стоит другой); только
     когда ОБА settled — включается коллизионная поза.
  2. **Минимум `COLLISION_MIN_HOLD_MS` (1000мс) показа**, даже если бэк уже увёл одного из них в другую
     клетку следующим же событием — реализовано холдом (`collisionHoldsRef`, мутируется ПРЯМО в теле
     `useMemo`, намеренно не через `setState` — не должен сам по себе триггерить лишний ре-рендер;
     `holdTick`/`setTimeout` — единственный способ форсить пересчёт РОВНО когда истекает `until`, если до
     этого не прилетело вообще ни одного нового события/пропа). Пока `pairKey` активен, РЕАЛЬНЫЕ позиции
     ОБОИХ бегунов в этом рендере игнорируются целиком (`heldRunnerIds`) — их рисует ТОЛЬКО второй проход
     по холдам, на замороженных x/y/leftRunner/rightRunner, пока не истечёт `until`. Как только холд
     снимается, `tokenOverlay` наконец отдаёт РЕАЛЬНУЮ (уже возможно ушедшую вперёд) позицию —
     `RunnerTokenSlide` не видит разницы между "x/y не менялись, потому что мы держим холд" и "x/y реально
     не менялись", просто честно анимированно доскользит остаток пути одним слайдом.
  Разъезд пары (7% ближе центров, чем геометрический максимум без наложения — `(pairGap/2+pairSize/2)*0.93`,
  по прямому запросу пользователя) — ЧИСТО `transform:translateX` (`innerOffsetX`), НЕ через x/width бокса
  (см. box-size-правило выше). Вертикально пара крепится к низу клетки ТАК ЖЕ, как одиночный токен (см.
  `imageAlign`/`anchorBottom` ниже) — раньше была единственным состоянием с центрированием, пользователь
  прямо попросил единообразия; на вебе — по-прежнему центр (эта правка вся целиком native-only).
- `RunnerToken` — `imageScale` проп независимо масштабирует картинку ВНУТРИ кольца-обводки, не трогая
  само кольцо (`size`) — по прямому запросу пользователя после нескольких итераций (кольцо на доске
  должно остаться исходного размера, а сам спрайт — крупнее его, вылезая за край, это осознанно). На
  native (`BoardGrid`) картинка ещё крупнее (2026-09-01, два раунда: ×1.3, затем ещё ×1.15 сверху, итого
  ×1.495 от исходного) — **`imageAlign` проп** (`'center'` по умолчанию / `'bottom'`) определяет, как
  переразмеренная картинка позиционируется ВНУТРИ кольца: `'bottom'` (только native, только доска) прижимает
  НИЗ картинки к низу кольца, весь лишний размер выпирает ТОЛЬКО вверх ("ноги остаются на месте, туловище
  выпирает больше", прямой запрос пользователя) — само кольцо-контейнер СНАРУЖИ тоже должно быть прижато к
  низу своего родителя (`BoardGrid#styles.tokenLayerBottom`), `imageAlign` красит только внутреннее
  позиционирование картинки относительно кольца, это два независимых уровня прижатия. Веб не трогали
  (пользователь прямо просил не менять веб-версию) — там всё то же центрирование, что было изначально.
  **Кроссфейд между анимациями, только native** (2026-09-02): фикс мигания через стабильный `key`
  (см. ниже) убрал ПРИНУДИТЕЛЬНЫЙ remount на повторе одной анимации, но живой тест показал, что блики
  остаются на Android МЕЖДУ РАЗНЫМИ анимациями (idle→move, move→attack и т.п., где `source` меняется на
  ДЕЙСТВИТЕЛЬНО другой файл) — на вебе того же не видно (подтверждено пользователем), значит дело в самой
  Android-декодировке нового gif (Fresco), а не в JS/remount (тот путь уже чист). Чинить саму декодировку
  из JS нельзя — вместо этого `RunnerToken` держит ПРЕДЫДУЩИЙ `source` видимым непрозрачным слоем ПОД
  новым, пока новый набирает `opacity` 0→1 за 120мс (`CROSSFADE_MS`) — старый кадр прикрывает паузу
  декодирования нового. Реализовано через `prevRef`/`fade`/`fadingLayer`-состояние и вынесенный
  `TokenImageLayer` (общий рендер dual-layer/одиночного слоя, переиспользуется для текущего и уходящего
  кадра). `key` у `<Image>` внутри — ВСЕГДА одна и та же строка (`'stable'`/`'avatar'`), никогда не
  меняется на смену анимации: раньше (до 2026-08-31, девятый заход) менялся нарочно при ПОВТОРЕ одной и
  той же анимации подряд, чтобы форсировать перезапуск gif с первого кадра — это и оказалось настоящей
  причиной "мигания" (смена key = полный размонт/монтаж `<Image>`, на Android decode-пауза). Обычная смена
  prop `source` на уже смонтированной `Image` перезапускает gif сама, без разрушения нативной view.
- **Известный Android-баг: `elevation` без явного `backgroundColor` иногда рисует тень/контур по
  ПРЯМОУГОЛЬНОЙ границе вида, игнорируя `borderRadius`** (2026-09-01, живой скриншот — у ВЫБРАННОГО
  бегуна на доске квадрат вместо круга; у токенов на доске `showRing=false`, кольцо обычно без фона
  вообще, что и провоцирует баг). Фикс — `backgroundColor:'transparent'` явно на `styles.selected` даёт
  Android нужную форму для расчёта тени, ничего визуально не меняя. НЕ подтверждено живьём в сессии, где
  чинилось (нет доступа к устройству) — это обоснованная, но не стопроцентно проверенная правка под
  хорошо документированный класс RN-Android бага.
- **Звук движения синхронно с анимацией** (`GameBoardScreen.js`, 2026-09-01/09-02) — `moveSound`
  (`useAudioPlayer(lazer.mp3)`, `loop=true`) играет, пока у ХОТЬ ОДНОГО бегуна сейчас `runnerAnim.anims[id]
  .kind === 'move'`, останавливается и перематывается в начало (`pause()+seekTo(0)`), как только ни у кого
  больше нет активной `move`-позы. Старый одноразовый "клик" (`shotSound`) на КАЖДЫЙ тап по клетке в
  `handleCellPress` — убран целиком по прямому запросу пользователя (казался блокирующим, "пока не
  доиграет — персонаж не идёт на вид", хотя технически `.play()` не await'ился).

### Известные грабли RN/Android (проверено на практике, не теория — держать в уме для ЛЮБОГО нового
компонента с картинками/анимациями/9-slice-декором, не только для тех, где это уже словили)

- **`resizeMode="repeat"` на Android НЕ учитывает плотность экрана** — тайл рисуется в физических
  пикселях файла 1:1, независимо от `dp`/`PixelRatio`, из-за чего на плотных экранах получается заметно
  мельче задуманного, рассинхронизируясь с соседними `stretch`-элементами. Тайлить только явными
  `resizeMode="stretch"`-копиями нужного размера (см. `components/ui/TiledImage.js`), не через `repeat`.
- **`onLayout` на абсолютно спозиционированном (`position:'absolute'`) элементе внутри родителя с
  AUTO-высотой на Android может стабильно вернуть 0** — родитель ещё не устаканил размер к этому
  измерению. Мерить такой размер через `measureInWindow` на реальном flow-элементе снаружи, не
  полагаться на `onLayout` самого абсолютного потомка.
- **Дробные (нецелые) пиксельные размеры/позиции соседних тайлов 9-slice-мозаики** (`TiledImage`/
  `PersonPanel`/`FramePanel`) **дают швы на Android** — Skia округляет каждый тайл независимо, соседи не
  гарантированно совпадают по границе. Всегда `Math.round()` ИТОГОВЫЕ размеры/позиции тайлов, не сам
  коэффициент масштаба.
- **Большой многострочный PNG-спрайт-лист (сотни KB decoded, единицы МБ) рендерится РАЗМЫТО на Android
  независимо от способа вырезания окна/масштабирования** (transform vs left/top, scale=1 vs честный
  масштаб) — точный механизм не установлен (не лимит текстуры GPU), но воспроизводимо и лечится
  нарезкой на отдельные файлы поменьше, по одному на анимацию/строку (см. `constants/
  spritePackStrips.js`).
- **`transform: X ? [...] : undefined` в стиле — краш** (`_validateTransforms`, `Cannot read property
  'forEach' of null`) — ключ `transform` не должен присутствовать со значением `undefined`, только
  реальным массивом либо отсутствовать целиком. Компоновать через `[base, condition && extra]` в
  массиве стилей, не тернарник на весь `transform`.
- **JS `setTimeout` НЕ синхронизирован с внутренними "часами" зацикленной (`loop:0`) анимации/gif** —
  внешний таймер может опоздать на десятки миллисекунд относительно реального конца цикла, и
  зацикленный ассет успевает показать кадр 0 повторно ДО того, как внешний код успеет скрыть/сменить
  его. Давать запас минимум в 2 кадра (не 1) до конца анимации у любого внешнего таймера, привязанного
  к её длительности.
- **`Animated.Text`/`Animated.Image` НЕ обходят внешний JSX-патч**, если тот матчит компонент по
  `type === Text`/`Image` — `createAnimatedComponent` рендерит НАСТОЯЩИЙ `<Text>`/`<Image>` внутри,
  транспилируемый тем же jsx-рантаймом, патч всё равно срабатывает.
- **CSS `animationKeyframes` (react-native-web) применяется ТОЛЬКО через `StyleSheet.create()`**, не как
  голый инлайн-объект стиля — иначе duration/direction применятся как обычные CSS-свойства, а
  `animation-name` останется `none`, и анимация физически не запустится.

## Известные пробелы / TODO (актуально на 2026-09-24, после сжатия старых записей)

**Открытые фронтовые задачи:**

- **Телепорт бегуна при движении (Android) — считаем ЗАКРЫТЫМ 2026-09-25, после ~15 заходов за
  несколько сессий.** Полная хронология (все промежуточные гипотезы, что не сработало, все живые
  прогоны) — `CLAUDE_DONE_TASKS.md`, записи 2026-09-20/21/24 и главная запись 2026-09-25 ("огромная
  сессия"). Итоговые причины и фиксы:
  1. **Гонка reanimated-таймингов в каскадах** (`RunnerTokenSlide.js`/`SpritePackAnimation.js`
     переведены на reanimated, честный `completeStep`/`onComplete` вместо угаданных таймеров вместо
     `ANIM_DURATION_MS`) — устраняет тот класс, где каскадные шаги (отскок→аномалия→...) теряли
     синхронизацию. Подтверждено живьём.
  2. **Split-пайплайн left/top (React-style) vs transform (reanimated `useAnimatedStyle`)** —
     два независимых конвейера обновления одной вьюхи не гарантированно попадали в один кадр на
     Android → кадр в точке назначения → откат → слайд. Фикс — оба на shared values внутри ОДНОГО
     `useAnimatedStyle`. Подтверждено: "на мок-дороге полностью отсутствуют телепорты и дёргания".
  3. **`move_north`/чистый шаг по прямой играл 1 кадр позы и откатывался в idle** — отдельная,
     более тяжёлая регрессия ОТ фикса (2) в тот же день: колбэк завершения слайда висел только на
     `translateX`, а чистый north/south-шаг не двигает X (`dx===0`) → reanimated `withTiming(0→0)`
     завершается почти мгновенно → колбэк обрывал позу раньше, чем реально едущая ось Y хоть
     что-то показала. Диагностировано живым `adb logcat`/`POSEDBG` (9мс до фикса → ~1.46с после,
     совпадает с длительностью слайда). Фикс — колбэк вешается на ту ось, у которой на старте
     слайда реальная ненулевая дельта. **Подтверждено живьём на Android**, пользователь: "north
     заработал".
  4. Заодно нашли, что `resolveMoveAssetDirection` (`constants/runnerAnimHelpers.js`) умела отдавать
     только 5 из 8 реально существующих в паке направлений (`spritePackStrips.js` содержит честные
     `south`/`east`/`west` move-клипы, никогда не запрашивавшиеся) — добавлены явные ветки для
     DOWN/LEFT_DOWN/RIGHT_DOWN (безопасно-аддитивно, бой их не шлёт, нужны только `MockRoadScreen`).
  `SLIDEDBG`/`WINDBG`/`POSEDBG` логи ОСТАВЛЕНЫ во всех трёх файлах — не убирать, вдруг пригодятся для
  похожего класса бага, но саму эту жалобу больше не преследовать.
  **Хвост, оставшийся НЕ проверенным**: оба фикса (2) и (3) лежат в общем `RunnerTokenSlide.js`,
  использует и настоящая игра, но живой прогон был только на мок-дороге + точечные `adb`-проверки —
  отдельного подтверждения "в реальной партии на бою тоже всё гладко" пока нет, первым делом
  проверить в начале следующей сессии.
- **Мок-дорога (`screens/MockRoadScreen.js`, кнопка "🧪 Тест-дорога" в главном меню, маршрут
  `ROUTES.MOCK_ROAD`) — рабочий инструмент**, добавлен и доведён до стабильного состояния 2026-09-25
  (полная история итераций/багов, найденных при постройке — `CLAUDE_DONE_TASKS.md`). НЕ отдельная
  реализация рендера — гоняет те же `runnerGameReducer`/`handleVersionedRunnerAnimEvent`/
  `handleTransientRunnerAnimEvent`, что и бой, события собирает вручную в той же форме, что шлёт
  Mercure. Возможности: выбор одного из 8 моковых бегунов (2 "игрока" + мяч-препятствие); "Шаг" —
  движение в любую из 6 смежных клеток (честная позиция + честная поза, включая south/east/west, см.
  выше); "Прыжок" — телепорт в любую клетку/сегмент (тест fly); "Стрелять"; мины (треть клеток
  `danger`, взрыв + отброс на клетку); авторазрешение коллизии при посадке на занятую клетку (отброс
  через `COLLISION_AUTO_RESOLVE_MS`); прямой запуск отдельных поз без перемещения; кнопка
  "Оттолкнуть". Все побочные эффекты (коллизия/мина/видимость в окне прокрутки) централизованы в
  ОДНОЙ `runPostMoveEffects`, вызывается из обоих путей перемещения (`applyMove`/`flyTo`) — это и
  даёт настоящие цепные реакции (коллизия→отброс→мина→взрыв→новый отброс→...), а не только
  единичные действия. На вебе доска и панель кнопок — бок о бок (`Platform.OS==='web'`); на native —
  общий `ScrollView`, доска+кнопки вместе (иначе кнопки уезжали за пределы экрана на широких
  раскладках, см. историю).
- Предложение по схеме бэковых событий (`CollisionEvent`/`RunnerSaveEvent` без id участников/причины
  — фронт вынужден гадать эвристикой) подготовлено и отправлено пользователю файлом
  (`backend_event_schema_proposal.md`, был в scratchpad, не в репозитории) — статус "отправлено", не
  "принято бэком".
- Рассматривался и отклонён вопрос "переписать под другой native-стек для Android целиком" (Kotlin/
  Flutter) — несоразмерно относительно точечного перехода компонентов на reanimated, который уже
  устраняет тот же класс проблем существенно дешевле.
- **2026-09-25, ПЯТЫЙ заход по телепорту — новая причина, отдельная от гонки
  reanimated-таймингов (та была устранена заходами 6-9 выше).** Живая жалоба
  ПОСЛЕ перехода на reanimated: паттерн тот же самый ("флеш вперёд → откат
  назад → доигрывает"), но теперь на КАЖДОМ обычном одиночном шаге (не только
  каскадах). Найдено чтением кода `RunnerTokenSlide.js`: `left`/`top` шли
  обычным React-style-пропом (из `x`/`y`, коммитится через Fabric), а
  `transform:translateX/Y` — через reanimated `useAnimatedStyle` (применяется
  на UI-потоке через СОБСТВЕННЫЙ choreographer-маппер, вне обычного React-
  коммита). На Android эти два конвейера обновления одного и того же View НЕ
  гарантированно попадают в один кадр — ровно та же трёхфазная картина, что
  описана в докстринге "Дёрганье" (Fabric уже отрисовал left/top на целевой
  клетке, reanimated ещё не успел применить компенсацию → кадр в точке
  назначения → резкий откат назад, когда компенсация наконец применяется →
  штатный slide). **Исправлено** — `left`/`top` переведены на shared values
  (`targetX`/`targetY`), читаются ВНУТРИ ТОГО ЖЕ `useAnimatedStyle`, что и
  `translateX`/`translateY` — один маппер Reanimated, одно атомарное
  обновление вьюхи за кадр, split между двумя пайплайнами style-пропов
  устранён структурно. **НЕ подтверждено живьём** — правка по чтению кода,
  ждём повторного прогона (шаг Скаутом/Танком, обычное движение без каскада).
- **2026-09-25 — песочница для проверки анимаций без бэка**
  (`screens/MockRoadScreen.js`, кнопка "🧪 Тест-дорога" в главном меню) — по
  прямому запросу пользователя, чтобы гонять move/fly/attack/collision/death
  сколько угодно раз без реальной партии/Docker/бэка. Переиспользует ТЕ ЖЕ
  функции, что и боевой код (`runnerGameReducer`, `handleVersionedRunnerAnimEvent`/
  `handleTransientRunnerAnimEvent` из `lib/runnerAnimTriggers.js`), просто
  события собираются вручную на фронте в той же форме, что шлёт Mercure —
  не отдельная, параллельная реализация рендера. 2 мок-игрока (для коллизий
  разных цветов) + мяч-препятствие на тех же тестовых картах, что и
  `constants/mockGameData.js`. Режимы: "Шаг" (только гекс-соседи, как в бою —
  на этом воспроизводится баг с телепортом), "Прыжок" (любая клетка/сегмент,
  для fly/коллизии), "Стрелять"; плюс прямой запуск отдельных поз
  (attack/gotShot/bomb/start/fly) и кнопка "Оттолкнуть" (не-соседний перенос,
  форсит эвристику отскока — воспроизводит цепочку collision→knockback→shoot
  один в один с боевой). Маршрут `ROUTES.MOCK_ROAD`, доступен из
  `MainMenuScreen`. **Живой баг, найден и исправлен в ТОЙ ЖЕ сессии, чтением
  сообщения пользователя "на вебе на мок-дороге на UI не видно кнопок,
  видимо они внизу за пределами экрана"** — риск, отмеченный сразу при
  создании экрана, подтвердился: `useBoardLayout()` в ландшафтной раскладке
  резервирует под доску почти всю высоту окна (расчёт на настоящую игру, где
  панель игрока стоит СЛЕВА) — этот экран панель не рисует, доска забирала
  весь экран, а панель кнопок (была в СВОЁМ отдельном `ScrollView` СНАРУЖИ
  доски с фиксированной высотой) физически не помещалась и не была
  достижима прокруткой. **Исправлено** — доска и панель кнопок теперь в
  ОДНОМ общем `ScrollView`, кнопки всегда достижимы прокруткой вниз
  независимо от соотношения сторон экрана. **Подтверждено живьём в веб-
  браузере (не в эмуляторе пользователя — своя, отдельная сессия
  claude_debug через обычную регистрацию)**: кнопки видны/доступны после
  фикса, выбор бегуна + тап по подсвеченной клетке ("Шаг") корректно
  проигрывает слайд (SLIDEDBG лог подтвердил перемещение). Сам мок-экран
  и его логика (переиспользование `runnerGameReducer`/`handleVersionedRunnerAnimEvent`)
  работают правильно — баг был чисто в вёрстке, не в анимационной логике.
  **На Android/native ещё не проверялся** — там `useBoardLayout()` (портретная
  ветка) резервирует ФИКСИРОВАННУЮ `panelH` снизу (не всю высоту, как в
  ландшафте) — риск того же класса меньше, но не исключён на очень высоких
  досках (много видимых колонок).
- **Производительность — "тормоза жуткие" на обоих платформах, жалоба 2026-09-24.** Разобрано в этой же
  сессии: (1) веб рендерил токены через тяжёлый спрайт-пак вместо старой GIF-системы — это был прямой
  откат уже принятого 2026-09-20 решения "спрайты только для Android"; **исправлено** — старые GIF-ассеты
  восстановлены из git-истории (`git checkout 13383bb -- src/assets/images/runners/`, коммит
  ПЕРЕД миграцией на спрайты) и `RunnerToken.js` снова делится по платформе (`Platform.OS==='web'` →
  старый одиночный `<Image>` с gif, native → спрайт-пак), подтверждено живым DOM-аудитом на независимом
  веб-сервере. (2) `RunnerToken` не был мемоизирован (в отличие от `RunnerCard`/`FramePanel`/
  `PersonPanel`) — добавлен `React.memo`, безопасный win независимо от остального. (3) На Android живой
  `dumpsys gfxinfo` показал ~48-55% janky-кадров ДАЖЕ В ПОЛНОМ ПРОСТОЕ — бинарные тесты (заморозка
  тикания спрайт-анимации, заморозка параллакс-фона) НЕ изменили цифру, при этом сам эмулятор
  (`qemu-system-x86_64`) на хосте жрёт ~260% CPU даже без действий игрока — сильный сигнал, что текущий
  Android-жанк на ЭТОЙ конкретной машине/сессии в основном НЕ регрессия кода, а контеншн хоста (Docker,
  Gradle daemon, много параллельных процессов). **Не подтверждено окончательно** — нужно: (a) проверить
  веб после восстановления GIF на РЕАЛЬНОМ браузере пользователя (не в тестовом окружении); (b) проверить
  Android на менее загруженном хосте/после перезагрузки, чтобы отделить контеншн от возможной реальной
  регрессии в коде.
- **Подсветка "что делать дальше" (`usePulse`/`PulseHighlight`/`PulseText`, см. архитектуру выше) —
  реализована только для 3 сценариев (выбор бегуна/усиления, Жнец на поле).** SHOOT/ROAD_BONUS/решение
  при столкновении явно отложены на "следующую итерацию" (2026-09-14) тем же механизмом — не начаты.
- **"Collision (звук/поза) проигрывается только один раз"** — живой вопрос пользователя 2026-09-24, НЕ
  разобран. Гипотеза (не проверена) — тот же класс "пара на миг выпадает из `tokenOverlay.items`", что и
  у телепорта, обрывая `onCollisionPoseStart`/`End`.
- **"Анимация start у Атлета — на мгновение виден предыдущий кадр перед idle"** — живой вопрос
  2026-09-24, НЕ разобран. Возможно СВЯЗАНО с тем же классом ремаунт-бага, что и телепорт (см. выше) —
  не проверялось отдельно.
- **Atlet (оба статуса) и Tank/Athlete-damaged в новом спрайт-паке не имеют клипа "start"** (контентный
  пробел самого AI-пака, не код) — откатываются на `idle` без анимации выхода на трассу. У Atlet-healthy
  это уже почищено (ошибочно размеченный ряд в `spritePackStrips.js` переименован в `start`, см.
  `CLAUDE_DONE_TASKS.md`, 2026-09-24), у остальных трёх комбинаций — нет.
- **`PulseHighlight`** (см. архитектуру выше) — оверлей заполняет padding-бокс родителя, у элементов с
  уже своим `borderWidth` это может дать тонкое "двойное кольцо" с внешней рамкой. Известно, не
  проверено живьём, чинится отрицательным отступом при необходимости.
- **Переход бегуна за передний край видимого сегмента не реализован.** Если `positionX` бегуна
  переполняется за пределы 3 загруженных сегментов трассы, клетка для тапа физически не отрисована —
  `forwardNeighbors` (`lib/hexDirection.js`) её просто не включает в подсветку. Нужна отдельная UI-
  аффорданса (кнопка/стрелка «съехать с трассы»), не наступали на это живьём ни разу.
- **ИСПРАВЛЕНО на бэке, статус пересмотрен 2026-09-25**: `RunnerPlayerViewModel` (REST-снапшот,
  `ViewModel/RunnerPlayerViewModel.php:13,32,53`) теперь ОТДАЁТ `activeRunner` — старая заметка
  ("`RunnerPlayer::toArray()` не отдаёт") устарела, перепроверено чтением текущего файла. Раньше поле
  было доступно только из событий (`player_step`/`step_selection`/`ability_*`), теперь REST-снапшот —
  полное зеркало live-состояния и в этой части тоже.
- **Звук смерти через `onStart` (2026-09-25) не проверен живьём в реальном бою** — только логически
  (переиспользован трижды уже подтверждённый в этом же файле паттерн) и сборкой бандла без ошибок.
  Проверить в начале следующей сессии: смерть В КАСКАДЕ (отброс→…→destroyed) должна звучать ПОЗЖЕ,
  синхронно с самой terminal-позой, а не в момент прихода события.
- **Подсветка «усиление применено» у Призрака — живая жалоба 2026-09-26 (не светится сразу после
  выбора у чужого игрока, видел и на Android, и на вебе), НЕ подтверждена повторно.** Прочитан весь
  путь (`ability_ghost`-редьюсер, форма события на бэке, `PLAYER_ABILITIES`, `AbilityZone.js`) —
  асимметрии между Призраком и остальными усилениями не нашёл. Поднял live-репро через 2 свежих
  тестовых аккаунта (Python-скрипт, прямые вызовы REST) — `GET /api/runner_game` сразу после выбора
  корректно отдаёт `ability:"ghost"`, и в РЕАЛЬНОМ веб-клиенте (залогинился одним тестовым аккаунтом,
  переключился на панель другого через свитчер) плитка «Призрак» подсветилась ПРАВИЛЬНО. Пользователь
  прервал дальнейшую проверку («сам посмотрю»). Вывод на сейчас — предположительно разовый сбой
  live-синка в ТОЙ КОНКРЕТНОЙ партии (не проверяли, помог бы resync/F5), не воспроизводимый баг кода —
  но статус не закрыт, ждёт самостоятельной проверки пользователем.
- **Прилёт Жнеца из-за края карты у ОППОНЕНТА (2026-09-26) — фикс НЕ проверен живьём.** Причина
  найдена чтением кода: анимация "прилёта" — целиком локальная иллюзия (`BoardGrid.js#
  reaperPreviewItem`, фейковый токен с `enterFrom` за пределами доски), существующая только на
  клиенте того, кто ставит Жнеца (`GameBoardScreen.js#reaperPreview`, `null` для всех остальных).
  Настоящий токен Жнеца у ОСТАЛЬНЫХ клиентов монтировался первый раз БЕЗ `enterFrom` (`
  lastKnownCellRef` ещё пуст) — слайду ехать неоткуда, токен просто появлялся на месте. Добавлена та
  же формула off-map `enterFrom` (+ увеличенная `REAPER_PREVIEW_SLIDE_MS`-длительность, для
  визуального единообразия с тем, что уже видел разместивший игрок) для РЕАЛЬНОГО токена при его
  самом первом появлении конкретно для Жнеца (`kind==='start'`+`side` в `runnerAnims`) — эта ветка
  структурно не может сработать для самого разместившего игрока (у него анимация подавлена
  `reaperStartSkipUntilRef`, `runnerAnims[id]` для Жнеца никогда не получает `kind:'start'` на его
  собственном клиенте), значит новый код влияет ТОЛЬКО на клиентов-наблюдателей. Собрано (бандл без
  ошибок), в реальной 2-игровой партии не проверено — нужен второй клиент/устройство.
- **Веб: остальные gif-анимации бегунов (кроме idle и collision) всё ещё на старом темпе 200мс/кадр
  (живая жалоба 2026-09-26 — "collision выглядит медленно", аудит вскрыл, что это не единичный случай).**
  2026-09-25 ускорили ТОЛЬКО 29 idle-gif (200→120мс/кадр, под темп спрайт-пака на Android). В этой
  сессии аудитом (`PIL`, читал байты delay/GCE напрямую) подтверждено: ВСЕ 615 остальных gif-файлов
  (move ×5 направлений, attack ×3, got_shot, fly, start, destroyed, acid, burn, heal, broken, bomb —
  для scout/tank/athlete healthy+damaged + drone + мяч) по-прежнему 200мс/кадр. `collision` (east/west,
  50 файлов, все типы+статусы+цвета+мяч) — пофикшено СРАЗУ в этой сессии, тем же приёмом (`21 F9 04`
  GCE-байты delay патчатся напрямую, БЕЗ перекодирования кадров, точь-в-точь как для idle) — это
  оказалось безопасно независимо от остального, т.к. `collision` НЕ идёт через очередь/таймер
  (`useRunnerAnimations.js:143`: "'collision' сюда НЕ входит — derived-состояние, вычисляется прямо в
  `BoardGrid` по факту '2 бегуна на одной клетке'"), в отличие от остальных транзиентных поз. **Не
  проверено живьём** (только PIL-верификация + чистая пересборка бандла).
  **Остальные 615 файлов НЕ тронуты — нужно решение пользователя, т.к. риск разный по группам:**
  1. `move`/`fly` — на ОБЕИХ платформах есть РЕАЛЬНЫЙ сигнал завершения (`RunnerTokenSlide#onSlideEnd`,
     не зависит от длины gif) — `ANIM_DURATION_MS.move/fly` (`useRunnerAnimations.js:77`) лишь
     страховочный таймер на случай, если сигнал не пришёл. Ретайм этих gif низкорискован.
  2. `attack`/`gotShot`/`start`/`bomb`/`heal` — на ВЕБЕ у этих поз НЕТ реального сигнала завершения
     вообще (gif не даёт колбэк "доиграл", `SpritePackAnimation#onComplete` — только native) — ЕДИНСТВЕННЫЙ
     механизм переключения обратно на idle на вебе — угаданный `ANIM_DURATION_MS[kind]`, подобранный
     под ТЕКУЩУЮ (200мс/кадр) длину конкретного gif с запасом в 2 кадра (см. докстринг там же, история
     багов "gif зацикливается и на миг снова показывает 1-й кадр раньше, чем мы переключим на idle" —
     ловили это уже трижды на других позах). Ретайм этих gif БЕЗ пересчёта соответствующей константы
     воспроизведёт ровно тот же класс бага.
  3. `destroyed`/`burn`/`acid` — терминальные позы, свой отдельный таймер (`DESTROYED_HIDE_DELAY_MS`/
     `TERMINAL_HIDE_DELAY_MS`, та же логика "текущая длина gif минус 2 кадра запаса") — тот же риск,
     что и группа 2.
  4. `broken` — статус-переход (healthy→damaged), нужно проверить, завязан ли на похожий таймер отдельно
     (не проверял в этом заходе).
  **Решение пользователя (2026-09-26): не трогать остальные группы сейчас** — оставлены на 200мс/кадр
  как есть, пересчёт констант делать только по отдельному запросу.
- **Найден и пофикшен РЕАЛЬНЫЙ баг из группы 2 выше (2026-09-26, живая жалоба на вебе): `destroyed` и
  `bomb` — таймеры без запаса, "флеш" первого кадра gif.** Скаут закончил ход на клетке Жнеца (bomb-
  ловушка) → у пользователя: "скаут мигнул, доиграл destroy, потом снова мелькнул первый кадр". Причина:
  `DESTROYED_HIDE_DELAY_MS` (`useRunnerAnimations.js`) была РОВНО 1800мс — это ТОЧНАЯ полная длина
  gif (9 кадров×200мс), БЕЗ 2-кадрового запаса, который acid/burn/start уже получили в 2026-09-12/13 —
  именно 'destroyed' тогда пропустили (свой отдельный HIDE-путь, не revert-to-idle). Пока веб был на
  `SpritePackAnimation` (2026-09-23/24, см. запись ниже "тормоза жуткие") баг физически не мог
  проявиться (там настоящий `onComplete`, не угаданный таймер) — вернулся, когда веб откатили обратно
  на gif. Заодно нашёл АБСОЛЮТНО ТУ ЖЕ дыру у `bomb` (Жнец играет эту позу У СЕБЯ в той же самой
  ловушке — `ANIM_DURATION_MS.bomb` тоже была РОВНО 1800мс = 9×200мс, без запаса). Оба пофикшены тем же
  запасом 400мс: `DESTROYED_HIDE_DELAY_MS` 1800→1400, `ANIM_DURATION_MS.bomb` 1800→1400. **Побочно
  найдено, НЕ тронуто**: `ANIM_DURATION_MS.heal` = 2200, а реальная длина `heal`-gif — 1800 (9×200) —
  ПРОТИВОПОЛОЖНАЯ асимметрия (константа БОЛЬШЕ файла, не меньше) — не даёт "флеш"-бага (переключение
  происходит уже ПОСЛЕ начала второго цикла, не в момент его начала), просто анимация лишний раз
  чуть-чуть "залипает" на 400мс дольше, чем могла бы — не трогал, не тот класс проблемы и не жаловались.
  Собрано (бандл открылся без ошибок в консоли), живого повторного прогона сценария "Жнец-ловушка" на
  вебе после фикса — не было.
- **Движение НА клетку, где уже кто-то стоит (Призрак), — fly вместо move (живая жалоба 2026-09-26).**
  Симметричный баг к "уходу С клетки" (см. выше, тот уже пофикшен через pushedFrom/ghostPairs) — но
  ПРИХОД на такую клетку по чтению кода вообще не должен зависеть ни от pushedFrom (та проверка смотрит
  только на СТАРУЮ клетку мовера, не на клетку назначения), ни от geometrической проверки `forwardNeighbors`
  (та не учитывает занятость вообще) — значит причина не найдена чтением кода, нужен живой повтор с
  уточнением: чья именно анимация была fly (зашедшего или уже стоявшего) и остался ли "залетевший" на
  месте или физически переместился. Пользователь прервал разбор ("забей пока") — не закрыто, вернуться
  при следующей жалобе на эту тему.
- **Анимация выстрела (attack) сыграла 4 раза подряд на веб при одном клике "стрелять" — разовое
  явление, повторно не воспроизвелось (живая жалоба 2026-09-26).** Гипотеза (не подтверждена): у
  транзиентных событий (`step_shoot` и другие без `version`, см. `useMercure.js#handleEvent`) НЕТ
  дедупликации вообще, в отличие от версионных (`e.version <= cur` → игнор) — если по какой-то причине
  (двойная SSE-доставка, гонка реконнекта) один и тот же транзиент долетит несколько раз, он
  безусловно переиграется несколько раз (каждый — свой шаг в очереди `useRunnerAnimations`). Не удалось
  подтвердить живьём (не воспроизвелось повторно, не успели проверить, дублировался ли заодно и звук).
  Не чинить вслепую — ждать следующего воспроизведения с уточнением деталей.
- **Накат иногда (нестабильно, не каждый раз) играет fly вместо move — живая жалоба 2026-09-26, НЕ
  подтверждена окончательно.** Проверены и ОТВЕРГНУТЫ гипотезы: коллизия (накат физически не может
  идти во время неразрешённого столкновения — то блокирует ВСЕ действия игрока), недавний выстрел
  (`wasJustShot`/`RECENTLY_SHOT_TTL_MS` — пользователь подтвердил, что не стреляли). Найдена, но НЕ
  подтверждена гонкой: накат — ЕДИНСТВЕННОЕ действие, которое шлёт ДВА `runner_save` подряд за один
  вызов (`RunnerRollService::run()`, read-only бэк — сначала "пустой" save со СТАРОЙ позицией, только
  сбрасывает `rollDice`, ПОТОМ настоящий save с новой позицией через `Orchestrator`). Если эти два
  события изредка долетают/обрабатываются не в том порядке (сетевой джиттер) — фронт увидел бы
  "позиция откатилась назад", геометрически похоже на отскок → `fly`. Совпадает с наблюдением
  пользователя, что баг воспроизводится НЕ каждый раз (гонка по определению нестабильна) — то же самое,
  возможно, объясняет и нерешённую загадку "приход на клетку с Призраком" выше. НЕ чинить вслепую без
  живого лога (`lib/logger.js` уже пишет каждое входящее SSE-событие с версией) — нужно поймать реальный
  порядок событий в момент бага, прежде чем решать, как чинить (дедуп по версии на фронте? гарантия
  порядка на бэке?).
- **Торчащие углы у FramePanel (аватарки/кубики/усиления) — подтверждено ТОЛЬКО у плиток бегунов
  (PersonPanel), FramePanel не проверен живьём.** Тот же приём (промеренный радиус клипа) применён и
  туда, отдельным замером под её собственный ассет (`frame_panel_top_corner.png`) — но нижний уголок
  этого набора (`frame_panel_bottom_corner.png`) оказался ДРУГИМ по конструкции (диагональный
  светящийся росчерк-акцент, не трасса по контуру угла) — просвет остаётся с ОБЕИХ сторон даже на
  большом радиусе, круговой CSS-клип его контур принципиально не повторяет. Ждёт живой проверки —
  возможно, для нижних углов аватарок/кубиков/усилений потребуется либо смириться с небольшим
  остаточным огрызком, либо новый арт-ассет (не код).
- **1px-смещения рамок на вебе — закрыто пользователем БЕЗ фикса (2026-09-26), не подтверждено что
  устранено, просто перестали преследовать.** Три симптома (чёрные поля вокруг рамки аватарки/усиления
  со всех сторон, вертикальные стенки рамки Жнеца смещены на 1px внутрь, нижняя грань рамки Скаута/
  Атлета поднята на 1px) — НЕ воспроизвёл ни точным замером `getBoundingClientRect()`, ни живым
  скриншотом при 400-600% zoom браузера (см. CLAUDE_DONE_TASKS.md за детали диагностики и временный
  отладочный экран). Гипотеза (не подтверждена) — зависит от `devicePixelRatio`/масштаба страницы на
  конкретном экране пользователя. Если всплывёт снова — нужен масштаб браузера/`devicePixelRatio`
  пользователя, замер на другом экране бесполезен, раз не воспроизводится.
**Известные бэковые баги (read-only — не трогать, только сообщать бэкенд-разработчику):**

- **`ReaperService::validateCell()` — СТАТУС ПЕРЕСМОТРЕН 2026-09-25**: старая заметка утверждала
  сравнение `Cell::getType()` (`string`) с `RoadType::WALL`/`ANOMALY` через `===` (enum-кейс никогда не
  равен строке). При повторном чтении текущего кода (`Validator/Ability/ReaperValidator.php:31-42`) —
  НЕ воспроизведено: там строгий `in_array($cell->getType(), [RoadType::ROAD->value, ...->value,
  ...->value], true)`, `Cell::getType(): string` подтверждённо возвращает строку — выглядит корректно.
  Либо бэк это уже починил, либо старая заметка изначально была неточной — не проверялось живым тестом
  (поставить Жнеца на клетку `wall`), только чтением. Фронт по-прежнему сам дублирует проверку
  проходимости/занятости при подсветке легальных мест (не вредно само по себе), но AS-IS считать этот
  пункт активным багом больше нельзя без свежей живой проверки.
- `POST /runner_game/ability` с `{"accept":false}` БЕЗ ключа `details` падает `500` —
  `AbilityDto::$details` (`DTO/AbilityDto.php:14`) типизировано `?AbilityDetailsBase` БЕЗ дефолта
  `= null`, при отсутствии ключа в JSON свойство остаётся неинициализированным. Подтверждено чтением
  текущего файла 2026-09-25 (не только по старой заметке). Фронт учитывает —
  `api/runnerGame.js#ability(accept, details = null)`, никогда `undefined`.
- `Collision::ballCollision()` в ветке "Мяч инициатор, реальный игрок крупнее" передаёт `lowRunner`/
  `topRunner` в порядке, который выглядит перевёрнутым относительно семантики "TOP — крупная сторона" —
  влияет только на редкий edge-case (`saveCollision`'s ранний return при уже DESTROYED/BROKEN раннере).
- `RunnerPlayerViewModel::$step` типизирован `?string` вместо `?int` (коммит `59aea0f`, 2026-09-24) —
  REST-снапшот отдаёт `step` строкой вместо числа, ломая строгие сравнения. Фронт защитно приводит через
  `Number(...)` в `api/normalize.js#normalizeRunnerGame` — работает независимо от того, поправят ли бэк.
- `DirectionService::run()` даёt LEFT_UP/RIGHT_UP чисто боковым шагом на чётном `positionX` и диагональю
  на нечётном — фронт (`lib/hexDirection.js#neighborPosition`) больше НЕ зеркалит эту чётность (по
  прямому запросу пользователя — стрелять/подсвечивать только строго вперёд, независимо от чётности).
  Остаточный риск: если бэк применит чётный ход как боковой (не диагональ), после тапа бегун может на
  миг визуально разойтись с подсвеченной клеткой — расхождение само исчезает на следующем live-событии.
- **Первый выход из резерва на трассу НЕ проверяет занятость стартовой клетки — столкновение вообще не
  срабатывает (найдено 2026-09-26, живая жалоба пользователя).** `StepMoveService::run()` для этого
  случая (`$onStart`, `Runner::positionX/positionY/segment === null`) вызывает
  `RunnerStartMoveService::run()` (`Service/Runner/RunnerStartMoveService.php:18-28`) НАПРЯМУЮ, в обход
  `Orchestrator`/`Move::handle()` — просто пишет `positionX=0/positionY=$firstPosition/segment=0` и
  сохраняет, ни разу не обращаясь к `TrackState`/`Cell::getRunner()`. `StepMoveValidator::diceValidate()`
  при `$onStart` тоже не проверяет занятость лейна. Обычный ход (не первый) идёт другим путём —
  `Orchestrator::start()` → `Move::handle()`, который в конце проверяет `$cell->getRunner()` и при
  занятости кладёт `Action::TYPE_COLLISION` → `Collision::handle()` → `extraTurnPlayer`/баннер — именно
  этого шага нет в `RunnerStartMoveService`. Итог: два бегуна (в репро — Танк и Скаут, оппонентов) могут
  занять ОДНУ И ТУ ЖЕ стартовую клетку одновременно и мирно сосуществовать на ней бесконечно — `game.
  extraTurnPlayer` не выставляется никогда, баннер выбора LOWER/TOP не появляется НИ У КОГО, игра
  продолжается штатно (подсветка ходов у следующего игрока). Поза "два бегуна рядом", которую видит
  игрок — чисто фронтовая (`BoardGrid#tokenOverlay`/`pushPair`), она рисуется всегда, когда видит
  совпадение позиций, независимо от того, разрулил ли бэк столкновение по правилам. Фронт тут ничего не
  чинит (read-only зона) — сообщено пользователю, ждёт передачи бэкенд-разработчику.
- **Хвост, оставшийся НЕ проверенным**: три фикса 2026-09-26 (`ghost_consumed` в редьюсере,
  `trackNext` в патче `game_track_updated`, `ghostPairs.js` переписан под реальное событие) —
  подтверждены только чтением кода и чистой сборкой бандла (веб, регистрация/логин/логаут живьём
  проверены, партии — нет). Проверить в начале следующей сессии в реальном бою: (a) Призрак реально
  переключает `ability` в `unghost` при повторном взаимодействии сразу после расхода; (b) "пик" 4-го
  фрагмента трассы (`flattenPeekColumn`) обновляется, а не залипает, после живого сдвига трассы;
  (c) пара бегунов при Призраке визуально рисуется как мирное сосуществование (solo/solo), а не
  коллизионная поза. Детали расследования и все ссылки на файлы/строки — `CLAUDE_DONE_TASKS.md`.
- Транзиентные "эхо" события `step_road_bonus`/`step_collision`/`step_ability` — принимаются, но
  используются ТОЛЬКО в `lib/eventLog.js` (дебаг-лог), не дают ни звука, ни анимации сами по себе —
  реальный эффект каждого приходит другими, отдельными событиями (`runner_save`/`ability_*`). Не баг,
  просто чисто зафиксировать: эти три события фактически ничего не делают на клиенте.

**Мелкие заметки:**

- `config/env.js`: `LAN_IP` — плейсхолдер, для теста на физическом устройстве подставлять реальный IP
  вручную (закомментированная строка в `devHost()`).
- `Runner::toArray()`/`RunnerViewModel` по-прежнему не отдаёт тип жетона повреждения — фронт больше НЕ
  копит его из истории событий (это теряло состояние при reconnect), а вычисляет напрямую из
  `runner.status` (`RunnerCard.js#damageSlotsFromStatus`, см. `CLAUDE_DONE_TASKS.md`, 2026-09-24) —
  `lib/runnerDamageTokens.js`/`hooks/useRunnerDamageTokens.js` остались в репозитории, но их больше
  никто не читает (мёртвый код, не тронут — не было отдельного запроса вычищать).
- **`POST /api/lobby/send` (чат) есть и в контракте, и в `api/lobby.js#send`, но ни одна кнопка/форма
  её не вызывает** — чата в UI нет вообще, ни отправки, ни отображения `chat_message`. Обнаружено
  аудитом 2026-09-26. Разобрано, что это НЕ свободный текст — `POST /api/lobby/send` принимает индекс
  0-6 (`SendLobbyDto::$message: int`), бэк валидирует его против фиксированного enum'а из 7 слотов
  (`Enum/LobbyMessage.php` — `message0`...`message6`, буквально плейсхолдерные строки, никакого
  реального текста бэк не хранит НИГДЕ — ни в БД, ни в translations, ни в fixtures, проверено). Если
  делать — это "колесо быстрых фраз" (7 кнопок с заранее заданными на фронте текстами), не чат с вводом
  текста; сами фразы и способ показа (баббл над игроком/лента) — ждут решения пользователя, не начато.

## Заметки

<!-- Веди историю здесь: дата — что сделано / что узнал / на что наткнулся. Не удаляй старые записи
     без архивации: когда этот раздел разрастётся сверх ~2-3 недель активной работы (файл ощутимо
     растягивает контекст каждой новой сессии — CLAUDE.md грузится ЦЕЛИКОМ как project instructions),
     перенеси записи старше 2-3 недель в CLAUDE_ARCHIVE.md (тем же приёмом, что уже сделан 2026-09-24 —
     см. заголовок архива) и оставь здесь только последние недели + этот комментарий. -->

**Записи с самого начала проекта по 2026-09-09 включительно** — в [CLAUDE_ARCHIVE.md](CLAUDE_ARCHIVE.md)
(полный текст, старым способом). **Записи 2026-09-10 — 2026-09-24** — полный текст в
[CLAUDE_DONE_TASKS.md](CLAUDE_DONE_TASKS.md), здесь ниже — только однострочные итоги (сжато 2026-09-24,
см. «Дисциплина ведения CLAUDE.md» в начале файла). Читать оба файла только если нужна полная хронология
конкретного бага/находки — для повседневной работы обычно достаточно однострочников ниже + «Ключевые
архитектурные решения»/TODO выше.

- 2026-09-10 — сдвиг фрагмента трассы: исчезновение старого фрагмента переведено с единого фейда на
  посегментную волну (симметрично появлению нового), обе волны замедлены на ~2с.
- 2026-09-10 — инфраструктура: порт 8080 `nginx` снова молча отвалился (тот же известный Docker/Windows
  баг, что и 2026-09-02) → `docker compose restart nginx`, без перезагрузки машины.
- 2026-09-10 — единая точка определения "куда вести игрока после логина" (лобби/партия/меню) через
  `GET /api/me` (новый `api/me.js`) вместо двух отдельных вызовов в `MainMenuScreen`.
- 2026-09-10 — первая перекраска бегунов под цвет игрока усилена (−25%L/+20%S) → уже на следующий день
  полностью заменена более насыщенной неоновой палитрой (см. 2026-09-11 ниже).
- 2026-09-10 — столкновение с Мячом ошибочно всегда авторазрешалось без выбора игрока → починено, выбор
  "Использовать/Перебросить" теперь есть всегда при разных размерах, как у обычного столкновения.
- 2026-09-10 — баннер столкновения не показывал результат уже брошенного кубика → добавлена вторая
  строка (какой бегун — LOWER/TOP — сдвинется и в каком направлении).
- 2026-09-10 — "коллизия бесконечно повторяется" + спам `NotAllowedError` на вебе → фикс логики
  удержания коллизионной пары (`collisionHoldsRef` в `BoardGrid.js`).
- 2026-09-11 — большой день: фикс рассинхрона позы/движения при переброске; полная замена палитры
  цветов игрока (финал: red `#ff2d55`, blue `#8b2fe0`, green — намеренно ТОТ ЖЕ цвет, что blue, yellow
  `#ff8c00`@45% альфы); починка Android-сборки (потерянный `debug.keystore` + порченный композитинг
  эмулятора, лечится `adb reboot`); переименование лейблов Танк/Атлет; компактный диалог столкновения;
  кроссфейд анимаций отключён на вебе; найден и почищен телепорт при multi-hop отбросе за край карты
  (`lastKnownCellRef`/`enterFrom` в `BoardGrid.js`).
- 2026-09-11 — фоновая музыка активной партии (`constants/backgroundMusic.js`, громкость в итоге 0.2).
- 2026-09-11 — звуки-комментарии партии: start/destroyed/lost/collision/anomalyHole
  (`constants/commentSounds.js`).
- 2026-09-12 — подсказки на спиннерах (`components/ui/LoadingTip.js`); найден и починен реальный баг
  громкости фоновой музыки на вебе (`AudioPlayer.replace()` сбрасывал `volume` на вебе обратно на 1.0).
- 2026-09-12 — паразитная 'fly'-анимация у выживших бегунов при сдвиге трассы (перенумерация `segment`
  ошибочно принималась за реальное перемещение) → почищено.
- 2026-09-12 — дедупликация автонаката иногда пропускала второй накат подряд (ключ строился по
  неуникальному `rollDice`) → переведено на монотонный `rollMoves`.
- 2026-09-12 — звук старта партии иногда терялся из-за гонки REST-снапшота/событий → добавлен фолбэк по
  факту смены статуса WAITING→ACTIVE (`sawWaitingRef`).
- 2026-09-12 — самый большой заход дня: подключены новые анимации (heal/burn/acid), с нуля собрана и в
  тот же день/на следующий ПОЛНОСТЬЮ УДАЛЕНА механика "существо на клетке-стене" (Death NPC) — по итогу
  клетка типа wall просто показывает acid/burn-текстуру, терминальную позу берёт сам бегун (см. также
  2026-09-13/09-18 ниже, механизм менялся ещё дважды).
- 2026-09-13 — кастомный шрифт + глобальная перекраска текста через jsx-патч в `App.js`; ребрендинг на
  "Star Runners"; общий `components/ui/LoadingCard.js`; реальный фикс "дёрганья" слайда токена
  (`RunnerTokenSlide.js` — `translate.addListener`+суммирование дельты вместо перезаписи).
- 2026-09-13 — найден подтверждённый бэковый баг: `auto_generate_proxy_classes: true` (Doctrine) вызывал
  гонку прокси-классов под параллельной нагрузкой → объясняет "Android теряет синхронизацию"/"не находит
  активную игру". Рекомендация дана бэку, фронт не при чём.
- 2026-09-13 — жалоба "wall_acid всегда один и тот же файл" → реальный баг в математике хэша
  `pickDeathVariant` → почищено (сам механизм позже полностью заменён 2026-09-18 явными типами fire/acid).
- 2026-09-13 — три фикса анимаций бегунов: наслоение move→idle (кроссфейд ошибочно применялся и к
  переходу в idle), мигание первого кадра сразу после `start` (запас в 2 кадра до конца цикла gif),
  несовпадение координат scout между start/idle (несостыковка в исходном арте, не код — пользователь
  переделал анимацию сам).
- 2026-09-14 — постоянное логирование по всему фронту (`lib/logger.js`) — попутно поймало 2 реальных
  бага: уже известную Doctrine-гонку и НОВЫЙ — фронт стучался на `/token/refresh` вместо реального
  `/refresh` (разлогинивало пользователя каждый час, когда обновлялся access-токен) → почищено.
- 2026-09-14 — глобальный циан для текста убран почти отовсюду, оставлен только на тайтле "Star
  Runners" (раньше форсировался jsx-патчем на КАЖДЫЙ текст в приложении).
- 2026-09-14 — подсветка "что делать дальше" во время своего хода (`usePulse`/`PulseHighlight`/
  `PulseText`) для 3 сценариев (выбор бегуна, выбор усиления, Жнец на поле) — SHOOT/ROAD_BONUS/решение
  при столкновении сознательно отложены на потом (см. TODO выше).
- 2026-09-14 — "Кубики" не пульсировали на Android → `Animated.Text` НЕ обходит глобальный jsx-патч
  (тот матчит по `type===Text`, а `createAnimatedComponent` рендерит настоящий `<Text>` внутри) → фикс
  через `noGlobalTint`.
- 2026-09-15 — диагностирован реальный тихий обрыв SSE на Android (транспорт умирает без единого
  `error`) → добавлен `heartbeat` на бэке (Mercure) + полностью переписан транспорт `lib/eventSource.js`
  (свой SSE-парсер: `fetch`/`ReadableStream` на вебе, `XMLHttpRequest` на native).
- 2026-09-15 — краш `connectAnimatedNodes` сразу после тапа по клетке хода → halo вокруг выбранного
  бегуна теперь ВСЕГДА смонтирован (видимость через `opacity`), не условный JSX-рендер.
- 2026-09-18 — адаптация бэковых коммитов: тип клетки `wall` выведен из реальных треков (заменён явными
  `fire`/`acid`), `RunnerDestroyEvent` — более узкий payload + поле `reason` (заменяет клиентские
  эвристики для терминальной позы гибели/ловушки Жнеца, см. архитектуру выше); плюс кнопка кика игрока
  из лобби (`LobbyScreen.js`).
- 2026-09-18 — самая существенная правка бэкового пула: `POST /runner_game/start` удалён (партия
  рождается сразу `ACTIVE`) → убран весь фронтовый ready-up-гейт партии, звук старта переведён на явный
  флаг навигации `justStarted`.
- 2026-09-19 — найден и починен реальный race condition: SSE-подписка устанавливалась ДО того, как REST
  успевал освежить куку `mercureAuthorization`, из-за чего "холодное" подключение молча теряло ВСЕ
  приватные события до случайного реконнекта → `connect()` в `useMercure.js` теперь ждёт `sync()` перед
  созданием `EventSource`. Отдельно подтверждено: "чёрный экран" — известная порча GPU-композитинга
  эмулятора, не код (лечится `adb reboot`).
- 2026-09-19 — звук промаха выстрела (`comments/miss`).
- 2026-09-19 — переверстка карточки бегуна доведена до конца: честная 9-slice сборка `PersonPanel`/
  `FramePanel` через новый общий `components/ui/TiledImage.js` — по пути найдены и исправлены 2 общих
  Android-бага рендеринга (см. «Известные грабли RN/Android» выше).
- 2026-09-19 — музыка главного меню/лобби (`hooks/useMenuMusic.js`, живёт выше `NavigationContainer`);
  звук рикошета (`comments/ricochet`).
- 2026-09-19 — рамка карточки бегуна была меньше самой карточки (баг двойного вычитания паддинга) →
  почищено; серия мелких правок размера/раскладки панели по ходу диалога; найдена и почищена настоящая
  причина тормозов на вебе того дня — `usePulse` был JS-driven Animated (→ переведён на CSS
  `@keyframes` на вебе) и `dragGhost`-state на КАЖДЫЙ pointermove при драге кубика (→ переведён на
  `Animated.ValueXY`, в обход React-реконсиляции).
- 2026-09-20 — длинный день по панели игрока: убран текст из фреймов усилений на Android; новая плитка
  Жнеца (`components/game/ReaperCard.js`); финальная вертикальная раскладка 3 плиток бегунов в ряд
  (чтобы никогда не было прокрутки); бейдж бонуса хода + отдельный счётчик накатов; разбор 4 регрессий
  одним сообщением (двойной тап/кубики за рамкой усилений/порядок звуков коллизия-аномалия — почищены;
  "регистрация урона green почти никогда не становится red" — тогда не найдено, вероятно решено косвенно
  2026-09-24 переходом на вычисление жетонов из статуса, см. ниже); реальный краш
  `AudioPlayer.replace(undefined)` при выходе индекса за пределы плейлиста меню (2 трека вместо 4 у
  игровой музыки) → почищено.
- 2026-09-20/21 — начат прототип перехода с gif на спрайт-лист для Android (борьба с decode-паузой/
  бликами при смене анимации) → впоследствии ПОЛНОСТЬЮ ЗАМЕНЁН 2026-09-23 готовым AI-паком; сам
  прототип (`SpriteSheetAnimation.js`/`scoutSpriteSheets.js`) остался в репозитории неиспользуемым.
- 2026-09-21 — 2026-09-23 — новый AI-сгенерированный спрайт-пак (`src/assets/sprites/`) перекрашен под
  все 4 цвета игрока для всех 5 типов (техника — colorize по luma с яркостной кривой, пороги неона/огня
  подобраны заново живыми сравнениями) — несколько дней итераций по каждому типу/цвету/damage-меткам,
  точные формулы и пороги — см. `CLAUDE_DONE_TASKS.md`.
- 2026-09-23 — новый спрайт-пак интегрирован в игру (`constants/spritePacks.js`+
  `components/ui/SpritePackAnimation.js`), `RunnerToken.js` переведён ЦЕЛИКОМ на него для всех типов и
  платформ (позже, 2026-09-24 этой же сессией — веб откачен обратно на старый gif, см. TODO выше про
  "тормоза жуткие"); попутно найден и починен build-breaking баг (импорт из файла-реестра ассетов тянет
  ВСЕ его `require()` целиком, даже неиспользуемые — чистые хелперы вынесены в
  `constants/runnerAnimHelpers.js`).
- 2026-09-24 — длинное расследование размытости спрайт-пака на Android (несколько ложных гипотез: кэш
  устройства, DP/физические пиксели, GPU-`transform`) → настоящая причина: ОГРОМНЫЙ многострочный
  PNG-лист (до 3068×4956) сам по себе рендерится размыто на Android независимо от способа
  масштабирования → фикс — нарезка на 565 отдельных PNG-полосок (`constants/spritePackStrips.js`), по
  одной анимации на файл. Подтверждено пользователем живьём ("стало круто").
- 2026-09-24 — GIF-аватарки в панели игрока вместо статичного кадра спрайт-пака
  (`constants/avatarGifs.js`); жетоны повреждения переведены с накопления по истории событий (терялось
  при reconnect) на прямое вычисление из `runner.status`.
- 2026-09-24 — адаптация бэковых коммитов `59aea0f`/`761b382`: бейдж бонуса хода (`trackGain`) теперь
  виден на любом бегуне в любой момент, не только доли секунды на активном шаге игрока.
- 2026-09-24 — живой инцидент "Твой ход есть, а подсказки нет и кубики не таскаются" — две независимые
  причины: CORS (веб-порт не в allowlist Mercure, чистая инфраструктура) + реальный бэковый баг типа
  (`step` приходил строкой вместо числа) → защитный `Number(...)` на границе разбора снапшота. Плюс
  отдельно: анимация `start` иногда стартовала не с первого кадра → `useLayoutEffect`+новый проп `loop`
  (транзиентные позы теперь играются РОВНО один раз, не зацикливаются).
- 2026-09-24 — телепорт бегуна в начале движения: реализована прогрузка-перед-показом кадра
  (`Image.prefetch`, без мигания при смене source) — сам позиционный телепорт остаётся ОТКРЫТЫМ (см.
  TODO выше, несколько гипотез не подтвердились: "прогрев" Animated-графа, допуск `RESIZE_EPS`).
- 2026-09-25 — телепорт бегуна ЗАКРЫТ: split left/top(React)/transform(reanimated) объединён в один
  `useAnimatedStyle` → подтверждено живьём (мок-дорога, "телепорты пропали"). Детали — CLAUDE_DONE_TASKS.md.
- 2026-09-25 — та же правка вскрыла отдельную регрессию: `move_north`/чистый шаг по прямой играл 1
  кадр позы и откатывался в idle (колбэк слайда висел только на X, а прямой шаг не двигает X) → фикс
  — колбэк на ту ось, у которой реальная дельта. Подтверждено `adb logcat` + пользователем на Android.
- 2026-09-25 — построена песочница `screens/MockRoadScreen.js` ("🧪 Тест-дорога" в меню) — те же
  `runnerGameReducer`/`handleVersionedRunnerAnimEvent`, что и бой, события собираются вручную, без
  бэка. Несколько итераций: раскладка веб бок-о-бок, мины, движение в 6 направлений, авторазрешение
  коллизии, централизованная цепочка побочных эффектов (`runPostMoveEffects`).
- 2026-09-25 — `resolveMoveAssetDirection` дополнена ветками DOWN/LEFT_DOWN/RIGHT_DOWN
  (south/west/east) — пак содержал эти клипы, но функция их никогда не запрашивала; аддитивно,
  бой их не шлёт.
- 2026-09-25 — живая жалоба "коллизия с Мячом не показала позу, сразу отбросило" разобрана как
  ОСОБЕННОСТЬ (не регрессия): весь путь ход→danger→спавн Мяча→collision→итог уместился в 34мс одним
  пакетом с бэка, клиент не увидел промежуточного кадра "оба на клетке".
- 2026-09-25 — визуальные правки по прямым запросам, обе ТОЛЬКО на Android (веб не тронут): скаут
  сдвинут на 10% высоты сегмента ниже (везде, включая коллизию — при паре трансформ мёрджится с
  `innerOffsetX`, не перезаписывает его, см. `BoardGrid.js`) и увеличен на +25% сверх общего
  множителя (остальные типы размера — без изменений, после отката более раннего "для всех").
- 2026-09-25 — инфраструктурный самозамес: `adb forward tcp:8081 tcp:8081` занял порт вместе с
  реальным Metro (`node.exe`), веб-версия перестала открываться → убил зависший `adb.exe`, порт
  освобождён, веб снова работает; Android-приложение принудительно перезапущено, чтобы гарантированно
  подтянуть все правки дня.
- 2026-09-25 (новая сессия) — коллизия одинаковых по размеру не показывала позу столкновения →
  `wait→fly` каскад проигравшего уезжал синхронно в ОДНОМ React-батче с settle победителя (событие
  авторазрешения почти всегда одним тиком) → фикс через `requestAnimationFrame` в
  `lib/runnerAnimTriggers.js`, подтверждено изолированным Node-тестом (см. CLAUDE_DONE_TASKS.md) И
  живьём пользователем ("сработало").
- 2026-09-25 — idle-анимация на вебе была ~1.67× медленнее спрайт-пака на Android (200мс/кадр gif vs
  120мс/кадр спрайтов, замерено напрямую по GIF89a-таймингам) → перепрошил delay-байты в 29 idle-gif
  (только idle — остальные позы на вебе завязаны на угаданные `ANIM_DURATION_MS`, трогать их без
  пересчёта констант рискованно, см. CLAUDE_DONE_TASKS.md). Оба сегодняшних фикса (коллизия одинаковых
  + скорость idle) подтверждены пользователем живьём.
- 2026-09-25 — звук-комментарий смерти (`destroyed`/`acid`/`burn`) играл синхронно с постановкой шага
  в очередь анимаций, а не когда терминальная поза реально стартует (живая жалоба, каскады отброс→…→
  destroyed) → перенесён на уже существующий `onStart`-механизм (`GameBoardScreen.js#triggerWithSound`,
  тот же паттерн, что у `deathOnStart`/`cellReleaseOnStart`/`mineOnStart`). `collision`/`anomalyHole`/
  `ricochet`/`miss` НЕ тронуты — их синхронность с событием осознанная (фикс 2026-09-20, обратный
  порядок звуков), пользователь подтвердил не трогать.
- 2026-09-25 — полный аудит "что фронт додумывает за бэк" по прямому запросу пользователя (два захода:
  сначала список проблем, затем контракт-уровень для бэка И фронта) — отправлен файлом, детали
  CLAUDE_DONE_TASKS.md. Попутно найдено и поправлено в этом же файле: `Lobby.version`/
  `RunnerPlayer.activeRunner` уже отданы бэком (старые заметки устарели), `ReaperValidator`-баг не
  воспроизведён при повторном чтении (статус пересмотрен на неподтверждённый).
- 2026-09-26 — обратный аудит: что бэк даёт, а фронт не использует. Найдено 5 пробелов, 4 из них
  почищены в этой же сессии (детали CLAUDE_DONE_TASKS.md): `ghost_consumed` теперь обрабатывается
  (`runnerGameReducer.js`); `lib/ghostPairs.js` переписан под реальное событие (было — несуществующий
  `ghost_pass`); `trackNext` теперь копируется в патч `game_track_updated`; `POST /api/logout`
  подключён (`api/auth.js#logout`, вызывается из `AuthContext#signOut`), подтверждено живьём в
  браузере (`POST /api/logout → 200 OK` в network-логе). Пятый — `POST /api/lobby/send` — НЕ чинил,
  это отдельная фича (см. «Мелкие заметки»), не баг.
- 2026-09-26 — пересобран холст `scout_healthy_start_*.gif` (все 4 цвета) со 168×168 на 216×216
  (персонаж по центру, пиксели не тронуты) — замерено PIL: персонаж занимал ~77% холста в `start`
  против ~60% в `idle` при одинаковом абсолютном росте (~130px), отсюда визуальный "прыжок размера"
  веб-версии при переходе start→idle. Подтверждено сравнением кадров до/после, живьём не проверено.
- 2026-09-26 — живая жалоба "уход с клетки, где Призрак допустил мирное сосуществование, показывал
  fly вместо move" → `pushedFrom`-эвристика в `runnerAnimTriggers.js` слепа к разнице
  "столкновение"/"Призрак" (оба выглядят как "старая клетка уже занята кем-то другим") → добавлена
  проверка через `ghostPairs`-Set (тот же, что уже передаётся в BoardGrid), теперь исключает
  ghost-совпадения из классификации отброса. Не проверено живьём (только логика).
- 2026-09-26 — по прямому запросу пользователя: пара при Призраке теперь разъезжается В СТОРОНЫ, как
  при коллизии (`BoardGrid.js#pushPair`, третий параметр 'idle' вместо 'collision'), а не рисуется
  двумя наложенными токенами — тот же геометрический разъезд, но поза idle, не "лицом друг к другу".
  Проверка перенесена ПОСЛЕ isArriving-гейта (было — до, минуя его) — иначе токен прыгал бы на офсет
  до конца слайда захода на клетку. Не проверено живьём.
- 2026-09-26 — прилёт Жнеца из-за края карты у оппонента: анимация была чисто локальной иллюзией
  ставящего игрока (`reaperPreviewItem`), реальный токен у остальных клиентов монтировался без
  анимации → добавлена та же off-map `enterFrom`-формула (+ 2x длительность) для НАСТОЯЩЕГО токена
  Жнеца при первом появлении (`BoardGrid.js`), структурно не задевает клиент ставящего игрока
  (у него этот kind анимации подавлен). Не проверено живьём.
- 2026-09-26 — блюр Скаута на Android (заметнее всего в позе коллизии): физически увеличены все 164 PNG
  скаута (healthy+damaged, 4 цвета) ×1.25 вместо доп. программного растяжения → подтверждено пользователем
  живьём на Android ("стало получше"). Детали — CLAUDE_DONE_TASKS.md.
- 2026-09-26 — веб: `destroyed`/`bomb` играли лишний кадр gif заново перед тем, как код прятал
  токен/переключал позу — оба таймера (`DESTROYED_HIDE_DELAY_MS`/`ANIM_DURATION_MS.bomb`) были РОВНО
  равны полной длине gif, без 2-кадрового запаса (та же дыра, что чинили для acid/burn/start ещё
  2026-09-12/13, но именно эти два тогда пропустили) → добавлен тот же запас 400мс. Не проверено живьём.
- 2026-09-26 — уход с клетки Жнеца (не только Призрака) ошибочно давал fly вместо move (найдено на
  Android живьём) — `pushedFrom`-эвристика теперь исключает и Жнеца (он никогда не толкает бегуна,
  делящего с ним клетку мимоходом — см. `Collision::reaperCollision()` на бэке, read-only), тем же
  способом, что и ghost-пары. Не проверено живьём после фикса.
- 2026-09-26 — квадратный фон торчал по углам плиток бегунов (декоративная дуга скругления не
  дотягивала до истинного угла бокса) → после нескольких неудачных попыток (радиус на весь угол резал
  саму рамку, сдвиг дуги дал "стало ужасно") — рабочий фикс: радиус клипа `wrap`, подобранный ПРОМЕРОМ
  (рендер уголка поверх контрастного тестового фона на разных радиусах, PIL), не на глаз. Тот же приём
  применён и к `FramePanel.js` (аватарки/кубики/усиления) — там радиус посчитан под свой ассет и теперь
  ДИНАМИЧЕСКИЙ (зависит от `scale` конкретного инстанса), экспортирован `frameNotchRadius()`, чтобы
  соседние оверлеи (заливка/PulseHighlight) не рассинхронивались. Подтверждено пользователем живьём
  ДЛЯ ПЛИТОК ("выглядят идеально"). Детали и известное ограничение (нижний уголок FramePanel —
  не подлежит полному фиксу без нового арт-ассета) — CLAUDE_DONE_TASKS.md.
- 2026-09-26 — 1px-смещения рамок на вебе (не Android), три симптома (чёрные поля вокруг рамки/сдвиг
  стенок Жнеца/поднятая грань у Скаута-Атлета) — не воспроизвёл ни точным замером, ни зумом браузера на
  временном отладочном экране → пользователь закрыл без фикса, статус не подтверждён. Детали —
  CLAUDE_DONE_TASKS.md.
